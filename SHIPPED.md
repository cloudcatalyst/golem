# Shipped

What has actually landed, newest first. One row per batch, with the outcome in a
line — the detail lives in the task doc and the wiki debrief it links to.

This table is written at close-out, per the checklist in `CLAUDE.md`.

| date | tasks | what shipped | outcome |
|---|---|---|---|
| 2026-08-21 | R11.6 | Every model on the status line wears one format — `<gateway> (<model>)` — with `+` joining models and `·` reserved for separating kinds | A user instruction, an hour after R10.24 landed: the chat model read `<gateway> (<model>)` while a worker read a bare id, and one glyph joined both the model list and the dial list so the line read as a flat run of fields. The ask could not be met without fixing the DATA first: `collectGolemState` passed a worker's raw `accountId` as its "gateway" (so a target with no account rendered gateway-less while the chat side said "ollama" for the identical provider), and `golem status --json` had no `gateway` on a worker row at all, which is where the VS Code extension gets everything it draws. Both now call `providerUpstreamLabel`, the function the chat segment already used. R10.24's parity test earned its keep on the first change after it shipped — failing on exactly the stale literal, passing everything else. Found and deliberately NOT fixed: the CLI prints `✂ off` where the status bar omits the segment, which `render.test.js` pins on purpose — every parity fixture used brevity "full", so a parity test only pins the states its fixtures visit. |
| 2026-08-21 | R11.3 | `golem on` / `golem off` persist `proxy.bypass_all` and apply it live, so the master switch survives a restart and every surface can see it | The switch was one POST to the running proxy's admin endpoint, which made it in-process only — and the non-persistence was the lesser half. `golem off` forwards raw with REDACTION OFF, while every status surface reads `proxy.bypass_all` from settings, so a bypassed machine rendered as a healthy pipeline with dials beside it and `REDACTION_OFF_WARNING` never fired. R11.1 had built the right home for that state a day earlier; the switch simply was not using it. Persisting fixed the reporting for FREE — no new status code, no fifth proxy state for R10.24's parity test to pin. Writing alone would have cost the instant apply (the setting is read at proxy construction), so it writes and then POSTs the same state live: durable and immediate. Nothing in the suite had ever covered these two commands, which is why R11.1's sweep missed them; the logic moved out of the commander callback so it could be tested at all. |
| 2026-08-21 | R11.5 | Every debrief is reachable from `WIKI.md` again, and `golem wiki check` fails on one that is not | Of 121 debriefs, 39 had no Index line — every one from 2026-07-16 on, all of R9 and R10. Not lost: the vector index still found them. What they had fallen out of was the GRAPH, so an agent starting at the Index and following wikilinks could not reach seven weeks of debriefs — the half the wiki exists for. `wiki check` passed throughout because it lints what a page CONTAINS (frontmatter, dates, resolvable links) and never asked whether anything pointed AT it. Backfilled with hooks read from each debrief, and the check now enforces it, scoped to `debriefs/` because those are append-only records whose whole value is being findable later. It caught its own debrief on the first run. |
| 2026-08-21 | R11.4 | R11.1's leftovers: four strings that still named the slider, a classifier branch for a tool that cannot be called, and the retired `/golem/slider` skill that `golem init` only CLAIMED to prune | All three found by deploying the previous release and reading its output — `golem init` printed “set compression.headroom_sidecar=true and slider ≥3”, a hint naming the control that release had removed. Pulling the thread: `golem config get`'s example key pointed at a setting that no longer exists; `classifyAction` still routed `mcp__golem__level` to a classifier whose five tests passed vacuously (removal is stricter, since an unrecognized tool falls through to `unknown`, which is gated); and `BASICS_SKILLS` carried a comment asserting that init prunes a skill dropped from the table, which `installSkills` — iterating that table — cannot do. `pruneRetiredSkills` makes the claim true under R9.5's provenance rule: byte-identical to what Golem wrote is Golem's to delete, edited or unrecorded is a reported conflict, so the install and prune sides now share ONE ownership rule. A retirement is done when the installed project stops mentioning the thing, not when the source does. |
| 2026-08-21 | R11.2 | The session-start index sync is bounded: a cap on how much it may embed unasked, a checkpoint so an interrupted run is not repeated, and one collection per project however its path is spelled | Keeping the KB fresh costs GPU time on the user's machine, and three faults made it pay far more often than the work required. Measured here: ~42 s for 8 changed files, ~10 minutes for 114 — and a branch switch rewrites mtimes wholesale, so an ordinary `git checkout` queued hundreds. `knowledge.auto_index_max_files` (default 50) now defers past the cap and names `golem index`, which stays uncapped because it IS the explicit ask. Worse than the size was the repetition: the manifest was written only after the last file, so any session shorter than the sync recorded nothing and the next session redid the identical work — now checkpointed every 20 files, deletions first because they cost no embedding. Third, `canonicalProjectId` folded drive-letter case only, so `d:/repo`, `D:\repo` and `D:\repo\` hashed three different ways: this repo had three collections, 231 MB, two of them unreachable. Windows spellings now collapse to one; POSIX ids are left alone, where `\` is a legal filename character. |
| 2026-08-20 | R11.1 | The slider is retired: `compression.level` and `brevity.level` are the only dials, and the full bypass becomes its own explicit `proxy.bypass_all` | The user's own status output made the case — `Slider: level 3 (aggressive) → effectively 1 (lossless)` plus a warning to explain why the number did nothing. A preset over two dials meant two controls for one thing; on a caching upstream (the default) its top half was inert. Best outcome: the stage table no longer has a redaction-free row, so "a dial turned redaction off" is unrepresentable rather than defended by a clamp — `MIN_ACTIVE_COMPRESSION_LEVEL` is gone with the scale it guarded. The `level` MCP tool, `golem slider`, `golem.setSlider` and the `slider` status block went too. Migration transforms rather than renames (one key → three, values computed), reproduces the retired resolvers exactly so a pin still wins, and was verified on seven fixtures plus this repo's real settings file. Found on the way: `golem off` is in-process only and silently reverts at every proxy restart. |
| 2026-08-20 | R10.24 | The CLI status line and the VS Code status bar render one canonical shape, the four proxy states are named in one vocabulary, and the VS Code picker can reach every configured model | The two lines had drifted apart in six ways (dial position, role order, gateway-vs-vendor label, model precedence, how many proxy states exist, and which glyph bypass gets) with nothing pinning them but a test on the GLYPHS — which is why they felt covered. A new parity test renders equivalent state through both implementations and demands one identical string; it immediately found the CLI drawing the redaction-only bypass shim as a filled green hexagon. Dials are no longer displayed in states where no stage runs (off/unwired/bypass), extending R8.32's rule from the glyph to the numbers beside it. The picker now offers TARGETS: `inference.default_target` accepts a target id and `golem target use` exists, so a gateway fronting two models no longer strands one of them. |
| 2026-08-19 | R10.23 | A mid-stream upstream failure reaches the client as an error instead of wearing the "empty completion" notice, OpenRouter reasoning traces stop being dropped, and Golem's own pipeline can no longer hold a live request silently | A user's deepseek-via-OpenRouter sessions dead-ended on Golem's empty-completion notice: the gateway's real cause ("Provider disconnected unexpectedly") arrives as a `data:` frame with a top-level `error`, and the translator dropped every frame without a `delta` — then closed the stream `end_turn`, so in-band prose read as a finished turn. Now relayed as Anthropic's own streaming `error` event, naming model, provider and cause, and retryable. Second dropped signal in the same file: OpenRouter returns the trace as `reasoning`, not `reasoning_content`, so every trace from every OpenRouter model was discarded (answers and corrects §84's open question). Third fault, from the same report's aside: the local-answer stage awaited a cold embedder inline on session-opening requests — now bounded at 2s, fail-open, and any hold past 750ms says so with a per-stage breakdown. |
| 2026-08-14 | R10.21, R10.22 | Claude Code's wiring moves to `.claude/settings.local.json`, `golem config set` defaults to the local scope, and the seeded guidance loses a third of its bulk | `golem init` no longer commits machine-local wiring (per-project port, hooks needing `golem` on PATH) into a file every clone receives — `claude.settings_scope` picks the file, default `local`, and init MOVES the wiring rather than duplicating it. Surfaced and fixed a second override path the two-file split creates: a user's `statusLine`/`defaultMode` in the committed file was left intact but inert. The seeded guidance rules, which load into every session, went 8,322B → 5,684B with four load-bearing facts restored. |
| 2026-08-14 | R10.12, R10.13 | The bypass shim is back, and daemon staleness sees code changes, not just versions | `golem proxy stop` no longer exists-and-kills: the port stays served by a redaction-only shim, so a stopped proxy answers 401 from upstream instead of connection-refused. `golem status` now reports a daemon that predates the last `npm run build`, which version-only comparison could never see. |
| 2026-08-14 | R10.16, R10.19, R10.20 | Stream keepalive, headroom_config reported where it is read, and synthesized thinking blocks labelled | A translated stream now pings during a long prefill instead of sending nothing for ~2 minutes (3 pings observed live). `golem status` names a `headroom_config` key that cannot reach Headroom even when the stage never runs. A thinking block Golem builds from `reasoning_content` now says so, after an unlabelled one was mistaken for cross-project contamination. |
| 2026-08-14 | R10.17, R10.18 | The CCR offload swap reaches `Read`, and an empty upstream completion surfaces as an error | `Read` output is offloadable for the first time (its text is nested at `file.content`; 62 unswapped in one project). An upstream that produces nothing now says so on both paths instead of returning a well-formed empty answer. |
| 2026-08-14 | R10.14, R10.15 | Vision-aware image translation for OpenAI-schema upstreams, and a local answer for `/v1/messages/count_tokens` | A duoop session that had answered nothing since 2026-08-13 answers again: 468k → 31k tokens (93.4%), 130s of silence → 4.4s. `count_tokens` returns `{"input_tokens":N}` instead of a billed completion. |
