/**
 * WS-E: settings hierarchy loader — user -> project -> local -> env -> per-request
 * (owned by agent-ux). snake_case keys; env overrides GOLEM_<SECTION>_<KEY>;
 * platform paths via env-paths.
 */
