/**
 * WS-E: local web dashboard — savings, slider level, stage attribution
 * (owned by agent-ux; task E3, dashboard v0).
 */

export type {
  DashboardHandle,
  DashboardOptions,
  DashboardSnapshot,
} from "./server.js";
export { REFRESH_MS, renderPage, startDashboard } from "./server.js";
