/**
 * WS-B: unified MCP server via @modelcontextprotocol/sdk (owned by agent-mcp).
 *
 * Frozen tool names (IMPLEMENTATION_PLAN §2.5): eol_search, eol_get_chunk,
 * eol_index_path, eol_expand, eol_stats, eol_set_slider, eol_delegate,
 * eol_devices. Prompts: slider, index, search, stats, expand, bypass,
 * devices, delegate (surface in Claude Code as /mcp__eol__<prompt>).
 */
