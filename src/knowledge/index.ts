/**
 * WS-C: vector knowledge base — ingestion, chunking, embed, rerank, watch
 * (owned by agent-knowledge). Embedded store per spec Decision 17 (LanceDB
 * candidate); optional Headroom-memory federation via the P2 sidecar.
 */
