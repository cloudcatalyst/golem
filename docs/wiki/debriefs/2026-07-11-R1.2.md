---
title: R1.2 — positioning decision + golem.run copy revision
type: debrief
tags: [positioning, roadmap, r1-batch, decision-32]
sources: [docs/plan/R1_BATCH.md, docs/golem-spec.md#decision-32, docs/wiki/syntheses/r1.2-positioning-universal-preprocessor.md]
created: 2026-07-11
updated: 2026-07-11
---

# R1.2 — positioning decision + golem.run copy revision

Batch gate task: get the assistant-for-Claude vs universal-pre-LLM-processor
call recorded (Decision 22 vs 23), not decide it unilaterally. See
[[R1.2 — positioning call, universal pre-LLM processor (Decision 32)]] for the reasoning;
this page is the what-changed record.

## What changed

- **Framed the two options** via `AskUserQuestion`, grounded in R1.1's finding
  (verification-notes §54) that levels 1/3 are pipeline-identical on
  Anthropic, and in §30-37's evidence for Decision 23's pillars. User chose
  **universal pre-LLM processor**.
- `docs/golem-spec.md` — new **Decision 32**: records the call, what it
  unblocks (R5.1/WS-F14 positioning gate), what it explicitly does NOT do
  (does not start WS-F* build work — that's still a separate ask per
  `R1_BATCH.md` §3), and reaffirms Decision 22's hard boundary (Anthropic
  byte-faithful path untouched) and Decision 23's evidence (unchanged).
- `README.md` — revised from "edge offload for Claude" (leads with
  compression) to lead with redaction / local tools / routing / honest
  observability, name Claude Code as the flagship integration, and keep
  compression's savings claim explicitly situational.
- `CLAUDE.md`'s "What this project is" — same repositioning, to keep the
  repo's own governing doc from contradicting the decision it now sits next
  to.
- `docs/plan/ROADMAP.md` — R5.1 row updated (unblocked, not yet scheduled);
  decision/research backlog's R1.2 and R1.1 entries marked resolved.
- `docs/plan/IMPLEMENTATION_PLAN.md` — WS-F14 row updated to reflect the
  resolved positioning gate.

## Design notes

- No frozen interface changed; no code lands with this decision — it is
  positioning + roadmap-priority only, matching Decision 23's own precedent.
- Deliberately did NOT start R5.1/WS-F14 build work. `R1_BATCH.md` §3 keeps
  all WS-F* work out of scope regardless of this call; Decision 32 unblocks
  the positioning gate on those items, not the build queue.

## Verification

- Docs-only change; ran `tsc --noEmit`, lint, format:check, `npm test` to
  confirm nothing else was touched incidentally.

See also [[R1.2 — positioning call, universal pre-LLM processor (Decision 32)]].
