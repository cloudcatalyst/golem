---
title: R4.7 — Drafter quality / catalog re-verification
type: debrief
tags: [r4, inference, coder, spike]
sources: [src/inference/catalog.ts, docs/plan/verification-notes.md]
created: 2026-07-15
updated: 2026-07-15
---

Spike (🔬) closing R4: is the co-developer's model catalog current, and how
good are the drafts?

## Outcome
- **Catalog re-verified (Decision 6), no change.** Qwen3-Coder is the 2026
  default for agentic work, but Qwen2.5-Coder still narrowly leads on
  single-function code (the drafter's job) and — decisively — `qwen3-coder` on
  Ollama ships only 30b/480b, so Golem's small tiers have no drop-in to switch
  to. Kept qwen2.5-coder; recorded optional P_MAX/non-coder upgrades as future,
  unmeasured-so-not-now. Full sourcing: verification-notes §63.
- **Draft-quality baseline measured (ungrounded, n=5): 2 accept / 3 revise /
  0 reject.** Pattern: accept-quality for small self-contained code,
  revise-quality for project-integrated code (invents plausible-but-wrong
  integration). Synthesis: [[R4.7 — drafter quality & catalog re-verification]].
- **R1.6 cross-OS checklist** still blocked (no non-Windows hardware) — the
  [[R1.6 — macOS/Linux Ollama verification blocked]] question page stands.

## Notes
- The n=5 sample doubles as honest dogfooding evidence: three of the five are
  this very batch's own coder drafts (R4.1/R4.2/R4.4), all of which needed
  rewrites — logged as-they-happened in each debrief.
- Grounded/refined re-measurement is a follow-up gated on an MCP reconnect
  (the running `golem mcp serve` predates the R4.2/R4.4 build); R4.3's
  `tool_usage` bucket is the instrument for tracking it.
- No code changed (spike); no interface touched.

