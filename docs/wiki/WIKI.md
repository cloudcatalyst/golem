---
title: WIKI
type: schema
tags: [meta]
sources: []
created: 2026-07-10
updated: 2026-08-21
---

# Golem project wiki — schema (Zone 0)

This directory is the project's durable knowledge store (spec Decision 28):
human-readable, committed to git, and the **first port of call** for Claude before
vector search or the outside world. The vector index under `.golem/knowledge` is a
derived, rebuildable cache of these pages — never the truth.

## Zones and write rules

| Zone | Where | Who writes | Rule |
|---|---|---|---|
| 1 — raw | `.golem/webcache`, `.golem/ccr` (local, gitignored) | Golem hooks | never committed; never hand-edited |
| 2 — wiki | `concepts/ entities/ sources/ syntheses/ questions/ artifacts/ debriefs/` | agent + human | **author freely** — create or refine pages without prior approval (spec Decision 44). Every write is committed to git, so it is diffable, reviewable, and revertible in history. Prefer append-and-refine over wholesale rewrites. |

> **Decisions (ADRs) live at `docs/decisions/`, outside this wiki** (spec Decision 44).
> They are human-driven dev artifacts with a stricter rule — accepted ADRs are
> immutable except status; superseded, never deleted — so they sit apart from the
> freely-authored wiki. `docs/golem-spec.md` remains authoritative for this
> project's own Decisions Log.

Hard rules for every write, agent or human (these still bind — de-gating is not a licence to skip them):

1. **Redaction before storage** — no secrets/PII ever land here (repo hard rule).
2. **Link, don't restate.** The wiki never duplicates what the code, `docs/`, or git
   history already record — link to the file/spec section instead. For this repo,
   `docs/golem-spec.md` stays authoritative for decisions.
3. **No raw fetched full-text.** Fetched pages live in the webcache (zone 1); what
   goes here is a distilled source note in our own words, citing the URL.
4. **Contradictions are reported to the human, never auto-resolved.**

## Page conventions

- Filenames: Title Case for `concepts/` and `entities/` (`Prompt Caching.md`);
  kebab-case slugs for `sources/`, `syntheses/`, `questions/`, `artifacts/`;
  `YYYY-MM-DD-slug.md` for debriefs. (ADRs — `ADR-NNNN-slug.md` — live outside
  this wiki at `docs/decisions/`, spec Decision 44.)
- Links: wikilinks (`[[Page Title]]`) between wiki pages; plain repo-relative paths
  for code/docs. Every page carries **at least one wikilink**.
- Required frontmatter on every page:

```yaml
---
title: Page Title
type: concept | entity | source | synthesis | question | artifact | adr | debrief
tags: [kebab-case]
sources: [urls or repo paths]   # where this knowledge came from
created: YYYY-MM-DD
updated: YYYY-MM-DD
---
```

- Format is Obsidian-compatible on purpose, but nothing may depend on Obsidian.

## Index

- [[Architecture]] — **visual map**: component topology, proxy request lifecycle, local/LAN/upstream routing, observability, and the PreToolUse guardrail stack (Mermaid diagrams, the deep-dive entry point)
- [[Wiki-First Knowledge]] — the pattern this wiki implements
- [[Guidance Rules]] — how Golem's working practices are stored as Claude Code `.claude/rules/golem-*.md` (seeded by init, toggled by `golem guidance`)
- [[Configuration Surfaces]] — one control surface (settings + guidance + runtime) behind the `golem` panel, `golem config`, and the VS Code webview; compile-enforced setting metadata, the pet, env-locked rows
- [[Cache Observability]] — R8.1: prompt-cache hit rate + prefix-bust detection (`golem stats --cache`); two signals (billed = authoritative, verdict = prediction) never merged; **measured 98.4% hit rate on this repo, which demoted bust-prevention to a guard rail** (§93)
- [[Managed Tools]] — spec Decision 53: external tools are **spawned or detected, never shipped**; the tier ladder (1/2/3a/3b), the three integration shapes, the admission bar, and `golem pkg` (which refuses to claim "running" and says why instead; the write half — consent-gated installs at a pin — landed in R8.14)
- [[Plugin Seams]] — the OTHER extension surface: third-party code running **inside** the redaction path. ADR-0005's headline is that there is no sandbox and Node cannot give us one, so every mitigation is structural — append-only redaction rules with built-ins always first, a pipeline stage whose output is redacted AGAIN, and no install verb at all
- [[Redaction Stage]] — rule table, entropy heuristic, known false-positive classes
- [[Usage Limit Park]] — Decisions 38/45: what the proxy's rate-limit reading, the 90% threshold, `PARK_EXEMPT_TOOLS` and the one-call `snooze(until, note)` add up to — and why a cold header feed warns instead of blocking
- [[Spawn Headroom Gate]] — the park is a tool-call gate and a subagent never reaches it (it dies on a *model request*), so Golem refuses the **spawn** instead: priced at a measured ~18% of a window per agent, charging siblings dispatched since the last reading, warning once rather than assuming headroom it cannot measure
- [[Redaction Path Placeholders]] — companion to Redaction Stage: what to do when a real path comes back as `[REDACTED:...]` (measure on disk, never re-paste from tool output), why `\` survives where `/` did not before §140, and the reversible-vs-standalone placeholder asymmetry (same-looking placeholder, only one kind restores)
- [[Compression Levels]] — the two dials that remain, `compression.level` (`off`/1/2/3) and `brevity.level`; **no dial value can disable redaction** (ADR-0004) — the full bypass is `proxy.bypass_all`, CLI-only and surfaced loudly; neither dial engages the local model (Decision 31)
- [[Compression]] — situational savings (Decision 23): lossless/CCR always pays, lossy semantic only on non-caching upstreams (~0% on Anthropic cached)
- [[CCR Ref Scope]] — ccr-ref-scope (2026-08-22): a git linked worktree is the SAME project as its main checkout for CCR purposes, resolved through the shared `resolveWorktreeRoot` (also used by `canonicalProjectId`) rather than raw `cwd`; `UnknownRefError` now names `location` and a real `reason` ("not-found"/"corrupt") instead of one "unknown or expired" — neither `CcrStore` nor `LocalDirBlobStore` has ever implemented eviction
- [[Repo Map]] — R8.5: whole-repo signature skeleton, graph-ranked and budgeted (`code` tool, `mode: "map"`) plus the symbol skeleton on an oversized `Read`; **measured +21.4 accuracy points for +57 tokens** against a plain path list (§101), byte-stable so it is cache-safe
- [[LSP Bridge]] — R8.6: `diagnostics`/`definition`/`references`/`hover` as MODES of the `code` tool, answered by a language server the USER installed; **+333 definition tokens when enabled, zero when off**, every wait bounded, and every absence a no-op rather than an error (§109)
- [[Change Ledger]] — R8.9: `golem checkpoint` snapshots the worktree to a **shadow git ref** (`refs/golem/ledger/*`) so a failed attempt can be DISCARDED instead of repaired; never a commit on your branch, never the index (throwaway `GIT_INDEX_FILE`), restore is human-gated *and* itself undoable, and no MCP tool — the guidance is a skill, because a definition bills every request
- [[Tool Search]] — Anthropic's on-demand tool loading (GA): defer_loading sends full defs but keeps them out of the cached prefix; why Golem relays it rather than shrinking the tools block itself
- [[Web Cache]] — WebFetch fetch-cache-serve flow (Decision 42 raw mode), freshness/revalidation, oversized → CCR ref
- [[Knowledge Base]] — RAG ingest + graph-first-then-vector search, scopes/federation, FileVectorDriver (vs spec's Qdrant)
- [[Auto-Index Cost]] — R11.2: what keeping the index fresh costs (**~5 s per file**, ~10 min for 114) and the three rules that bound it — checkpoint every 20 files so an interrupted sync resumes, `knowledge.auto_index_max_files` defers oversized automatic runs to `golem index`, and one canonical project id so path spellings can't fork duplicate collections
- [[Distillation Pipeline]] — capture -> distill -> promote data flow (capture + distill built, T4/T3)
- [[Blocked State Read Model]] — R12.2 / ADR-0006 §1: what Golem can tell a renderer about a session waiting on the human — which project, which session, blocked on **what** (the tool and the verbatim argument, since Claude Code's own notification names neither), and since when. Four statuses rather than a flag, so `abandoned` (blocked, stale, nobody ever wrote again) stops reading as "nothing is happening"; redacted at an unconditional, fail-closed choke point because this is the first Golem artefact whose *purpose* is to carry a tool argument
- [[Conversation Store]] — R13.2 / ADR-0007 §6: the deliberate, narrow exception to `session-tree.ts`'s content-hashes-only rule — one redacted-before-write JSON file per conversation under `.golem/conversations/`, bounded by count (32) and age (30 days) with the oldest evicted first, `golem session forget <id>`/`--all` to delete, identity shared with `session-tree.ts` via `cachePrefixFingerprint`, gitignore coverage proven with `git check-ignore` rather than assumed
- debriefs/2026-08-22-redaction-path-uuid-hex-chunk-allowance.md — a hex chunk now counts as clean in `isPathLikeToken`, so a UUID/hash segment no longer disqualifies a whole path from the path-like exclusion; safe by call order (a bare hex token is already excluded earlier), gated by a 3-chunk floor as defense-in-depth; verified end-to-end through a rebuilt/restarted proxy, both path families and both `/`/`\` spellings
- syntheses/wiki-knowledge-loop-batch.md — retrospective tying the T1–T7 batch + init-guidance work into one knowledge loop; records patterns + open follow-ups
- sources/llm-wiki-second-brain-obsidian.md — distilled source note for the
  originating article
- sources/local-coder-models-2026.md — distilled landscape of small local coder models (Qwen3-Coder vs Qwen2.5-Coder; Ollama tag availability), captured during R4.7
- sources/kimi-k3.md — Kimi K3 (Moonshot) API: OpenAI-compatible (base `https://api.moonshot.ai/v1`, model `kimi-k3`, bearer); fronted via Golem's `openai` provider + the reasoning/vision translator support (R6.1 b4-kimi)
- sources/agentic-token-saving-techniques.md — four families of agentic token-saving (caching, lazy tool-def loading, routing/cascading, compaction) mapped to Golem's stance; names the honest gaps (lazy tool-defs, cache-hit observability)
- `questions/` — open questions carried over from the Decision 28 proposal
- debriefs/2026-07-10-T7.md — entropy sweep path false-positive fix
- debriefs/2026-07-10-T1.md — wired durable `ccrRefsRetrieved` telemetry
- debriefs/2026-07-10-T2.md — shipped the missing `wiki-query`/`wiki-ingest` skills
- debriefs/2026-07-10-T4.md — shipped `golem note` capture (spec Decision 20f)
- debriefs/2026-07-11-T5.md — graph-first lookup ahead of vector search in `search`
- debriefs/2026-07-11-T3.md — distillation engine + lazy webcache distill (`golem wiki distill`)
- debriefs/2026-07-11-golem-init-guidance.md — baked wiki-promotion + local-model-first practices into the `golem init` guidance template
- debriefs/2026-07-30-control-panel-and-config-surfaces.md — the `golem` control panel + the one control surface behind CLI/TUI/VS Code; why the config layering needed no refactor, only metadata
- debriefs/2026-07-30-r8.5-repo-map.md — R8.5: the first item in the R8 context-economy line its own instrument APPROVED (`golem bench map`: 28.6% → 50.0% retrieval accuracy for +57 tokens/call, 22 cases × 3 repeats); three ranking bugs found by reading real output (exported-only graph targets, damping + affinity for a queried map, word-part/IDF matching instead of substrings); displacement (memo open question 3) still unmeasured
- debriefs/2026-07-30-r8.4-context-ledger.md — R8.4: `golem stats --context` attributes every token in the outgoing request to a bucket and resolves `tool_result` blocks back to the producing tool; first capture found the `tools` block at **18,827 tokens** (~5× §88's figure, promoting schema shrinking), Bash the biggest tool consumer at 36,968 across 132 results (quantifying the RTK case), and one `expand` costing 6,356
- debriefs/2026-07-30-r8.1-cache-observability.md — R8.1: `golem stats --cache` (billed hit rate + prefix-bust verdicts); the instrument's first measurement (98.4% hit rate, uncached input 0.06%) demoted its own bust-detection half to a guard rail and promoted the context-shrinking levers
- debriefs/2026-07-31-headroom-net-gate.md — the Headroom net gate closed offline: NET 8.7x–11.3x WORSE on caching traffic (§103), and `golem status`/statusline were reporting the nominal compression level instead of the effective one
- debriefs/2026-07-31-r8.13-cache-verdict-fix.md — R8.13: the verdict half was 98% wrong because `cache_control` (a breakpoint *marker*, not content) was in the fingerprint — §99's "colliding conversation key" hypothesis disproved by recording one more number (the bust index, always `prevCount-1`); 0% → 73% append, and surviving busts now bill 3.3x the cache write of an append; adds a `lookback` bust component for Anthropic's documented 20-block window
- debriefs/2026-07-31-park-locality-precedence.md — three `S` honesty fixes: the snooze park procedure's first step (`golem task add`) was denied by its own second step under Decision 45, fixed by folding the note INTO the `snooze` tool (structurally impossible, not exempted); `golem devices`/`golem local status`/the `devices` MCP tool now report per-role **pulled / not-pulled / unknown** against `/api/tags` instead of presenting the tier catalog as an inventory (the gap that cost the LE2 judge bug, §89 and §100), with the availability warning moved *before* the benchmark run; and §91's undocumented PreToolUse precedence closed on both halves (§105) — the docs now state `deny > defer > ask > allow`, and a live opt-in test against Claude Code 2.1.220 proves a `deny` still discards a peer hook's `updatedInput` rewrite
- debriefs/2026-07-31-r8.8-model-catalog.md — R8.8: per-model price and context limits as Golem's own cached data (§106) — `golem bench cost` reports real money, `golem stats --context` checks the window, `golem models list|refresh`; models.dev's WEB table shows several Anthropic models at $0.00 while its JSON API is correct, so the built-in dated table always beats the fetched one and nothing fetches implicitly; every "cannot price this" case stays visible (unattributed / unpriced / ambiguous / provider-unconfirmed); the telemetry allow-list dropped the new fields for the THIRD time (R8.1, R8.13, now `model`)
- debriefs/2026-07-31-p3b-caveman-shrink.md — P3b: Golem's existing tools gate pointed at `caveman-shrink`'s own implementation instead of rebuilding it (§107) — 53 of 1,089 description tokens (4.9%), accuracy unchanged, so a reproducible negative; resolved from the USER's install with absence as a hard refusal (an identity transform would publish a fake 0% under their name); also documents the install/config surface §87 could not find on their README
- debriefs/2026-07-31-r8.s2-system-prompt-slimming.md — R8.S2: declined with the arithmetic (§108) — the system prompt is 3,347 tokens = 0.92% of a 362k request, a 30% trim collects 0.205% of input cost (~$22/month) and one busted prefix costs 6.7x a request = 3,247x the saving; declined on ownership as much as size, the fourth input-side idea to die on "whose bytes are they, and how big is the prize"
- debriefs/2026-07-31-r8.6-lsp-bridge.md — R8.6: the LSP bridge shipped as four MODES of the existing `code` tool (`diagnostics`/`definition`/`references`/`hover`), not four tools — measured +333 full-definition tokens when enabled versus the ~250-320-token envelope four separate tools would each pay, and **zero when off** (the schema only grows when `knowledge.lsp_enabled` injects the bridge; the default census is 22 tokens cheaper than before); tier-2 spawn of the user's own `typescript-language-server`, every wait bounded, and 17 tests against a fake server prove every degrade path (absent binary, unclaimed extension, handshake/request timeout, mid-session crash, protocol desync) resolves to `available: false` rather than throwing (§109)
- debriefs/2026-07-31-r8.7-local-editor.md — R8.7: `coder` gains `mode: "edit"` — the local model rewrites one small file and **Golem validates it** — but the harness came first and rejected two of the three designs: `search-replace` **33.3%** semantic (half the replies were not even in the format) and `udiff` **33.3%** (perfect compliance, half the hunks unmatchable — the model paraphrases lines it was told to copy), against **whole-file 91.7% semantic / 100% apply** (§110); validation gained a **definition-loss guard** for the whole-file failure a parse check cannot see; +313 definition tokens when on, **byte-identical to R8.6 when off** (`inference.local_editor_enabled`, default false), and the fixture-scale arithmetic (~30 output tokens saved per edit) honestly does NOT repay that — the claim is conditional on bigger edits; `--repeats` proven useless at temperature 0 (only more cases sharpen the instrument)
- debriefs/2026-07-31-r8.9-change-ledger.md — R8.9: `golem checkpoint` over shadow refs, so a failed attempt is discarded rather than repaired; shadow refs + a throwaway `GIT_INDEX_FILE` were forced by the repo's "commit only when asked" rule and paid off (a snapshot is an ordinary commit, so `git diff refs/golem/ledger/<id>` needs no Golem command); destructive half in ADR-0002's never-auto set, safe half deliberately ungated so the habit stays cheap; NO MCP tool (a definition bills every request — the guidance is a skill); the live smoke test caught what 11 green tests missed (a restore deleted Golem's own `.golem/` state in a repo with no `.gitignore`), and restored files carry **git's** line endings, not the disk's
- debriefs/2026-08-01-r8.m1-upstream-switch-ui.md — R8.M1: one report ("the status bar says an upstream we're not using"), three independent defects, **none of them in the routing** — `golem proxy status/start/restart` read the legacy `proxy.upstream_base_url` leaf while the proxy correctly served the active account (fixed by resolving the banner through the same `resolveUpstreamDisplay` the runtime uses, so display and route cannot drift); the VS Code panel's setting toggles were **outvoted, not broken**, because the scope `<select>` pre-selected `project` while a `local`-layer value kept winning under `default < user < project < local < env` (any UI over a layered config must select the OWNING layer, or every control is a silent no-op for customized users); and "Switch upstream…" paid ~2.8s per open because `buildModel` expected a bare array where `golem account list --json` emits the `AccountsReport` object — a shape mismatch that degraded to correct-but-slow, which is harder to notice than a throw
- debriefs/2026-08-03-r8.28-server-split.md — R8.28: `src/mcp/server.ts` 2,213→130 lines, split into 9 domain modules (search/fetch/ingest, wiki tools, coder, code+LSP, prompts, devices+snooze, deps, shared); barrel unchanged and zero behavioral change, so the split is provable by the existing suite rather than argued
- debriefs/2026-08-03-r8.30-headroom-backoff.md — R8.30: two Headroom sidecar lifecycle defects from the fresh-eyes audit — `#cleanup` reset the start promise so every request re-attempted a spawn immediately (now exponential backoff 1s→2s→4s capped at 30s, reset on success), and a startup timeout logged a generic failure while holding the captured stderr that said *why* (now surfaced, so "uv couldn't find Python 3.13" is visible)
- debriefs/2026-08-03-r8.s3-session-tree.md — R8.S3 (spike): `SessionTreeRecorder` reconstructs a conversation's branch/fork shape from content hashes of observed request bodies (**no content stored**), persisted to `.golem/state/session-tree.json` and rendered by `golem session tree`; recording only — actuation stays out because Decision 37's relaunch limit (`claude -p --resume`) still stands
- debriefs/2026-08-06-r8.31-proxy-stop-bypass-shim.md — R8.31 / spec Decision 56: stopping the proxy left `ANTHROPIC_BASE_URL` pointing at a dead port, and the obvious fix (delete the key) **cannot work** — `env` in `.claude/settings.json` is not hot-reloaded (§13), so it fixes the next session and leaves the current one broken; so `stopped` now means *pipeline off* and a redaction-only shim keeps the port served, while going direct becomes the explicit `golem proxy unwire`/`wire` pair with the required reload surfaced; the trap worth remembering is that the cheap shim (`x-golem-bypass`/level 0) forwards **unredacted**, so building Stop on it would have made the single sanctioned redaction-off path reachable from a click that never mentions redaction — the shim pins level 1 and a *control* test asserts level 0 really does leak, because without it the redaction test could keep passing for the wrong reason; the ownership guard was moved out of `init.ts` and shared rather than re-derived, so a foreign base URL survives an unwire; two lifecycles ("is the process up?" vs "where is the client pointed?") had been treated as one, and a manual workaround documented in the wiki for weeks was the design gap announcing itself
- debriefs/2026-07-30-decision-53-managed-tools.md — spec Decision 53 (Workstream P of the R8 memo): the dependency-tier ladder written down + `src/ext/` registry + `golem ext`; the real invariant is "ship no third-party bytes", not "no binaries"; two audit fixes (`unpdf` was documented optional but mandatory; no `LICENSE` file); a pin is not a passthrough
- debriefs/2026-07-30-brevity-dial.md — spec Decision 52: the slider becomes a preset over two pinnable dials, `compression.level` (input) and the new `brevity.level` (output); Caveman is a prompt that shortens *replies*, not a compression library, which inverts Decision 23's economics because output tokens are never cached; ships OFF behind `golem stats --brevity`; five design errors caught by the repo's own guards (the redaction safety clamp being the important one); Workstream B (`tools`-block shrinking, ~900 tokens of measured headroom) scoped but blocked on a tool-selection-accuracy harness
- **ADRs live at `../decisions/`** (outside this wiki, spec Decision 44): `ADR-0001-file-watcher.md` (accepted: `node:fs.watch` behind a swappable `FileWatcher`), `ADR-0002-autonomy-approval-gates.md` (accepted: R5.4 autonomy threat model — PreToolUse allow/ask gate, fail-closed classifier), `ADR-0003-credential-storage-and-account-routing.md` (PROPOSED: R6.2 multi-account credential threat model + ToS scope)
- debriefs/2026-07-11-T6.md — implemented ADR-0001: `golem index --watch` / `ingest` tool `watch:true` now actually watch and incrementally reindex
- syntheses/r1.1-net-of-cache-ab.md — R1.1 live billed-`usage` A/B: level 1 vs 3 are pipeline-identical on Anthropic post-Decision-31, so there's currently nothing to A/B there
- debriefs/2026-07-11-R1.1.md — shipped `UsageSniffer`/`aggregateUsageByLevel` usage-telemetry infra + the gzip response-decoding fix it required
- syntheses/r1.2-positioning-universal-preprocessor.md — R1.2 positioning call (Decision 32): Golem commits to Decision 22's universal pre-LLM processor; R5.1/WS-F14 unblocked but not yet scheduled
- debriefs/2026-07-11-R1.2.md — recorded Decision 32 + revised README.md/CLAUDE.md copy to lead with redaction/local tools/routing/observability
- debriefs/2026-07-11-R1.3.md — fixed the §50 credit-card redaction false-positive (separator-format guard) + new T-C3 corpus cases
- debriefs/2026-07-11-R1.4.md — closed the §24 provider-key redaction gaps (Google/Stripe/GCP/Azure) with four new dedicated rules
- questions/r1.6-ollama-verification-blocked.md — R1.6 macOS/Linux/no-winget Ollama checklist rows still NOT YET RUN, no non-Windows hardware in this session
- debriefs/2026-07-11-R1.7.md — R1.7's cross-OS e2e smoke + Linux fs.watch matrix was already shipped (T-C2/T6); recorded the Ollama/uv CI-stub decision that was only ever implicit
- debriefs/2026-07-11-R2.5.md — Headroom `read_lifecycle` disable verified from pinned package source: possible but not the right lever; the cache-risky half is already off by default, real cache-safe mechanism is proxy-only and out of reach; recommends R2.6's actual shape
- syntheses/r2.1-avoidedupstream-spike.md — R2.1 spike: no telemetry exists yet for KB-answer substitution (`search`/`fetch`/`ingest`/`wiki_read` uninstrumented); the one real signal (CCR retrieval rate) is 0 misses in 1051 swaps — encouraging but indirect
- debriefs/2026-07-11-R2.1.md — recorded the R2.1 measurement gap + carried the "ship avoidedUpstream telemetry from day one" recommendation into R2.2
- debriefs/2026-07-11-R2.6.md — R2.6: opt-in `force_semantic_on_caching` bypass + `aggregateUsageBySemanticForced` A/B infra built and tested; the live real-traffic A/B itself deliberately deferred (restarting the dogfooded proxy mid-session was judged too risky to do unilaterally)
- debriefs/2026-07-11-R2.4.md — closed the §38 expand↔Headroom-CCR gap: `backfillHeadroomCcrRefs` verifies Headroom's `hash=` markers against SHA-256/MD5 of the elided content and backfills Golem's own CCR store, so `expand` recovers it with no marker-text changes
- debriefs/2026-07-11-R2.2.md — shipped Decision 24 sub-mode 1: proxy-side webcache context substitution (new pipeline stage 4, gated like the semantic stage) + a durable `avoidedUpstream` telemetry bucket
- debriefs/2026-07-11-R2.3.md — shipped Decision 24 sub-mode 2 (Decision 33): opt-in, off-by-default local-answer proxy-as-responder — new frozen `LocalAnswerService` contract + `ProxyRequest.respondDirectly` seam, decoupled from the slider, PROPOSED pending human review of a real served answer
- debriefs/2026-07-11-R3.2.md — R3 batch opens: real `.html`/`.pdf` text extraction (`src/knowledge/extractors.ts`) ahead of chunking, via the optional `unpdf` package for PDFs; `.pdf` was previously entirely unchunkable
- debriefs/2026-07-11-R3.3.md — opt-in `web-tree-sitter` WASM syntax-aware code chunker (`knowledge.syntax_aware_chunking`, default off); grammar packages are devDependencies only, never shipped to `golem-run` consumers
- debriefs/2026-07-12-R3.5.md — `distillNote`/`golem note distill [ts]`: shapes a captured note into a draft `question`/`artifact` wiki page, reusing the existing `.golem/distill/` draft storage and JSON-forcing distill machinery
- debriefs/2026-07-12-R3.4.md — user-scope `~/.golem/wiki/` federated read-only into `search`/`fetch` via `FederatedWikiReader`; `golem wiki synthesize` drafts a weekly through-line over recent debriefs + notes
- syntheses/r3.7-lancedb-scale-spike.md — `FileVectorDriver` benchmark: search stays fast to 30k chunks, but `#flush()`'s whole-collection `Array.join` hard-crashes between 30k-50k chunks (earlier than ROADMAP's assumed 10⁵); no-go on LanceDB today, recommends a cheap stream-write fix instead
- debriefs/2026-07-12-R3.7.md — recorded the LanceDB scale spike finding + go/no-go
- debriefs/2026-07-12-R3.1.md — Decision 34: chat-judge rerank via the existing "judge" role + `jsonSchema` on the frozen `InferenceService.chat()` (no interface change), opt-in `knowledge.rerank_enabled` decoupled from the slider per Decision 31
- debriefs/2026-07-15-R3.6.md — C4: MEMORY-scope federated search via the optional Headroom `[memory]` sidecar (`HeadroomMemorySidecar`); verified the real `easy.Memory` API (docs' `MemoryCategory` doesn't exist in source); search-only (no write path exists yet), opt-in `knowledge.memory_federation_enabled` decoupled from `headroom_sidecar`
- [[Dogfooding Golem]] — two-proxy stable/dev split, daemon lifecycle, promote flow, Headroom sidecar setup (relocated from docs/DEVELOPMENT.md by Decision 36)
- debriefs/2026-07-16-decision-36-refocus.md — Decision 36: roadmap refocused on the co-developer core (R4), old R4/R5 → R5/R6 ON HOLD; spec renamed golem-spec.md, EOL scrubbed, batch briefs retired
- debriefs/2026-07-16-R4.1.md — R4.1: `docs/plan/BACKLOG.md` ideas inbox + `/golem/plan` skill close the second-brain loop into tasks (plan-gated); the last leg of capture → distill → plan
- debriefs/2026-07-16-R4.2.md — R4.2: coder grounding — extracted shared `assembleHits`, `gatherGrounding` size-capped RAG injection into the local drafter (`ground` opt-out), degrades to ungrounded on any failure
- debriefs/2026-07-16-R4.3.md — R4.3: honest tool telemetry — `kind:"tool"` events for search/fetch/ingest/wiki_read/coder (coder tracks drafted-locally chars), surfaced in `stats` MCP tool + `golem stats`
- debriefs/2026-07-16-R4.4.md — R4.4: coder iteration loop — `refineDraft` (judge→revise, opt-in `refine`, best-effort fallback); `/golem/develop` hardened around grounding + refinement
- debriefs/2026-07-16-R4.5.md — R4.5: `golem wiki promote` closes capture→distill→promote (append-and-refine, Decision 26 consent); wiki-lint debt cleared (18→0), checker ignores code-fenced links, `wiki check` in CI
- debriefs/2026-07-16-R4.6.md — R4.6: `FileVectorDriver.#flush()` streams JSON lines (backpressure-aware) instead of one `Array.join` string — removes the ~30k–50k-chunk `RangeError` crash wall
- debriefs/2026-07-16-R4.7.md — R4.7: drafter catalog re-verified (no change — no small qwen3-coder tags); ungrounded draft-quality baseline 2 accept / 3 revise / 0 reject
- syntheses/r4.7-drafter-quality-baseline.md — R4.7 spike: catalog re-verification + measured coder accept-rate baseline (accept for self-contained code, revise for project-integrated)
- syntheses/r4-co-developer-core-batch.md — R4 batch retrospective: all 7 tasks (planning surface, coder grounding/telemetry/refinement, promote+lint, flush fix, re-verification); through-lines + open follow-ups
- debriefs/2026-07-16-R5.2.md — R5.2 (R5 batch opens): consolidated `SessionStateReport` (one zod payload for statusline/dashboard/VS Code/remote) + `golem watch` full-screen TUI (hand-rolled ANSI, no deps) + `.golem/` storage sizing; dashboard serves it at `/api/state`
- debriefs/2026-07-16-R5.1.md — R5.1: durable `TaskStore` (`src/tasks/`, one zod JSON per task under `.golem/tasks/`) + `golem task add/list/show/resume/cancel`; resume mechanism verified as headless `claude -p --resume` (no PTY, verification-notes §65); capacity gate via `notBefore`
- debriefs/2026-07-16-R5.4.md — R5.4: cruise-control autonomy (`src/autonomy/` levels/classifier/gate + `PreToolUse` hook + `golem autonomy`); threat model ADR-0002 written first; default-deny/fail-closed proven (never auto-approves destructive/outward); surfaced in the R5.2 report
- debriefs/2026-07-16-R5.3.md — R5.3: local conversation multiplexing (`src/tasks/multiplex.ts`) — service queued tasks locally (bounded concurrency, fail-open), explicit escalate-to-Claude folding the local pass as grounding (21a); `golem task run`/`escalate`
- debriefs/2026-07-16-R5.5.md — R5.5 (spike): prompt translation (`src/prompt/`) — local rewrite of a raw note into a clearer prompt, always shown/never sent/off proxy path, few-shot on accepted examples; `golem prompt translate/accept`; scoring loop demand-gated
- syntheses/r5-autonomy-orchestration-batch.md — R5 batch retrospective: all 5 tasks (dashboard sidecar, durable tasks, autonomy gates, local multiplexing, prompt-translation spike); through-lines (verify-first, default-deny, local-first explicit escalation, one state contract) + open follow-ups
- debriefs/2026-07-16-init-mcp-permissions.md — `golem init` now pre-approves Golem's MCP tools (`mcp__golem__*` allow + `wiki_upsert` ask); verified MCP permission-rule syntax against Claude Code docs
- debriefs/2026-07-16-webcache-revalidation.md — webcache freshness was pure-TTL, so a CHANGED docs page stayed served until the 7-day TTL lapsed; conditional revalidation added opt-in after a 4-6-day-old truncated capture was served during R4.7
- debriefs/2026-07-17-le5-decision-33.md — LE5: three embed-path bugs that made the semantic KB unbuildable (input bounding, batching, reindex dim-reset); Decision 33 semantic re-review + finding-#2 prose restriction; the wiki-first payoff ([[Compression Levels]], [[Compression]])
- debriefs/2026-07-17-pre-r6-loose-ends.md — PRE-R6 loose-ends closeout (LE1–LE5) + the auto-resume feature build; coder `refine` judge-model-not-pulled root cause fixed
- debriefs/2026-07-22-golem-snooze.md — spec Decision 38: Golem snooze parks a live session inside a blocking heartbeat tool call until the usage limit resets (#10–#16); limit-prediction observability + document-and-hold PreToolUse trigger, on by default
- debriefs/2026-07-22-coder-first-enforcement.md — spec Decision 39: the `local-coder` guidance becomes an enforced PreToolUse gate (deny the first non-trivial hand-written code write of a session, one-shot); "enforced if guided"
- debriefs/2026-07-22-autonomy-gate-toggle.md — spec Decision 40: decouple the autonomy gate from the shared PreToolUse hook — independently disableable (`golem autonomy disable`), on by default; fixes snooze activation silently turning on outward-command prompts
- debriefs/2026-07-22-decision-41-distribution.md — spec Decision 41 (R7): golem.run install one-liner (nginx UA-sniffing → install.sh/.ps1), npm-first tiered installer + Bun standalone binary, version single-source-of-truth, and `golem update` self-update surfaced in status/statusline/VS Code
- debriefs/2026-07-23-R6.1a.md — R6.1(a): the byte-faithful-safe half of the provider work — an Anthropic-native gateway needs no translation, which is why §73 split it away from case (b) and shipped it first
- debriefs/2026-07-23-R6.1b1.md — R6.1(b1): the response-transform seam, established on the NON-streaming path because that is where it is simplest to get right
- debriefs/2026-07-23-R6.1b2.md — R6.1(b2): streaming translation (OpenAI SSE → Anthropic events) — the path that actually matters, since Claude Code almost always sends `stream:true`
- debriefs/2026-07-23-R6.1b3.md — R6.1(b3): `tool_use` ↔ `tool_calls` both ways — without it a coding agent's whole tool exchange is lost against an OpenAI-schema upstream
- debriefs/2026-07-23-R6.1b4-gemini.md — R6.1(b4): Gemini is a SECOND upstream schema, not OpenAI Chat Completions, so it needed its own translator plus a proxy-seam extension; the API was verified (§77) before a line was built
- debriefs/2026-07-23-R6.2-account-switching.md — R6.2: multi-account/provider selection, scoped by ADR-0003 to legitimate switching only — the quota-evasion half (21e) is explicitly out
- debriefs/2026-07-23-R6.4.md — R6.4: `golem bench cost` turns Claude Code's own “Manage costs effectively” doc into explicit benchmarks; it went first in R6 because it was the only task with no security/ToS gate
- debriefs/2026-07-23-statusline-golem-dir-gating.md — status surfaces run in every project, so they must not footprint non-Golem repos: best-effort writers never bootstrap `.golem/` — the local-model cache (PR #20) and the update-check cache (PR #27, full writer audit) — the VS Code bar/CLI-poll stay quiet outside Golem projects, and the "off" state (proxy stopped or level 0) reads `Passthrough → [local + ]<upstream>` on both surfaces (PR #25)
- debriefs/2026-07-23-webfetch-raw-cache.md — spec Decision 42 (Option A, PreToolUse replace): the pre-hook fetches the RAW page itself on a miss (`fetchRawPage`), caches/ingests it and serves it via `deny` so WebFetch never runs on the happy path (fail-open on fetch failure); not Claude Code's prompt-specific answer; config `knowledge.webcache_fetch_raw` (default on); the local-answer length-gate stays (fail-open path still transits the proxy)
- debriefs/2026-07-24-R6.1b4-kimi-reasoning-vision.md — Kimi K3 works through the existing `openai` provider (no new code); the OpenAI translator gained `reasoning_content`↔Anthropic `thinking`, `reasoning_effort` passthrough, and image passthrough
- debriefs/2026-07-25-architecture-diagrams.md — committed architecture diagrams for the pipeline/proxy/knowledge surfaces
- debriefs/2026-07-25-golem-skills-expansion.md — expanded the seeded `/golem/*` skill set
- debriefs/2026-07-26-golem-managed-credentials.md — spec Decision 46 (amends ADR-0003 invariant 2): an OS-store credential chain (macOS `security` / Linux `secret-tool` / Windows DPAPI, no native deps) with `golem account login/logout`, a fail-closed preflight in `account use`, and CLI-owned resolution injected into the daemon at spawn — ends the "key missing in every new terminal" failure
- debriefs/2026-07-29-credentials-local-model-display-fixes.md — spec Decision 47: removes the env-var credential backend entirely (`golem account login` is the only way to set a key; stdin for CI; the var name survives only as the internal CLI→daemon handoff) and makes `account remove` log out first; adds the `golem local status/enable/disable/url` group for the local/LAN model; fixes two stale displays — the VS Code status bar advertising a disabled local coder, and every surface showing the previous account's model after an upstream switch (`servedModelFor` + clear-on-switch)
- debriefs/2026-07-29-verbatim-model-ids.md — spec Decision 49 (USER): model ids print verbatim on every surface (statusline, `golem status`, VS Code bar/hover/panel) — the R6.2 "friendly" prettifiers (`Opus 4.8`, `Qwen 2.5`) only ever worked for Claude ids, so one line mixed two naming schemes once the upstream showed a raw id; all three helpers plus their VS Code mirrors deleted rather than deprecated
- debriefs/2026-07-29-openrouter-case-b.md — spec Decision 48: `openrouter` moves from case (a) to case (b) (its Anthropic endpoint serves only Claude models, so byte-faithful made the entire free tier unreachable and silently ignored the account's `model`); `vendor/` prefixes preserved for multi-vendor gateways; route/base-URL mismatches warned at `account add`; the credential probe now reports the request URL it does NOT test; the proxy banner names the resolved account instead of the top-level config
- debriefs/2026-07-30-decision-55-plan-tasks.md — Decision 55: roadmap work becomes committed task docs under `docs/plan/tasks/`, reducing ROADMAP.md to a generated index — the same Task concept that parks a session at a usage limit
- debriefs/2026-07-30-r8.s1-tool-schema-shrinking.md — R8.S1: REJECTED by its own instrument — nothing shipped to the request path, the measurement harness shipped instead. A negative result, recorded as a result
- debriefs/2026-07-30-workstream-b-tool-selection.md — Workstream B: the ~900-token tools block and the housekeeping backlog; what remained after Decision 52 was blocked on hardware, a credentialed act, or a user deferral
- debriefs/2026-08-06-r8.32-unwired-proxy-honesty.md — R8.32: found by dogfooding and reported as a QUESTION rather than a bug — a running proxy nothing was pointed at; every status surface now cross-checks wiring against the listener
- debriefs/2026-08-09-R9.7-webfetch-red-dot.md — R9.7: Golem's BEST path — cache hit, no network, no summarizer call — was the one that rendered as a failed tool call
- debriefs/2026-08-09-p3a-claude-md-compaction.md — P3a: the actuator behind R6.4's leanness check — a reviewed CLAUDE.md rewrite, reporting the measured ~1-3% rather than a claimed saving
- debriefs/2026-08-09-r8.33-level-zero-cli-only.md — R8.33: the `level` MCP tool could set slider **0** (redaction OFF), persisted across sessions, guarded only by a warning in the response *after* the write; narrowed to 1–3 at two gates, with level 0 a CLI-only act per ADR-0002 threat item 4. A guardrail that runs after the write is a receipt, not a control
- debriefs/2026-08-09-r9.1-target-registry.md — R9.1: `proxy.targets`, one registry for every local and upstream model (a local model is just a target whose provider is `ollama`); the design was decided by reading the six accounts already in this repo's config, five sharing one credential while naming different models — the many-targets-one-account case in production
- debriefs/2026-08-09-r9.10-coder-naming.md — R9.10: pointing `coder` at a `claude-sonnet-5` target made the naming contradiction visible inside a single object — workers route to ANY target, so “local” had to go
- debriefs/2026-08-09-r9.13-config-file-migration.md — R9.13: R9.6 made a retired key keep WORKING, but nothing ever fixed the file — migration now runs on version change, with a backup, so adoption is not manual
- debriefs/2026-08-09-r9.14-mcp-credential-resolver.md — R9.14: `coder` was already pointed at Sonnet 5 and already 401ing — the MCP server could not authenticate to any credentialed target
- debriefs/2026-08-09-r9.15-claude-cli-target.md — R9.15: spend the subscription quota without borrowing its token — spawn the official client rather than lifting its credential
- debriefs/2026-08-09-r9.16-stale-vscode-extension.md — R9.16: config right, CLI right, DEPLOYED extension three releases behind — staleness is now reported, and only `golem init` fixes it
- debriefs/2026-08-09-r9.2-multi-target-proxy.md — R9.2: the blocker was one constructor line (a single undici `Pool`); per-origin pooling makes concurrent multi-target serving fall out for free. Strictly additive — every byte-fidelity test passes unmodified — with an unknown target dialling *nothing*
- debriefs/2026-08-09-r9.3-coder-any-target.md — R9.3: `src/mcp/` had no redaction calls, which was correct while the only reachable model was local; giving `coder` a remote target turns that sound omission into an egress hole, so reversible redaction ships in the same slice, and `trust: "local"` is honoured only on a genuinely loopback endpoint
- debriefs/2026-08-09-r9.4-role-named-status.md — R9.4: the status line's *grammar* (`local + upstream`) could no longer express the truth once either end could be any target; now chat plus only the workers that diverge, with `inference.worker_targets` keyed by worker so adding a writer is one line
- debriefs/2026-08-09-r9.5-managed-file-refresh.md — R9.5: two opposite bugs from one missing question — a re-init OVERWROTE a hand-edited skill (silent data loss) and NEVER refreshed a guidance rule; a provenance record decides which
- debriefs/2026-08-09-r9.6-config-migrations.md — R9.6: a settings file naming a dead key loaded with exit 0 and a warning only `golem status` printed — the setting silently stopped taking effect while every surface reported success
- debriefs/2026-08-09-r9.8-marker-free-default.md — R9.8 / Decision 57: marker-free by default — CCR refs in tool output made an exact-match `Edit` fail against the file's real bytes, so the model edited what it SAW, not what was there
- debriefs/2026-08-09-r9.9-config-set-objects.md — R9.9: `golem config set` coerced booleans, numbers and arrays and nothing else, so every object-valued leaf — half the schema — was unwritable
- debriefs/2026-08-10-R9.12-webfetch-green.md — R9.12: a served WebFetch renders GREEN by default, behind a CA name-constrained so it cannot vouch for anything but loopback; every unwired session still gets R9.7's honest fallback
- debriefs/2026-08-10-R9.17-R9.18-two-wrong-diagnoses.md — R9.17 + R9.18: three wrong diagnoses for two defects, both closed by observation rather than reasoning
- debriefs/2026-08-10-R9.22-ca-path-local-scope.md — R9.22: `golem init` stops writing a machine-absolute CA path into a COMMITTED file, and deletes one an earlier init left behind
- debriefs/2026-08-13-R10-deploy-incident-and-three-bugs.md — R10.3/R10.4: a deploy broke the live session and exposed three more bugs — Headroom sidecars outliving the proxy that spawned them, and a hardware-tier downgrade silently swapping the embedder
- debriefs/2026-08-13-R10.1-first-pancake-part-1.md — R10.1 (part 1): the first pancake was not burnt, just big — ~1,200 LOC of genuinely dead code deleted, four monoliths split, three unenforced invariants turned into tests, no test lost
- debriefs/2026-08-13-R10.2-suite-flake.md — R10.2: the flaky suite was paying a retry-prone recursive delete PER TEST — and hiding a real corruption bug; ten consecutive green full runs afterwards
- debriefs/2026-08-13-R10.7-R10.10-routing-and-the-reporting-path.md — R10.7-R10.10: the default that was never a default (`coder` fell through to LOCAL, not `inference.default_target`), and a status bar flapping on a healthy proxy
- debriefs/2026-08-14-R10.12-R10.13-a-shipped-decision-fell-out.md — R10.12/R10.13: a SHIPPED decision fell out of a rewrite and nothing noticed for three days — `proxy stop` again left Claude Code wired to a dead port
- debriefs/2026-08-14-R10.14-R10.15-images-as-prose.md — R10.14/R10.15: a screenshot became 125k tokens of PROSE and the model answered nothing — 467,764 → 30,817 tokens (93.4%) once translation went vision-aware
- debriefs/2026-08-14-R10.16-R10.19-R10.20-liveness-and-attribution.md — R10.16/R10.19/R10.20: liveness and attribution — a slow translated stream pings instead of going silent for two minutes, an unreachable `headroom_config` key is reported even when its stage never runs, and thinking blocks round-trip
- debriefs/2026-08-14-R10.17-R10.18-silent-fallbacks.md — R10.17/R10.18: two “can't fail” fallbacks that turned diagnosable errors into silence — `Read` output was never once offloadable, and an empty upstream completion was manufactured into a valid empty answer
- debriefs/2026-08-14-R10.21-wiring-belongs-in-the-local-file.md — R10.21/R10.22: Claude Code's whole wiring belongs in the gitignored `.claude/settings.local.json`, not in the committed file — and seeded guidance was three times longer than it needed to be
- debriefs/2026-08-14-R9-R10-tail-seven-tasks.md — R9/R10 tail: seven tasks closed, and the four bugs the fixes found on the way
- debriefs/2026-08-19-R10.23-the-error-frame-golem-threw-away.md — R10.23: the translator dropped every frame without a `delta`, so a mid-stream upstream `error` reached the user as Golem's empty-completion notice — and every OpenRouter `reasoning` trace was discarded with it
- debriefs/2026-08-20-R10.24-two-status-lines-one-truth.md — R10.24: a user read both status lines side by side and listed five faults in one sentence — every one real; a parity test now renders equivalent state through both and demands one identical string
- debriefs/2026-08-20-R11.1-the-control-that-needed-a-paragraph.md — R11.1: if a dial needs a paragraph to explain why its number does nothing, the dial is the problem — the slider is retired and `proxy.bypass_all` takes the one job no dial may have
- debriefs/2026-08-21-R11.2-what-the-index-was-quietly-spending.md — R11.2: the expensive bug was REPETITION, not size — the index manifest was written only after the last file, so any session shorter than the sync recorded nothing and the next one redid it all; now checkpointed every 20 files, with `knowledge.auto_index_max_files` bounding the automatic session-start run (a `git checkout` rewrites mtimes wholesale, so hundreds of changed files is routine) and one canonical project id after three collections, 231 MB, two unreachable
- debriefs/2026-08-21-R11.4-what-the-retirement-left-behind.md — R11.4: a retirement is done when the INSTALLED project stops naming the thing, not when the source does — `golem init` still printed a `slider ≥3` hint, `golem config get` still gave `slider.level` as its example, a classifier branch for the removed `level` tool kept passing five vacuous tests, and `/golem/slider` was still installed because the comment claiming init prunes retired skills described machinery nobody had built; the prune now exists, sharing R9.5's provenance rule so it never deletes an edited file
- debriefs/2026-08-21-R11.6-one-format-for-every-model.md — R11.6: the R10.24 parity test earned its keep on the FIRST change after it shipped, failing on exactly the stale literal; a worker had no gateway to print (`collectGolemState` passed the raw `accountId`, and the status JSON had no field at all), so a formatting ask found a missing field; and the brevity-off divergence beside it turned out to be a DELIBERATE decision with a test pinning it — a parity test only pins the states its fixtures visit. Plus the mid-stream drop Golem could not prove its innocence of: no timestamps, no request outcomes (R11.7)
- debriefs/2026-08-21-R11.7-proving-your-own-innocence.md — R11.7: Golem was cleared of a live "Connection lost mid-response" honestly, but every fact came from OUTSIDE the proxy — a log with no timestamps is not a log, and a line written BEFORE forwarding records an intent, not an outcome, so a request that died looked exactly like one that succeeded. Now: one timestamped sink, one outcome line per request across six kinds, and a stream ending without `message_stop` named as TRUNCATED — the signal only a proxy holding both sockets can see. Built on the sniffer already reading SSE event names rather than a second stream hop
- debriefs/2026-08-21-R11.8-a-dial-at-zero-is-still-a-dial.md — R11.8: a parity test only pins the states its fixtures visit — R10.24's all used brevity "full", so the two lines differed by a whole segment for a release inside the one file whose job is to notice. The tell was two dials on one line under opposite rules (`🗜 off` shown, `✂ off` hidden); an absent segment reads as an absent dial, not as a value. "Nothing is running" and "a dial is at zero" are different questions, and both now have their own parity case
- debriefs/2026-08-21-R11.3-R11.5-the-switch-that-kept-nothing.md — R11.3 + R11.5: state that existed, mattered and was visible to nothing — `golem off` flipped an in-memory flag, so redaction was OFF while every status surface (which all read `proxy.bypass_all`) still drew a healthy pipeline; persisting it fixed the reporting for free. Same shape in the wiki: 39 debriefs had no Index line, searchable by embedding but unreachable by graph traversal, and `wiki check` passed because it linted what a page CONTAINS and never asked what pointed at it
- debriefs/2026-08-21-r8.14-pkg-write-half.md — R8.14: the write half of `golem pkg` is mostly REFUSALS, and that ratio is the design — two rows get an automated path, every other one explains why not and quotes its documented route. `upgrade` cannot move a pin because upgrade IS install (`"reinstall"` expands to the install argv, byte-identical, so there is nothing to guard); consent comes from asking `decideGate`, not from re-implementing its conclusion, so the user reads the gate's own reason and one test over `AUTONOMY_LEVELS` pins it. Adding `remove` also exposed the read half lying: `pluginOnDisk` checked the CONTENT CACHE, which `claude plugin uninstall` leaves behind, so Caveman read `[found]` for ten days while `claude plugin list` said nothing was installed — a cache is not an inventory
- debriefs/2026-08-21-r8.11-plugin-seams.md — R8.11: the task said "if it needs a sandbox to be safe, say so in the ADR rather than shipping" — it does, Node cannot provide one, and saying so in the words a user would use IS the deliverable. What made the decision defensible was comparing against the STATUS QUO rather than zero: extending redaction meant forking Golem, and a fork drifts out of date with fixes to the redaction stage itself. Every mitigation is therefore structural — every "yes" in the threat table is a property of the seam, every "no" a property of the runtime, and two rows are deliberately unmitigated. The best answer to "can a plugin stage weaken redaction" was not to audit it but to REDACT AGAIN over its output
- debriefs/2026-08-21-R12.2-blocked-state-read-model.md — R12.2: the payload you need is not in the payload you get — Claude Code's Notification hook says "Claude needs your permission" and names no tool, so the tool and argument had to come from PreToolUse and be correlated ~6 s later. R11.8's lesson went unheeded one release: `golem statusline` printed `⏸ waiting` while the VS Code status bar an inch away printed nothing, and nine parity assertions missed it because not one fixture was blocked. Writing the test found the design bug too — the pending record skipped the gate's forced `ask`, i.e. the single highest-stakes prompt Golem can produce
- debriefs/2026-08-21-docs-slider-drift-the-check-that-stops-the-sixth-line.md — docs-slider-drift: R11.1 retired the slider and R11.4 swept the code, but neither task listed the README, so the front page taught the retired control for a release. Fixing five lines was ten minutes; the task was the check that stops the sixth — `golem wiki check` now fails on a retired identifier in prose that TEACHES, while staying silent where the wording IS the record. The exemption list is the whole design: dated-record zones are never scanned, a unit citing a record is exempt (this is what keeps the Index legal), and a unit naming the retirement is exempt. Whole-section scope would have broken the primary case, since one honest "retired" line in `## Index` would have pardoned the drift the task was filed about — so every list item is its own unit. `level N` is deliberately NOT in the pattern: compression is still a 0–3 dial. The sweep found 22 hits across 8 files, including a redaction page naming a dial value as the way to turn redaction off, a copy-pasteable `golem slider 3`, and two symbols that no longer exist
- debriefs/2026-08-21-R12.6-a-doorbell-not-a-channel.md — R12.6: the reopened option 1 (relay-originated push) is refused by one bullet in ADR-0006 §3b — sending to a VAPID-restricted subscription needs a JWT signed by the key bound at subscribe time, so the relay would hold a signing key, and forwarding a pre-signed one still hands over a capability URL ("Knowledge of a push URI implies authorization to send push messages") plus offline addressability, which §3b never conceded. The argument that settled it was not cryptographic: the blocked laptop is awake BY DEFINITION, so it can send its own push and relay-origination buys only IP-hiding from Apple/Google. Web Push bodies verified genuinely opaque (RFC 8291), but the Push API's own privacy section concedes timing, frequency and size with padding as "the only known mitigation" — encrypted and private are different claims. Apple's no-silent-push rule turned out to BE the feature (push count == notification count, no covert channel possible); Declarative Web Push is the right primitive because ITP deletes service worker registrations of rarely-visited sites and a companion app is rarely opened by construction. Every LAN-only alternative fails at the same point — locked and off Wi-Fi — and ntfy's own maintainers say instant iOS push is impossible "without a central server". The transport answer then exposed a bigger question: a subscription is bound to an ORIGIN, and the app's origin is a LAN address that changes with the network, so R12.5's "the UI must not know which transport it is on" hardens into a requirement on the origin and lands on R12.8
- debriefs/2026-08-22-R12.1-the-line-that-kept-adr-0002-intact.md — R12.1: narrowing the capability until ADR-0002 needed no amendment; a reversed non-negotiable recorded as a reversal; and why an ADR that says "impossible" ages badly where "declined, and here is why" does not.
- debriefs/2026-08-22-R12.7-the-channel-that-appeared.md — R12.7: asked to re-verify Decision 37's "a phone cannot resume a session" and found the reason had expired after five weeks. Measured against the installed client `2.1.235`, Claude Code now has three supported ways to put a turn into a running session, and the material one is an **MCP server**: **channels** ("a channel is an MCP server that pushes events into your running Claude Code session" — `capabilities.experimental['claude/channel']`, `notifications/claude/channel` with `content`/`meta` over stdio, arriving in context as `<channel source=…>`), plus **Remote Control** (`claude --rc` — the Claude app drives your live local session) and **cross-session messaging** ("when the receiving session is idle, Claude Code starts a new turn with the message"; v2.1.224+, macOS/Linux only). Anthropic also relays permission prompts to a channel (`notifications/claude/channel/permission_request`, local dialog held open in parallel, first answer wins) — ADR-0006 capability 2, built first-party, converging independently on R12.3's design. Decision 37 turned out to be two claims in one sentence: the architectural half still holds verbatim (the proxy sits below Claude Code, `/resume` is a client-side command it never sees), the product half ("no IPC into an already-running interactive session") is now false — so ADR-0006's capability-3 row and spec Decision 59(g) are superseded on their REASON, not their outcome, and carry dated pointers instead of an agent's silent rewrite. The outcome survives with better reasons: no continue button, because the channel path is opt-in per session by launch flag ("being in `.mcp.json` isn't enough"), off the Anthropic-curated allowlist, gives **no delivery acknowledgement** ("drops the events silently and returns no error to your server", so a phone button can only lie), rides an explicitly unstable contract needing claude.ai/Console auth that does not compose with Golem's account routing — and, the one objection that is not contingent, **authoring a turn is a larger authority than answering one**, so injected free text routes around ADR-0002's class line rather than sitting behind it. Lesson: "we cannot" rots and "we will not" does not, so every "structurally" in the tree is a place to check the date. Honest substitute unchanged — enqueue, do not inject (R5.1) — kept out of R12.5 and, if ever surfaced, labelled "Add a task for later" with "Nothing runs until your laptop picks it up.", never beside Approve/Deny. R12.5's sentence for the screen: *"There is no prompt box — this screen can answer what the agent is already asking, but nothing here can start a turn."* Evidence: verification-notes §136; follow-up R12.10
- debriefs/2026-08-22-subagent-park-gate-the-spawn.md — `subagent-park`: naming the mechanism ("the park gates tool calls, the limit kills on a model request") is what made the fix findable — the child can never be gated, so the spawn is; a spawn is priced as a span of burn rather than a call, from the incident's own measurements; the in-flight term is where the three-at-once fan-out was actually lost; and fail-closed had to be split from fail-hard so a blind gate warns once instead of bricking spawning (R9.23's lesson)
- debriefs/2026-08-22-ccr-ref-scope-worktree-identity.md — ccr-ref-scope: `expand` returned "Unknown or expired CCR ref" for refs a hook had written minutes earlier from inside a git worktree — the hook rooted its write on `cwd`, `expand` reads from the main checkout, same refId, two `.golem/ccr` directories. A grep for `prune|evict|ttl|maxEntries` found neither `CcrStore` nor `LocalDirBlobStore` prunes, so "expired" was always a guess. Reproduced with a real `git worktree add` and a real hook run BEFORE fixing (`0f37604`). Decided a worktree is the SAME project as its main checkout for CCR purposes, agreeing with `canonicalProjectId`'s identical call for the vector index — both now resolve through one new shared, synchronous, subprocess-free `resolveWorktreeRoot` (reads the `.git`-file/`commondir` chain git itself writes) rather than two independent opinions (R11.2's shape, repeated). `UnknownRefError` gained `location` and `reason: "not-found" | "corrupt"` so a miss names what was checked and where. Verified end-to-end against rebuilt `dist/` artifacts, not just source. Evidence: verification-notes §138
- debriefs/2026-08-22-docs-slider-drift-remainder-spec-and-vscode-readme.md — docs-slider-drift-remainder: the gap the predecessor task filed rather than left silent — `golem wiki check`'s allowlist covered every wiki page and `README.md`, but not `docs/golem-spec.md` or `vscode-extension/README.md`, both still teaching the retired slider. Widening the allowlist was the easy half; verifying whether the existing per-unit exemptions already covered the spec's own Decisions Log was the real question, and they did not — most of its 44 slider mentions cite a decision or `verification-notes` by name rather than the literal path/extension the existing rule requires, so loosening it would have loosened it everywhere. Added a heading-scoped exemption instead (`decisionsLogStartLine`: skip any unit at or after a `## … Decisions Log` heading), leaving the per-unit rules exactly as strict. The spec rewrite caught two bugs a rename pass alone would have missed — a compression level referenced that never existed even pre-ADR-0004, and a line describing a feature Decision 31 removed outright rather than renamed. The VS Code README rewrite was grounded in `render.js`/`extension.js`'s actual current model before any prose was touched, catching along the way that the pre-existing text conflated **Passthrough** (proxy stopped) with **Bypass** (proxy running, `bypass_all` on — Decision 56). Two leftover code strings fixed (`ui-model.ts`'s brevity summary, `header.ts`'s `slider`→`level` rename). Left deliberately open: `src/dashboard/server.ts`'s DOM ids (owned by R12.6) and stale prose in `render.js`'s own code comments (not one of the three named surfaces). Evidence: verification-notes §139
- debriefs/2026-08-22-R12.11-channel-wins-conditionally.md — R12.11: the spike asked whether Golem can enforce ADR-0002's class line on an approval that arrives through the channel relay R12.7 found, or whether that requires the from-scratch transport R12.3/R12.4/R12.8/R12.9 were paused to build. Load-bearing discovery: `src/autonomy/gate.ts`'s `decideGate` never emits `deny` — its type is `"allow" | "ask" | null`, and `destructive`/`outward` gets `ask` at every autonomy level. `ask` forces a question, it does not resolve one, and a forced question is exactly what opens the dialog the relay is documented to notify on with no exemption field — so Golem's *shipped* mechanism does not by itself hold the line against the relay. What closes the gap without a new transport: `code.claude.com/docs/en/hooks` documents an unused **`PermissionRequest`** event, firing "when Claude Code is about to ask you for permission" strictly before `PreToolUse`'s `ask` would lead to a dialog, with its own `hookSpecificOutput.decision.behavior: "allow"|"deny"` shape — "if no hook returns a decision, it denies the tool call." A real `deny` there, ahead of `PreToolUse`, means no dialog ever exists for `destructive`/`outward`, and therefore nothing for a relay to be notified of — a hook fix Golem already owns, not a new pairing/relay/account stack. **Recommendation: channel wins — conditionally** — the deciding property is whether the class line resolves before a dialog can exist, and that property is documented but not yet live-confirmed (no PTY harness exists in-repo to drive an interactive session). Disposition: `R12.3`/`R12.4`/`R12.8`/`R12.9` cancelled, `R12.5` reduced to observe-only, `R12.12` (`owner: agent`, the hook fix) and `R12.13` (`owner: user`, the live interactive confirmation) filed. Evidence: verification-notes §141; [[R12.7 — the channel that appeared: continue is not approve, and the reason expired]]; [[R12.1 — ADR-0006, and the line that kept ADR-0002 intact]]
- debriefs/2026-08-22-R13.2-conversation-store.md — R13.2: ADR-0007 §6's deliberate, narrow exception to `session-tree.ts`'s content-hashes-only rule — a redacted-before-write local conversation store, bounded by count and age, forgettable one-at-a-time or entirely. Redaction proven structural with a runtime-generated secret, never a hardcoded one. Identity shared with `session-tree.ts` via `cachePrefixFingerprint`; worktree resolution shared with `ccr-ref-scope` via `resolveWorktreeRoot`. `.gitignore` coverage proven with `git check-ignore`, not assumed. Found and fixed along the way: a Commander.js quirk where a parent command and a child subcommand declaring the SAME option flag (here `--dir`) silently drop the CLI-typed value to the child's own default rather than what was typed — root-caused with three isolated single-file repros outside the codebase, fixed by reading the parent's parsed value via `command.parent.opts()`. Evidence: verification-notes §143; [[Conversation Store]]
- debriefs/2026-08-29-R12.5-companion-app.md — R12.5: the companion app turned out to be the dashboard that already existed, bound one interface wider. R12.11 had already reduced it to observe-only (Anthropic's channel relay carries remote authorize first-party), so what was left was reachable, phone-shaped, installable, and honest when disconnected. Lessons worth carrying: **the security argument for a LAN bind has to be structural** — read-only here is the absence of a write route plus a method guard, asserted across four methods x five paths, not a policy that could be forgotten; **`0.0.0.0` is a bind target, not a destination**, so LAN mode enumerates the addresses a phone can actually type; **a PNG is CRC'd chunks around a zlib stream** and `node:zlib` is built in, so three home-screen icons cost ~130 lines instead of a dependency; **iOS ignores the web manifest** and reads `apple-touch-icon` instead; and **no service worker, deliberately** — it cannot register over plain-HTTP LAN anyway, but the real reason is that a caching worker is a machine for showing stale data and the gate forbids exactly that. The real-device half of the gate is R12.14 (`owner: user`). Evidence: `docs/plan/tasks/R12.5.md`; [[Blocked State Read Model]]
- concepts/Device Authentication.md — who may SEND: two independent claims, both required, neither able to stand in for the other — a client certificate from the project's device CA (mutual TLS) and a live unlock window. Failure is denial, never degradation. The observe-tier dashboard is a different server with no write route, so an unpaired browser still gets it. TWO CAs deliberately (one is installed in a trust store so clients trust our server; the other is installed nowhere and identifies a client). Enrolment is local-only forever — the claim route is reachable but can only succeed because a human ran the local command seconds earlier. Revocation bites on the next request because the catalog is never cached. [[Autonomy Gate]]
- debriefs/2026-08-29-R13.4-device-and-user-auth.md — R13.4: **~1,180 lines of this task were already merged on `main`, and none of it ran** — no CLI registration, middleware imported by nothing, `requestCert` absent from the whole repo, zero tests, and a code comment citing a verification-notes section that belongs to another task for the one fact the gate said must be MEASURED. Replaced rather than repaired (USER, 2026-08-29). Lessons: **"merged" is not "wired"** and nothing in a green build distinguishes the two; **a fabricated citation is worse than no citation** because it reads as settled and stops the next person looking — and here it blamed secure contexts (solvable) instead of WebAuthn's RP-ID naming rule (not); **no cache is the feature** that makes revocation effective on the next request; `rejectUnauthorized: false` is right because a handshake-level rejection is unreadable on a phone, with the certificate verified explicitly per request instead. Evidence: verification-notes §146; [[Device Authentication]]
- concepts/Autonomy Gate.md — the approval gate: autonomy level x action class, enforced through TWO hooks that are wired and unwired together. The never-auto invariant (no level auto-approves `destructive`/`outward`), the fail-safe rule (every failure path exits 0 with no stdout; no path emits `allow` on error), and the one detail that bites: `PreToolUse` takes a FLAT `permissionDecision` while `PermissionRequest` nests `decision.behavior` with `message` — emitting the wrong shape is a silent no-op. [[Blocked State Read Model]]
- debriefs/2026-08-28-R12.12-permission-request-gate.md — R12.12: R12.11 found that Golem's gate never emitted `deny` for `destructive`/`outward` — it emitted `ask`, which **forces a question but does not answer one**, so a permission dialog opened and a connected relay channel is notified *when a dialog opens*. Fixed by deciding one event earlier, at `PermissionRequest`, where the docs say *only the decision object can grant or deny the request*. `PreToolUse`'s `ask` deliberately KEPT underneath — it is what carries the audit log and pending-call record, and what still stands if the earlier hook is unwired. `unknown` is deliberately NOT denied: fail-closed means make the human decide, not decide for them one event earlier. What it could not prove — that the deny pre-empts a live channel relay — is R12.13 (`owner: user`), because headless `-p` cannot open a dialog at all. Evidence: verification-notes §141; [[Autonomy Gate]]
- debriefs/2026-08-28-R13.12-default-coder-subagent.md — R13.12: `inference.default_coder` naming a MODEL makes `golem init` generate `.claude/agents/golem-coder.md`, so delegating a coding task to a different model is native — real tool use, own context, and traffic still through the proxy so redaction and telemetry apply. The constraint that shaped it: **an MCP server cannot spawn a harness subagent**, so Golem owns the WIRING (generate the artifact) and is honest at the point of use (`coder` declines and names it) rather than simulating the capability. Records why the coder prompt is NOT a skill (R9.11’s layering rule: wrong direction, wrong portability, wrong semantics) and why a plain model id beats `golem/<target>` (§114 caveat 5 still open). Two lessons worth carrying: a generated file people are meant to edit needs provenance from day one, and **generation implies de-generation** — a step that writes a file from config must remove it when the config stops calling for it. Evidence: `docs/plan/tasks/R13.12.md`; [[Configuration Surfaces]]
- debriefs/2026-08-28-R13.13-target-surface-honesty.md — R13.13: two `golem` surfaces that stated the opposite of the truth while the logic behind them was correct. `target list` rendered `key MISSING` for a provider reached without a key, because the warning list asked about the PROVIDER (correctly exempting a self-hosted model server) while the renderer asked only whether an account id was present — fixed by computing the fact ONCE in the collector and carrying it on the row, so two surfaces cannot answer differently. `gateway add` warned that a model id would be ignored on a spawn provider that pins `--model` explicitly, because `!isTranslatingProvider` was standing in for "forwards the client’s model id" — different sets. The lesson worth keeping: **a predicate borrowed as a proxy for a related question will eventually be asked about a case where the two come apart**, and a display branch that re-derives a fact the collector already knows will eventually disagree with it. Evidence: `docs/plan/tasks/R13.13.md`; [[Configuration Surfaces]]
- debriefs/2026-08-23-R13.1-hosted-multi-turn-viability.md — R13.1: the spike ADR-0007 §3a was waiting on. Client `2.1.235`, eight items measured, six OBSERVED. **Verdict: the `claude` CLI qualifies as the §3a runner** — `-p --input-format stream-json --output-format stream-json` takes a second user message on stdin after the first turn's `result` event without exiting, holds one `session_id` across both, honours `ANTHROPIC_BASE_URL` so the proxy sees every request, and loads the project's hooks exactly as an interactive session does. Two gaps that are behaviour, not viability: `child.kill("SIGINT")` does not interrupt a running turn on Windows (process-kill only; POSIX UNESTABLISHED), and the usage-limit park is Golem's own concept that a spawned process does not participate in. A second close-out written independently the same day reported the opposite and was **withdrawn 2026-08-29** (USER ruling: §142 stands) — see the `### 143.` tombstone in verification-notes. Evidence: verification-notes §142
- debriefs/2026-08-27-R13.11-coder-inherit-auth-and-stateless-dispatch.md — R13.11: `coder` returned a bare `401` whenever nothing routed it, and a retried draft "looped" — two user-reported symptoms with one root cause each, both reproduced live before any code changed. `upstream_auth_scheme = "inherit"` answers two incompatible questions: for the PROXY it means "forward the client’s own headers" (right), for a DISPATCH that originates its own request it can only mean "send nothing" — a guaranteed 401 against `api.anthropic.com`, and the pre-flight guard exempted exactly that case. So R10.8’s step 4 had never once worked, while `golem status` and the audit record were truthful throughout: **a gate phrased over observability surfaces can be green while the behaviour they describe has never happened.** A test even asserted the defect as intent. Latent defect beside it: `makeAuthMapper("inherit", key)` discards any key, so a stored credential could never reach a draft. The "looping model" was a stateless transport — `DispatchRequest` carried one prompt, so every call was turn one; qwen3.7-flash said so directly. Fixed with `originationAuthScheme()`, a non-error `NoDrafterConfiguredError` decline that hands the work back inline (USER decision, chosen over a local-Ollama fallback), and `attempts`/`system` on the request — whose turns are redacted under ONE shared placeholder table, because per-message tables would silently CORRUPT restoration rather than leak. Evidence: verification-notes §144; [[Redaction Stage]]
