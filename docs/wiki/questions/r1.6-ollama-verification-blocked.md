---
title: R1.6 — macOS/Linux Ollama verification blocked
type: question
tags: [ollama, r1-batch, environment, blocked]
sources: [docs/plan/verification-notes.md#§48, docs/plan/R1_BATCH.md]
created: 2026-07-11
updated: 2026-07-11
---

## Blocked — no non-Windows hardware available in this session

ROADMAP R1.6 asks for the three remaining rows of verification-notes §48's
manual Ollama install/pull checklist to be run: macOS (with Homebrew), macOS
(no Homebrew), and Linux. This session's environment is Windows-only — no
macOS or Linux machine, and no CI runner reachable from here — so none of the
three rows can actually be exercised. Windows is already done (§48, 2026-07-09).

Not attempting to guess or simulate the behavior; per `R1_BATCH.md` §1's
log-and-move-on guidance, this is left open rather than falsely marked
verified.

## What would unblock this

Whoever next has access to a macOS or Linux box (or a CI runner with
interactive/manual-step capability) should run `golem ollama status` then
`golem ollama setup` per §48's table and record the dated outcome there,
same as the Windows row.

See also docs/plan/verification-notes.md#§48. Related: [[Dogfooding Golem]].

