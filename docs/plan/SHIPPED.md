# Shipped

One line per landed release or task. **Detail lives in the linked wiki debrief and the
spec Decisions Log** — this file is an index, not a record, and it is append-only.

Split out of `ROADMAP.md` on 2026-07-30, because ~150 lines of shipped prose was
burying the open work. Full prose for anything here is in git history and in
`docs/wiki/debriefs/`.

Legend: 🔬 research/spike · 🧭 decision · 🔒 security/ToS gate · 🛠️ build

## Milestones

| what | when | record |
|---|---|---|
| **P0** — `npx golem-run init` on 3 OSes, byte-faithful proxy at level ≤1, `/golem/*` skills + `/mcp__golem__*` prompts, redaction verified | 2026-07 | IMPLEMENTATION_PLAN §4 |
| **P1 + the wiki knowledge loop** — live and dogfooded daily | 2026-07 | WS-W W1–W4 |

## Releases

| release | shipped | one-line outcome |
|---|---|---|
| **R1 — Honest baseline** | 2026-07-11 | Net-of-cache A/B infra (§54), positioning Decision 32, redaction false-positive + provider-rule fixes, cross-OS e2e smoke. Remainder: R1.6 (blocked on hardware). |
| **R2 — Real savings, evidence-gated** | 2026-07-11 | Cache-safe bypass A/B infra, `avoidedUpstream` telemetry + context substitution, local-answer contract (Decision 33), expand↔Headroom-CCR backfill. Remainder: R2.6 (needs a non-caching upstream). |
| **R3 — Knowledge depth** | 2026-07-12 → 07-15 | Chat-judge rerank (Decision 34), HTML/PDF extractors, opt-in tree-sitter chunker, user-scope wiki federation + weekly synthesis, note→distill shaping, MEMORY-scope federation via the Headroom memory sidecar, LanceDB no-go spike (R3.7). |
| **R4 — Co-developer core** | 2026-07-16 | `/golem/plan` + BACKLOG inbox, coder grounding (`gatherGrounding`), honest per-tool telemetry (closed the §59 gap), coder refine loop, distill-draft promotion + wiki-lint cleanup, `FileVectorDriver.#flush()` stream-write fix, drafter-catalog re-verification. 7/7 tasks. |
| **R5 — Autonomy & orchestration** | 2026-07-16 | Durable task queue (`golem task`), dashboard-as-sidecar + `golem watch`, local conversation multiplexing + explicit escalate, cruise-control autonomy with approval gates (ADR-0002, default-deny/fail-closed), prompt-translation spike. 5/5 tasks, suite 922 → 1018. Retrospective: `syntheses/r5-autonomy-orchestration-batch.md`. |
| **R6 — Multi-provider & remote** (core) | 2026-07-23 | R6.1 provider adapters — Anthropic-native gateways (a) + OpenAI/Ollama-LAN/Gemini translation (b1–b4, incl. SSE and tool-use); R6.2 account switching (Decision 21d, ADR-0003); R6.4 `golem bench cost`. PRs #31–#40, suite 1159 → 1237, Anthropic path untouched. Retrospective: `syntheses/r6-multi-provider-batch.md`. Open: R6.3, 21e, R6.1-live. |
| **R7 — Distribution, versioning & self-update** | 2026-07-22 → 07-23 | Version single source of truth, tiered install scripts + nginx UA map, `bun --compile` binaries + tag-triggered Release workflow, install-method-aware `golem update` with status/statusline/extension surfaces, interactive control panel (`golem`, Decision 50), CLI startup latency work (Decision 51). Open: R7.3, R7.5, R7.6-infra. |

## Standalone decisions and tasks

| what | when | one-line outcome |
|---|---|---|
| **PRE-R6 loose-ends closeout** | 2026-07-17 | Cleared LE1–LE5: Decision 33 ACCEPTED, grounded/refined coder quality measured, grounding into `golem task run`, ledger tidy, semantic embed-path robustness. |
| **Golem snooze** (Decision 38) | 2026-07-22 | Park a live session in-place until the usage limit resets, via a heartbeating MCP wait-tool + limit prediction from the rate-limit headers. Enforcing by default (Decision 45). |
| **Auto-resume REMOVED** (Decision 37) | 2026-07-18 | Phase 1 reverted, Phase 2 abandoned unbuilt: a proxy cannot drive Claude Code's interactive TUI. The durable task queue is unaffected. |
| **Coder-first enforcement** (Decision 39) | 2026-07-22 | PreToolUse gate denies the first non-trivial hand-written code edit of a session and redirects to `coder`. |
| **Autonomy-gate toggle** (Decision 40) | 2026-07-22 | `golem autonomy disable` separates the gate from the hook wiring. |
| **WebFetch raw-page caching** (Decision 42) | 2026-07-23 | The pre-hook fetches the raw page itself and serves it, so the cache holds pages rather than prompt-specific answers. Falls open. |
| **Wiki authorship de-gated; ADRs relocated** (Decision 44) | 2026-07-25 | Agents author wiki pages directly — git makes every write reviewable. ADRs move to `docs/decisions/` under a stricter rule. |
| **Credentials: OS-store chain, env backend removed** (Decisions 46, 47) | 2026-07-26 → 07-29 | `golem account login` becomes the only way to set a key; the var name survives only as the internal CLI→daemon handoff. |
| **Verbatim model ids** (Decision 49) | — | Ids print exactly as configured; no prettified names anywhere. |
| **Always-on upstream provider + model display** (#46, #63) | 2026-07-24 → 07-29 | `resolveUpstreamDisplay` (`src/providers/`) plus `src/proxy/served-model.ts` (last-served model, written fail-open from `onResponseUsage`) surfaced across all three status surfaces: `golem status`'s upstream block, the statusline's destination segment (`→ anthropic (claude-opus-5)`), and the VS Code status-bar hover tooltip. For a translating provider the *configured* model is the honest current model; `served-model.json` covers the live/last-served case when they differ. Obeys Decision 49 — ids print verbatim. |
| **Brevity dial — the slider becomes a preset over two dials** (Decision 52) | 2026-07-30 | Decoupled `compression.level` from the new `brevity.level`, with `slider` demoted to a preset and a pin that wins and sticks. The first dial attacking **output** tokens (never cached, ~5×). Ships OFF behind a `golem stats --brevity` rollup that reports saving and cost together. |
| **Windows suite flake fixed** | 2026-07-29 | All 85 temp-tree deletes share a retry-hardened `rmTemp`; three consecutive full-suite runs green. |
| **Workstream B — tools-block shrinker REJECTED** | 2026-07-30 | Built the tool-selection-accuracy harness (`golem bench tools`, 27 labelled cases), then let it decide: whitespace saves **0**, first-sentence saves 56% but **triples false positives**. Native tool search verified GA and found to be Anthropic's to run; `golem init` already set `ENABLE_TOOL_SEARCH=true`, so that path was live and unasserted — now guarded. §89. |
| **Managed tools: the dependency-tier ladder + `golem ext`** (Decision 53) | 2026-07-30 | Wrote down the policy Headroom/Ollama/Caveman had converged on — external tools are spawned or detected, **never shipped** — as a tier ladder, three integration shapes, and a four-criterion admission bar the Caveman speech skill deliberately fails. Spawn-free `PATHEXT`-aware detection that refuses to report "running". Two audit fixes (`unpdf` genuinely optional, `LICENSE` added). §90–§92. |
| **R8.1 — cache-hit observability + prefix-bust detection** | 2026-07-30 | `golem stats --cache`: billed hit rate beside a per-request prefix verdict, two signals deliberately never merged. **Its first measurement re-ranked R8** (§93): 98.4% hit rate, uncached input 0.06% of billed input → ~83% of input cost is re-reading an already-cached context. |
| **R8.2 — STRUCK, not built** | 2026-07-30 | The memo proposed suffix-only tool-result dedup as novel; `native-lossless.ts` has shipped it since task A2 (Decision 18), achieving cache-safety by *purity* rather than by special-casing the suffix. Measured: **4,449,703 tokens saved**. Lesson: check the code before proposing. §94. |
| **R8.3 — line-aware digests** | 2026-07-30 | Rescoped twice by its own evidence: Grep/Glob had no measured traffic, so the oversized-output digest became **line-aware** instead — names the ranges it shows and the range it elided, and recommends a narrow re-read *before* offering `expand`. §97. |
| **R8.4 — the context ledger** | 2026-07-30 | `golem stats --context` attributes every token in the outgoing request to a bucket and resolves each `tool_result` back to its tool. First capture (§95) moved three things: the `tools` block measured 18,827 tokens, **Bash** is the biggest tool consumer (36,968), and one `expand` cost 6,356. |
| **R8.10 — supply-chain hardening** | 2026-07-30 | `.npmrc` `save-exact` + `min-release-age=2` (verified working on npm 11.12.1), all 6 direct deps pinned exactly, `npm-shrinkwrap.json` generation for published tarballs, `verify:deps` wired into `npm run check` with a ≤5 runtime-dependency ceiling. §98. |
| **R8.12 — coexistence with an output-compacting peer (RTK)** | 2026-07-30 | The finding was not the predicted one: Golem's autonomy **allow**-list was start-anchored, so `rtk vitest` fell through to a prompt — fail-closed but irritating, and irritation loosens safety settings. Fixed by retrying only the allow-list against the unwrapped command. Also: `buildDigest` preserves an external compactor's recovery pointer. §96. |
| **R8.S1 — tool-schema shrinking REJECTED** (#72) | 2026-07-30 | The tools-block line of work, closed. Of the 18.8k block, **93.9% is client built-ins**; Golem's share is **1,130 tokens = 0.8% of a 139k request**. §89's "~2900 of input schemas" was an artifact — the real figure is ~1,128, the rest being `outputSchema` + MCP metadata Claude Code never forwards. Shipped instead: per-definition ledger attribution, a schema-aware render, and an argument-construction gate. §100. |
| **`golem init` stops gating `wiki_upsert`** (Decision 54, #72) | 2026-07-30 | Decision 44 un-gated wiki authorship in every document but `init` kept writing a `permissions.ask` rule, and `ask` beats `allow` — so the gate outlived the decision in the mechanism. |
| **Plan tasks + roadmap restructure** (Decision 55, #74) | 2026-07-30 | Roadmap work becomes **committed Golem tasks** under `docs/plan/tasks/` — same `Task` shape and same `golem task` CLI as a parked session, so an item can be handed whole to a fresh agent. `ROADMAP.md` reduced to a generated index; this file created. |
| **R8.5 — repo map + the oversized-`Read` skeleton** | 2026-07-30 | The first R8 context-economy item its own gate **approved**: `golem bench map` measured retrieval accuracy **28.6% → 50.0% (+21.4 points) for +57 tokens/call** over a plain path list, 22 labelled cases × 3 repeats. Ships tree-sitter symbols → import/reference graph → personalized rank → budgeted, byte-stable skeleton, reached via **one** `code` tool with a `mode` parameter (~262 forwarded tokens) and via the symbol skeleton now attached to a swapped-out oversized `Read`. Three ranking bugs were found by reading real output, not by reasoning. Displacement (does the model then *skip* the read?) remains unmeasured. §101. |
| **Headroom's net gate measured + the effective-level fix** | 2026-07-31 | Closed the question §34 left open for 25 days. Gross savings are *larger* than recorded (**7.08% / 21.69%**, scaling with session length) but net on caching traffic is **8.7×–11.3× WORSE** than not compressing: `read_lifecycle` diverges from real history at **message 6 of 4,631**, forfeiting a prefix billed at a 98.4% hit rate. Decision 31's gate is confirmed correct and `force_semantic_on_caching` is now proven unsafe rather than merely unproven. `scripts/measure_headroom_cache.py` is the reusable bar for any future compressor — report first-divergence, not just gross tokens. Also fixed the misreport it exposed: five surfaces said "aggressive" while the pipeline ran lossless; the **label** now carries the effective level, not a footnote under it. §103. |
| **R8.13 — the cache verdict was 98% wrong about a marker** | 2026-07-31 | §99's leading hypothesis (a colliding conversation key) was **wrong**, and disproving it cost one recorded number. `cache_control` — a breakpoint *marker*, not cached content — was in the prefix fingerprint, so Claude Code moving its breakpoint to the newest block each turn made **every** turn a false `bust` at index `prevCount-1`. Anthropic's docs address the case outright: a block later unmarked "will still be considered a cache hit". Live result: **0% → 73% append**, billed hit rate 99.0%, and the surviving busts are real — they bill **3.3× the cache write** of an append turn (2,951 vs 892 tokens), where before the fix the verdict predicted nothing at all. Also: a bust now reports *how deep* it landed (a tail rewrite is not a full re-prefill), and a fourth component `lookback` models the documented **20-block lookback window** — a bust where nothing changed. §104. |
| **Three `S` honesty fixes: park locality, model availability, hook precedence** | 2026-07-31 | Each surface was telling the reader something untrue. (1) `snooze-taskadd`: Decision 45's enforcement denied `golem task add` — the park procedure's own **first step** — so `snooze` now takes a `note` and files that durable local task itself, *before* the wait; the ordering problem is structurally impossible rather than exempted, and `golem task add` is still denied. (2) `local-models`: `golem devices` / `golem local status` / the `devices` MCP tool presented the tier **catalog** as an inventory (4 of 7 slots were not downloaded here); they now report per-slot **pulled / not-pulled / unknown** against `/api/tags`, and `golem bench` warns *before* scoring — the caveat §89 and §100 each wrote by hand afterwards. (3) `hook-precedence`: §91's undocumented PreToolUse precedence closed on both halves — the docs now state `deny > defer > ask > allow`, and the case they still omit (a peer returning only `updatedInput` racing a `deny`) is pinned by an opt-in live test against Claude Code 2.1.220: both hooks fire, the deny wins, the rewrite is discarded. §105. |
| **R8.8 — model catalog: price and context limits as Golem's own data** | 2026-07-31 | `golem bench cost` reports **real money** instead of reference baselines only: per-model billed spend from the sampled `usage` events, priced from a two-layer catalog where **Golem's own dated table always beats the fetched one** (models.dev's web table shows several Anthropic models at `$0.00` — §106). Nothing fetches implicitly; `golem models refresh` is the only network path. Ids stay verbatim (Decision 49) and every failure mode is visible rather than folded into the total — *unattributed*, *unpriced*, *ambiguous*, *provider-unconfirmed*. Also: `golem stats --context` now says how much of the model's window a request used, and the telemetry allow-list bug that silently drops new fields bit for the **third** time (R8.1, R8.13, now `model`) — caught only by running the real command against real telemetry. First live number: $5.01 over 20 samples, 96% of it cache reads. §106. |
| **P3b — caveman-shrink measured and declined** 🔬 | 2026-07-31 | Golem's existing gate pointed at *their* implementation rather than rebuilding it: `--shrink ext-caveman-shrink` resolves `caveman-shrink` from the **user's own install** (never vendored; an unresolvable package is a hard refusal, because measuring an identity transform would publish a fake 0% under their name). Result: **53 of 1,089 description tokens (4.9%)**, accuracy 88.9% → 91.4% = `NO-MATERIAL-CHANGE` on 27 cases × 3. No accuracy objection to their transform — the prize just does not exist (0.04% of a 139k request, in a prefix billed at 0.1×). Also documents the install/config surface §87 could not find on their README. §107. |
| **R8.S2 — system-prompt slimming DECLINED** 🔬 | 2026-07-31 | Measured, then declined, exactly as scoped. Third capture of the system prompt agrees with the first two: **3,347 tokens = 0.92%** of a 362k request. A 30% trim collects 100 equivalents/request (**0.205%** of input cost, ~$22/month here) while one busted prefix costs **6.7× a whole request — 3,247× the saving**. Declined not on the arithmetic alone but on ownership: `system` is the *client's* operating instructions, nothing in it is provably inert, and rewriting it is a fidelity change. Fourth consecutive input-side idea to die on the same two questions — whose bytes, and how big is the prize. §108. |
| **R8.7 — the local editor, in the one format its harness approved** | 2026-07-31 | Harness first, and it overturned the design: `search-replace` scored **33.3%** semantic (half the replies were not in the format at all) and `udiff` **33.3%** (100% compliance, half the hunks unmatchable — the model paraphrases lines it must copy), against **whole-file 91.7% semantic / 100% apply** (§110). `coder` gains `mode: "edit"` — propose a validated diff by default, write only on `apply: true` — with four validation layers including a new **definition-loss guard** (a rewrite that parses but drops a function is rejected). +313 definition tokens on, **byte-identical to R8.6 off** (`inference.local_editor_enabled`, default false); the fixture-scale saving (~30 output tokens/edit) does not repay that, so the claim is conditional on larger edits and the mode is opt-in. Also proved `--repeats` cannot sharpen a temperature-0 harness. Debrief: `debriefs/2026-07-31-r8.7-local-editor.md`. |
| **R8.9 — change ledger: discard an attempt instead of repairing it** | 2026-07-31 | `golem checkpoint create|list|show|restore|drop|prune` snapshots the worktree into `refs/golem/ledger/<id>` — a `commit-tree` object no branch points at — and can put it back, so a dead end is thrown away rather than unpicked (§93: the wreckage of a repair also sits in context for every later turn). The repo's "commit only when asked" rule forced the design and paid off: **no branch, no commit, no index write** (every staging step uses a throwaway `GIT_INDEX_FILE`), git's default refspecs do not carry `refs/golem/*` so it cannot leave the machine, and a snapshot being an ordinary commit means `git diff refs/golem/ledger/<id>` needs no Golem command. `restore|undo|drop|prune` are **destructive** in ADR-0002's never-auto set (`ask` overrides this repo's `Bash(golem:*)` allow-list); `create` stays ungated on purpose, because gating the snapshot means the model stops taking snapshots. A restore takes its own `pre-restore` checkpoint first and prints the undo. **No MCP tool** — a definition bills every request (§88/§100), so the guidance is a lazy `/golem/checkpoint` skill. The live smoke test caught what 11 green tests missed: in a repo with no `.gitignore` a restore deleted Golem's own `.golem/` state, now excluded by pathspec and regression-tested untracked *and* un-ignored. Restored files carry **git's** line endings, not the disk's. Saving is structural, not measured — named as such. §111. |
| **R8.6 — the LSP bridge, as four modes of one tool** | 2026-07-31 | `diagnostics` / `definition` / `references` / `hover`, answered by a language server the **user** installed and Golem merely spawns (tier-2, Decision 53) — reached through the existing `code` tool's `mode` parameter, never four tools. Measured both ways with `golem bench tools --lsp`: **+333 full-definition tokens when enabled, and zero when off** (the mode enum, position parameters and prose only enter the schema when the bridge is injected; the default census came out **22 tokens cheaper** than before). Four separate tools would each pay the ~250–320-token envelope a definition costs before any content. Lifecycle was the risk, so every wait is bounded — handshake, request, and the graceful `shutdown`/`exit` before the kill — clients are pooled and idle-evicted, and `process.once("exit")` kills children synchronously so none is orphaned. 17 integration tests spawn a real child (`tests/fixtures/fake-lsp-server.mjs`) and prove every degrade path — absent binary, unclaimed extension, handshake timeout, request timeout, mid-session crash with a pool re-spawn, protocol desync, unreadable file, missing position — resolves to `available: false` with a reason rather than throwing. Unmeasured, and named as such: real `typescript-language-server` timings, and whether an agent then *stops* grepping (the same displacement question R8.5 left open). §109. |
| **R8.M1 — three upstream-switch UI defects** | 2026-08-01 | One report (*"the status bar says an upstream we're not using"*), three independent defects, none of them in the routing. (1) `golem proxy status/start/restart` read `proxy.upstream_base_url` — the legacy leaf — and announced `-> https://api.anthropic.com` while the proxy correctly served an active OpenRouter account; the banner now resolves through `resolveUpstreamDisplay`, the **same** resolver the runtime uses, so display and route cannot drift. Correct routing reported wrongly is the worse failure: it reads as a failed `golem account use`. (2) Panel setting toggles were **outvoted, not broken** — the scope `<select>` marked nothing `selected`, so the browser picked `project` while precedence (`default < user < project < local < env`) let a `local` value keep winning and the row snapped back; it now pre-selects the layer that *owns* the value. Any UI over a layered config needs this or every control is a silent no-op for customized users. (3) "Switch upstream…" took ~2.8s to open because `buildModel` expected a bare array where `golem account list --json` emits the `AccountsReport` object, so the cache was always empty and the quick-pick re-ran a CLI call that DPAPI-decrypts every account's credentials. Debrief: `debriefs/2026-08-01-r8.m1-upstream-switch-ui.md`. |
| **R8.28 — mcp/server.ts split into 9 domain modules** | 2026-08-03 | 2,213→130 lines: search/fetch/ingest, wiki_read/wiki_upsert, coder, code+LSP, prompts, devices+snooze, deps, and shared each extracted to their own module. Barrel unchanged, zero behavioral change. 185/186 test files green. |
| **R8.30 — Headroom worker-respawn backoff + stderr tail** | 2026-08-03 | Exponential backoff (1s→2s→4s, capped 30s) on unexpected Headroom worker death, reset on successful start. Startup timeout now surfaces captured stderr so "uv couldn't find Python 3.13" is visible rather than a generic failure message. |
| **R8.S3 — session tree recorder + CLI (spike)** | 2026-08-03 | `SessionTreeRecorder` observes request bodies, builds branch/fork tree from content hashes (no content stored). Persisted to `.golem/state/session-tree.json`. `golem session tree` CLI renders it. 10 unit tests. Relaunch remains with `claude -p --resume` per Decision 37. |
| **R8.31 — `proxy stop` no longer leaves a dead port** | 2026-08-06 | Reported as *"I stopped the proxy from the VS Code UI and it didn't remove the Anthropic URL"* — and the wiring was only half the story: `env` in `.claude/settings.json` is **not** hot-reloaded (only `permissions`/`hooks`/`apiKeyHelper` are), so deleting the key fixes the *next* session and leaves the current one broken. Stopping therefore keeps the port **served** by a redaction-only shim (`stopped` → *pipeline off*), and going direct becomes the explicit `golem proxy unwire`/`wire` pair with the reload surfaced. **The shim pins level 1, not level 0** — the cheap passthrough (`x-golem-bypass`/level 0) forwards *unredacted*, so a Stop button built on it would have made the single sanctioned redaction-off path reachable from a click that says nothing about redaction; a test now pins that, alongside a control proving level 0 really does skip redaction. Ownership guard reused from `init.ts`, never re-derived: a **foreign** `ANTHROPIC_BASE_URL` survives an unwire untouched. Verified live — port still accepting after `stop`, and a request through the shim reached `api.anthropic.com` (401 on a bogus key, i.e. forwarded, not refused). Also fixed a smaller dishonesty the work exposed: the statusline advertised `✂ full` while the shim runs no brevity stage. +20 tests (2261→2281). §112. |
| **R8.32 — the running proxy nothing was pointed at** | 2026-08-06 | The **mirror** of R8.31, found the same day and strictly worse: there, live wiring named a dead port and every request failed loudly; here the port was served and `ANTHROPIC_BASE_URL` was **absent**, so requests **succeeded** while bypassing redaction, compression and telemetry — and all four surfaces reported success. The statusline was the worst: it derived "active" from the pid file alone and painted green `⬢ Golem · Aggressive · ✂ full` over traffic it never touched (now yellow `⬡ Golem · Unwired`, no level, no badge). R8.31's own debrief had *named* this gap — "is the process running?" and "where is the client pointed?" are independent facts — and then fixed only the half that failed loudly. Needed **no new mechanism**: `readWiringState` already returned `golem | foreign | none`, so the work was one shared `wiringGap()` (four hand-written strings would drift, always toward reassurance) called from `proxy status`, `golem status`, the statusline and the VS Code control. `foreign` gets a different message and **no remedy** — the ownership rule forbids touching another gateway's wiring — while `none` names `golem proxy wire`, not the far heavier `golem init` the checkbox used to suggest. Status now *reads* `.claude/settings.json`, so a test pins that it never writes it: repairing on read is exactly the mistake R8.31 avoided. Verified live by unwiring this repo's proxy and reading all four surfaces. **Cause, confirmed after shipping:** the `env` block was deleted **by hand, deliberately**, to go direct to Anthropic — no rogue caller. That *inverts* the finding's weight rather than closing it: going direct is a routine act here (R8.31's debrief recorded the same hand-edit weeks earlier), so running-but-unwired is a state this repo enters **on purpose and often**, and a statusline painting green over it is a recurring lie, not an edge case. Still open, ergonomically: `golem proxy unwire` shipped hours earlier and does exactly this job, yet a hand-edit was preferred. +12 tests (2286→2298). |
| **R8.33 — a tool call could turn redaction off, and only warn afterwards** | 2026-08-09 | The `level` MCP tool accepted `0`, wrote it straight to `sliderStore` **persisting across sessions**, and the only guardrail was a warning in the *response* — after the write. `LEVEL_TABLE[0].redaction === false`, so anything that can put text in the model's context (poisoned tool result, hostile file, crafted page) could persistently disable the project's most important hard rule, and the user's only signal was a line they may never read. **A guardrail that runs after the write is a receipt, not a control.** ADR-0002 had already settled this for the *autonomy* dial (threat item 4: "the level cannot be set from within a tool call — only the explicit `golem autonomy` CLI", and invalid values resolve to the **most restrictive**); the slider did the opposite of both halves. Now 1–3 only, refused at **two** gates — `sliderLevelInput.min(1)` carrying the refusal text, and an independent check before `sliderStore.set()`, because the schema is a description and the write is the boundary. Decision 8 is intact (the model keeps 1–3, so it can still ask for fidelity); Decision 30 is untouched (level 0 is still a real, supported level — `golem slider 0`). Reporting deliberately unchanged: a CLI-set 0 still round-trips through `stats` as `passthrough`, pinned by its own test, and every loud surface stays loud. Half the vulnerability was **prose** — the `slider` and `bypass` prompts and both skills instructed the agent to call the tool with 0; note `.claude/skills/**/SKILL.md` is generated from `src/cli/skills.ts`, so editing the installed copy alone would have been erased by the next `golem init`. +2 tests (2298→2300). Debrief: `debriefs/2026-08-09-r8.33-level-zero-cli-only.md`. |
| **R9.1 — the target registry, and the config that was already one** | 2026-08-09 | `proxy.targets`: **one table for every model Golem can reach**, local or upstream — the pivot being that a local model is just a target whose provider is `ollama`. Splits what `proxy.accounts` fused: an account answers *whose credential*, a target answers *which endpoint + model* and points at an account. **A target holds no secret**, which is why the entire ADR-0003 threat model carries over untouched — the property that kept it an `M`, pinned by a test that a registered target serializes with no key field. The design was decided by reading the config that already existed: this repo's own settings hold **six** accounts, five sharing one OpenRouter credential while naming five different models — the many-targets-one-account case in production, before the registry existed, and the thing a one-target-per-account design would have got wrong. It became the fixture, and it forced `golem target list` to **derive a row from every account**, so an existing config is a usable registry with no edit (an empty table beside six happily-served accounts would have been R8.32's failure mode again). Fail-closed is exact: an unknown id yields no target *and the ids that do exist* — `No substitute was used` — because a registry that quietly swaps a neighbour for a typo'd id sends your **context** somewhere you did not name, which is worse than ADR-0003's mis-billed key. `trust` (`vendor \| local \| lan \| third-party`) is stored and surfaced now, consumed as a redaction floor in R9.3; its default is deliberately pessimistic — loopback Ollama `local`, non-loopback `lan`, `anthropic` `vendor`, **everything else `third-party`** (the most-redacted level), because an omitted field must never buy a target more of your context than it asked for. `credentialEnvForProxy` now resolves **N credentials, not 1**, with no new secret mechanism (`perAccountEnvVar` was already per-account, Decision 47) — and the widened blast radius is recorded at the function that causes it rather than discovered later. Deliberately **inert**: nothing routes on it (R9.2/R9.3 consume it); the only behaviour change is that startup now warns per misconfigured target instead of only the served one. `active_account` → `default_target` shim reads the old key when the new one is unset, so an existing config keeps working untouched. +37 tests (2300→2337). Verified live against the real six-account config. Debrief: `debriefs/2026-08-09-r9.1-target-registry.md`. |
| **R9.2 — the proxy serves many targets, and the one `Pool` that stopped it** | 2026-08-09 | The structural blocker was one constructor line: a single undici `Pool` built from `upstreamBaseUrl`. A lazy `Map<origin, Pool>` (all closed in `close()`, bounded at 32 origins — and reaching the cap dials an unretained pool rather than failing the request) made concurrency across targets fall out for free, since per-origin pooling is what undici is *for*. **Strictly additive**: `resolveRoute` is optional and, absent, the proxy is the code it has always been — the gate was that every existing byte-fidelity and recorded-shape test pass **unmodified**, and 104 → 513 → 2360 tests did, none edited. Three explicit levels, all user-authored — virtual `model: "golem/<id>"` > `x-golem-target` header > `proxy.default_target` — plus a conversation binding that is an optimisation, not a level. `providers/routing.ts` is **pure and total** and deliberately returns an id it cannot validate, because only the caller can fail closed naming the configured targets. The design's key property is what it *cannot* express: no rules engine, no suggestion channel, so no rule can key on a per-turn signal and ping-pong a conversation between targets, busting the cache prefix on every switch. Fail-closed is literal — an unknown virtual id or header target dials **nothing** (tests assert both stub upstreams saw zero requests), because serving the default there sends the caller's context to a model they did not name and Claude Code will not catch it (§114 caveat 4). A virtual id is **never forwarded**: the body's model is replaced with the target's own — the single model rewrite in the proxy, and only ever over a string that was never a real model id. Restated per-route: byte-fidelity is a property of the **provider**, not the proxy. `buildUpstreamTransport` was extracted so the single-upstream and routed paths share one translator construction — drift there does not throw, it mangles a response — and the extraction immediately caught that `createOpenAIToAnthropicSSE` takes one merged argument, not two. Observability, because N targets breaks every "current" surface: per-target `served-model.json` (top-level fields kept = most-recent, so pre-R9.2 surfaces stay correct), per-target rows in `golem status`, and limit predictions stamped with their **source target** plus `unmonitored_targets` — only some providers emit `anthropic-ratelimit-unified-*`, and without naming the rest one target's utilization reads as coverage for all seven while the auto-park is silently blind. 12 integration tests run **two real stub upstreams against one proxy**. +23 tests (2337→2360). **Still open (§114 caveat 5):** the proxy half of the slash question is proven end-to-end; whether *Claude Code* forwards `model: golem/<id>` intact needs a live session, and checking it would spend the user's credential on a third-party upstream — fallback is a flat id, design unaffected. Debrief: `debriefs/2026-08-09-r9.2-multi-target-proxy.md`. |
| **R9.3 — `coder` on any target, and the egress path that had no redaction** | 2026-08-09 | The finding that shaped it: **`src/mcp/` contains no redaction calls at all**, and that was *correct* — while the only reachable model was local, nothing left the machine. The moment `coder` can name a remote target it becomes an egress path bypassing the proxy's redaction stage, carrying exactly the material most worth redacting (KB/wiki grounding; in edit mode, project source). So redaction ships in the same slice as the `target` parameter and is enforced **structurally**: `dispatch()` has one non-local code path and it cannot reach the network without passing through `redactReversibleText`. The frozen `src/interfaces/inference.ts` was **not amended** — its doctrine is that callers never name concrete models, so `TargetDispatcher` sits *above* it and the local path is untouched. Redaction became reversible without becoming weaker: a new entry point beside `redactStandaloneText` using the **same rules and the same per-value placeholder allocation**, so what crosses the wire is byte-identical to before; the restoration map lives in memory for one dispatch and is never serialized, stored or logged. Per-VALUE numbering is what makes restoration total, and placeholders carry their closing bracket so none is a prefix of another. **Trust may raise the floor, never assert its way past physics**: `trust: "local"` is the only value permitting the unredacted path and is honoured ONLY on a genuinely loopback endpoint — a target claiming `trust = "local"` on a cloud URL is redacted like anything else, because trust is user-authored config and a typo must not become an egress. The gate is a recorded proof on **captured outbound bytes**: no key and no address in the payload, and the echoed redacted prompt restores to the original byte for byte. Selection is bounded by the schema itself — a zod enum over targets config declares and marks `agent_selectable` (default true), so an ad-hoc URL is not expressible; the parameter only appears when there is more than one target, since `coder`'s definition bills every request (§110). Refinement stays local: sending the draft out twice doubles egress for no benefit. The tests caught a real bug — `upstreamChatCompletionsPath` returns a *path* (built for undici's origin+path), and `fetch` needs an absolute URL, so every remote dispatch would have failed against a real endpoint. +14 tests (2360→2374). **Not verified live:** the "completes a draft on a remote target" half would spend the user's credential on a third-party provider, an outward act; the local-coder setting was left disabled as found. Debrief: `debriefs/2026-08-09-r9.3-coder-any-target.md`. |
| **R9.4 — the status line was describing a constraint that no longer existed** | 2026-08-09 | Reported the moment R9.3 landed: *"the status line and status bar makes less sense."* Both surfaces rendered `local (qwen2.5-coder:7b) + anthropic (claude-opus-5[1m])` — a shape whose **grammar** encodes the assumption R9.1–R9.3 removed, that drafting is local and only the upstream is a real choice. The R8.32 family one step milder: not a surface asserting something false, but one whose vocabulary could no longer express the truth. Now both name the **chat** model plus **only the workers whose model differs from it**, flattening to one segment when they agree — printing a worker that agrees costs width and says nothing, and "show what diverges" is what keeps the line **bounded** as workers accumulate. Naming: **"chat", not "thinker"/"reasoner"** — both collide with this repo's narrow existing use of reasoning/thinking (`upstream_reasoning_effort`, `map_reasoning_to_thinking`, `reasoning_content` → `thinking` blocks), so either would read as a claim about reasoning traces; "chat" names the job like the existing roles do. The config shape changed **twice**, and the second time mattered: a scalar `inference.coder_target` shipped first, then the user mentioned future workers ("a writer for document writing, with its own default target") — which invalidated it, since each worker would grow a schema leaf, a UI-model entry, a status field and two status-surface branches. Reshaped to `inference.worker_targets`, a map keyed by worker, **before release** — adding a worker is now one line of config plus a glyph. The map's one weakness (a typo'd key silently ignored) is bought back by `KNOWN_WORKERS` + `unknownWorkerWarnings`, deliberately a warning not a failure — a config typo must not stop the worker that IS configured correctly, while an unknown *target* stays a hard dispatch failure. Glyphs are placeholders in one `ROLE_MARKS` per surface; two copies are unavoidable (the extension shares no module with the CLI) and drift invisibly, so a test pins that they agree, are single code points, and are distinct. **Running it caught what reading it did not:** an unresolvable target rendered `coder qwen2.5-coder:7b (target ghost)` — a model that can never run, via a target that does not exist, contradicting the warning printed underneath; both surfaces now omit the worker and say FAILS CLOSED. +7 tests (2387→2394) plus 50 extension tests. Deployed locally — the VS Code extension had been stuck at a registered **0.5.0** while the repo was 0.7.0, so `deploy:local` had been a no-op against a path that did not exist. Debrief: `debriefs/2026-08-09-r9.4-role-named-status.md`. |
| **P3a — the CLAUDE.md compaction actuator, and the honest number it produced** | 2026-08-09 | R6.4 shipped a leanness *check*; this is the write half. `golem prompt compact [file]` rewrites an instruction file with the **local** model and reports the saving beside its cost. **Route: tier 3b (Golem's own rewrite), not tier 2 (install Caveman's `/caveman-compress`)** — decided on a collision the memo had not spotted: Caveman's installer drops its *speech skill* plus a session-flag hook into the agent's skill dir (§87), and `hasExistingBrevityDirective` stands down on any `/caveman/i`, so installing theirs to compact one file would **silently switch Golem's own brevity stage off**. Byte-preservation is a property of the code, not the prompt: fenced code, headings and frontmatter never reach the model at all, inline code/paths/URLs/wikilinks/`SCREAMING_SNAKE`/`$ENV` are masked with opaque sentinels, and a segment whose sentinels do not come back intact is **discarded in favour of the original** — a failed rewrite costs compaction, never fidelity. Nothing is ever written over the target: the proposal lands in `.golem/compact/`, and `--apply` is a second command after a human reads the diff. The cost side is computed **without** the model (stemmed content-word survival per directive), so it cannot vanish when Ollama is down. **The measured number is the point**: on this repo's already-lean 58-line CLAUDE.md the actuator buys **~1–3%**, not the ~46% Caveman claims — and reading the first real diff showed the percentage was hiding real losses ("check the docs **before implementing**" → "before implementing" dropped, while the rule still scored ≥60%), which added a `partial` line to the report. On dense prose the 7b drafter broke the placeholder rule on **3 of 5** segments; the verifier caught every one. +17 tests (2394→2411). Debrief: `debriefs/2026-08-09-p3a-claude-md-compaction.md`. |
| **R9.17 — the coder-first gate stopped starving concurrent sessions** | 2026-08-10 | Reported from R9.12's own session: the `local-coder` gate denied **every** non-trivial code write instead of one, leaving agents unable to write code at all once `coder` was also 401-ing. The first diagnosis, written into the task doc, was that Claude Code rotates `session_id` per tool call — **inferred from two denials writing two different ids, and wrong.** A payload probe settled it: across Bash and Write calls there is exactly **one** `session_id`, matching the transcript filename. The real defect was that the one-shot state was a single global slot for the whole project, and a project routinely holds more than one session — a second window, a parallel agent, a headless `claude -p`. Each new session overwrote the slot, so the previous session's one-shot was forgotten and it was nudged again at its next code write, for ever; the two ids that looked like rotation were two sessions taking turns, and the foreign id was still sitting in the file long after its session had gone. The state now remembers a **set** (`nudgedSessionIds`, capped at 50, newest kept), still reads the old single-slot shape so upgrading does not re-nudge, and the tests now exercise the case that actually fails — two sessions interleaving, nudged exactly once each. Guidance re-enabled and proven live: the first non-trivial write nudged, the next two went straight through. +9 tests. |
| **R9.18 — `golem proxy restart` stopped lying about a proxy that was starting fine** | 2026-08-10 | `proxy restart` reported `did not come up on port 4930` roughly one run in three while the proxy was in fact running. Two hypotheses were written down and both died on measurement: `ProxyServer.close()` already force-closes keep-alive sockets, and the port is not slow to release (**process dead after 4ms, port free after 4ms**). The cause was on the start side. Timing every pre-`listen()` step showed **`credentialEnvForProxy` at 6668ms of a ~6.9s total** — against a `waitForPort` budget of **8000ms**, paid twice per restart (parent builds the child's env, child resolves again), so ordinary jitter crossed the line. Worse, on runs that appeared to succeed `waitForPort` was sometimes seeing the OLD listener: a false success. Fixed by raising the budget to 30s (slow to declare failure costs seconds; quick to declare it sends people debugging a healthy daemon), by making `restartProxyDetached` **honour** the `waitForPortFree` result it had been discarding rather than spawning a daemon guaranteed to exit with "already running", and by leaving credential resolution sequential — making it concurrent was tried and reverted, because it bought nothing on Windows (concurrent PowerShell startups contend rather than overlap) and the process burst destabilised the test suite. Verified with **10 consecutive restarts, 10 successes**, each a distinct new pid. The underlying latency — one PowerShell/DPAPI spawn per *stored* account (~2.6s one-time self-test, then ~0.94s each, while unstored accounts cost ~1ms) — is filed as R9.20 rather than papered over. |
| **R9.12 — the served-WebFetch red dot goes green, behind a CA that cannot lie** | 2026-08-10 | R9.7 accepted the red dot and §120 declined the loopback escape; the user asked whether the opt-in could be avoided, and answering it honestly overturned three prior findings in a row. **§122** exercised the one field §115 had listed but never tested: `additionalContext` on an `allow` **reaches the model**, and — measured with a rewrite to a dead port — still reaches it when the rewritten call **fails**. That decoupled content delivery from tool success and killed §121's blocker B (*"a failed rewrite is worse than a red dot"*), while reshaping the design: the endpoint serves a few hundred bytes of **stub** and the raw cached page rides in on `additionalContext`, so WebFetch's summarizer never touches the content and Decision 42 is preserved rather than inverted. **§123** then nearly killed the whole thing: `claude` is a `PE32+` **Bun** binary whose TLS errors read `OPENSSL_internal` — **BoringSSL** — and it refuses a `CA:FALSE` leaf as a trust anchor, with an `openssl`-generated leaf as a control to rule out Golem's hand-rolled DER. Green appeared to need exactly the unconstrained CA §120 objected to, and the task was written up as REGRESSED. **§124** found the way through, prompted by *"is there a way to use WebFetch without https?"* — there is not (a plain-HTTP loopback server logged `HPE_INVALID_METHOD` and never saw a request; `https://localhost:<port>` is `Invalid URL`, so the IP literal is mandatory) — but the question reopened the trust problem: X.509 **`nameConstraints`**. BoringSSL cannot parse an `iPAddress` constraint (`unsupported name constraint type`, failing closed) yet **does enforce `dNSName`**, so the shipped anchor is `CA:TRUE, pathlen:0` permitting only `DNS:golem.invalid`, signing a leaf whose SAN is `IP:127.0.0.1` alone. The security claim is not argued but measured: a leaf from that CA additionally bearing `DNS:api.anthropic.com` was refused by the client with **`permitted subtree violation`**. §120's objection is closed structurally, not mitigated. Built with **zero new dependencies** — a hand-rolled ASN.1 DER encoder over `node:crypto` (`verify-deps: 5 runtime … all pinned exactly`), every test parsing the result back with `X509Certificate` so a bad encoding fails loudly. Default-on: `golem init` generates the pair, sets `NODE_EXTRA_CA_CERTS` **only when unset** (§121-C — a corporate TLS-inspection bundle is never clobbered, never concatenated), prints what it trusted and what the CA cannot do, and `--no-loopback-cert` declines; `golem uninit` removes it on the same ownership rule; `golem status` reports the three states separately because a correct settings file plus no restart still means red (§112). Both gate halves proven by live `claude -p` runs: **green** `is_error=false` with the heading answered from context and no network fetch, and — same build, no env — **`is_error=true, NOT AN ERROR — Golem served this URL…`**, R9.7's text verbatim. Two defects found in passing and filed rather than folded in: `golem uninit` had begun leaving the CA behind (fixed here, with a test), and the wording of the stub had to be measured too — an earlier draft made the model report a **succeeded** fetch as errored. Follow-up in the same batch, from the user asking to tell a cached fetch from a live one: served fetches now carry a deterministic provenance receipt (`**Golem** Served from cache (stored 14h ago) — <url>` / `Fetched live … — now cached`), carried on the stub URL as `s=hit|miss` and substituted via `updatedToolOutput`. Building it exposed a silent defect: **the CCR oversized-output swap had never once fired for WebFetch**, because WebFetch answers with `{bytes, code, codeText, result, durationMs, url}` and `findTextSlot`'s key list lacked `result` — while `web-fetch.ts`'s own `textOf()` had listed it all along, two lists of the same thing drifting apart where nothing checked. Also learned by measurement, after two wrong guesses: the label must be emitted from the CCR hook, not the WebFetch post hook (that one is registered `async`, so its stdout is discarded); and in the VS Code extension a collapsed WebFetch row renders `Fetched from {url}` built from the ORIGINAL input URL, so neither the row nor `systemMessage` (absent from the event stream entirely) can carry a label — the receipt is reachable via Focus view (`Ctrl+Alt+F`) and always present in the transcript. +30 tests (2510→2540). §122/§123/§124/§125. Debrief: `debriefs/2026-08-10-R9.12-webfetch-green.md`. |
| **R9.9 — `golem config set` can write an object-valued setting** | 2026-08-09 | `config set` parsed booleans, numbers and arrays but not **objects**, so every `z.record`/`z.object` leaf reached zod as a raw string and failed with `Expected object, received string` — `compression.headroom_config` was settable only by hand-editing `settings.local.json`, which is exactly what `golem config` exists to make unnecessary. Object leaves now parse as JSON through the same seam the array path uses, and errors name the **cause** (`invalid JSON for "<key>": …`, or `expects a JSON object, got array`) rather than the downstream symptom. Because JSON on a command line is a shell fight that PowerShell, cmd and POSIX each lose differently, `--value-file <path>` and `--value-file -` (stdin) are the quoting-proof escape hatch, BOM-tolerant, and passing both a positional value and `--value-file` is refused rather than silently preferred. Fixed a latent bug the work exposed: `setConfig` compared the written value to the effective one by **reference**, so every object write reported itself overridden by a layer that had overridden nothing. The gate is a test that enumerates every object/record leaf in the schema and asserts each is settable — not special-cased to the one that motivated it. +14 tests. |
| **R9.8 — marker-free tool output is now the default, and the daemon stopped eating its own warnings** | 2026-08-09 | Filed after an editing-heavy session at slider 3 looped: tool output came back carrying `<<ccr:…>>` refs and crushed tables, so an exact-match `Edit` built from what the model *saw* failed against the file's real bytes. The task assumed the fix was reaching *nested* Headroom config. Measured against the pin, that assumption was wrong in a more interesting way: `HeadroomConfig.smart_crusher.lossless_only` is settable and **does nothing**, because Headroom's default pipeline builds `ContentRouter()` with no config at all — a "correct" nested key would have reported success and changed nothing, the very failure Decision 53(h) exists to prevent, one level down. The reach point is the **transform instance**, so the worker swaps only the ContentRouter inside a real default pipeline (an upstream release that adds a transform keeps it) and restores it on every request that did not ask for one, since the singleton is process-global. Marker-free is now Golem's **default** wherever the semantic stage runs: measured 15,740 tokens saved vs 19,181, i.e. **82% of the saving for zero markers** — the right price when the flagship client is a coding agent doing exact-match edits. Overridable with `{"lossless_only": false}`. Two of the task's other premises were measured and dropped: Read was never the exposure (Headroom already excludes Read/Glob/Grep/Write/Edit and Golem forwards the `tool_use` blocks that make the name resolvable — Bash/WebFetch/MCP were), and level-3's `compress_user_messages: true` is **inert** on the shipped install (byte-identical output both ways; the prose stage needs `[ml]`/torch we omit), so it was documented rather than flipped on a theory. The reporting half of the gate was failing outright: `startDetached` spawned with `stdio: "ignore"`, so this repo's own silently-dropped `headroom_config.plugins` key had a warning that reached nobody — the daemon now appends to `.golem/proxy.log` and `golem status` names the path. Decision 57. +19 tests (2415→2434). §116. |
| **R9.6 — a renamed setting no longer stops working in silence** | 2026-08-09 | Golem had no migration machinery: a settings file naming a retired key loaded with **exit 0**, and the setting silently stopped taking effect while every surface reported success — the failure class this project keeps rediscovering (R8.32's running-but-unwired proxy, R9.4's status line naming a model that could never run). There is now one declarative table (`src/config/migrations.ts`) consulted by the loader: the old key keeps working, is reported exactly once naming the file and the key to edit, and provenance reports the key the **file** contains rather than the one the value landed on — otherwise the user is told to edit a line they do not have. `golem config set` and `writeSetting` both resolve retired names, so no write path can put one back. The guard that makes it real is `assertLeafRename`: a migration whose `from` is still a live leaf is a fiction (both names stay writable and the loader never consults the table), so the table's own test refuses it — you cannot register a rename without retiring the leaf. `proxy.active_account` → `proxy.default_target` moved onto the mechanism and R9.1's bespoke `?? settings.active_account` fallback is gone. That retirement exposed a split nobody had noticed: R9.1 unified *routing* on `default_target` while the *account* layer still read `active_account`, and collapsing them revealed the unified selector may legitimately name a **target** that is not an account — which would have warned on every proxy start. The account layer now stands aside for a known target id, with fail-closed intact (a selector in neither registry still warns, and the proxy checks both registries independently). Config warnings also print at proxy startup now, which is only useful because R9.8 stopped the daemon discarding its own stderr. +11 tests (2434→2445). §117. |
| **R9.5 — managed files stop being clobbered *and* stop being frozen** | 2026-08-09 | Golem wrote two kinds of file into a user's project and treated them inversely wrong: a re-init **overwrote** a hand-edited `.claude/skills/golem/<cmd>/SKILL.md` (silent data loss) and **never refreshed** `.claude/rules/golem-<name>.md` (every guidance improvement invisible to anyone who had already run `golem init`). They read as opposite bugs and were the same one — both surfaces asked *does this differ from what Golem ships?*, which cannot separate "Golem's text moved on" from "the user edited it", so each picked an interpretation and was wrong half the time. Recording the hash of what Golem last wrote adds the missing question: a managed file is **current**, **absent**, **stale** (matches what Golem wrote → refresh loses nothing) or **owned** (does not → the user's; reported and kept). No record means owned *deliberately* — a pre-R9.5 project's drifted files are kept with a note, because refreshing when Golem cannot prove it wrote the content is the original data-loss bug in a new mechanism; a corrupt record degrades the same conservative way. The guidance sentinel keeps its real job: `golem guidance disable` still survives a re-init ("disabled — not re-seeded"), which has its own test, because a refresh that resurrected a disabled rule would be worse than the bug being fixed — it overrides an explicit choice rather than an implicit one. `conflict` is a new `ActionKind` rather than a `modify` variant, because "refreshing stale text" and "replacing your edit" are rendered generically in several places and must not collapse; the detail string carries the way forward. Record lives in `.golem/state/managed-files.json`, gitignored, removed by `uninit`. Verified live: edit a skill, re-run `init`, edit survives. +17 tests (2445→2462). §118. |
| **R9.10 — the coder stopped being "local"** | 2026-08-09 | R9.3/R9.4 made `coder` dispatch to any declared target; the naming never followed, and pointing it at a `claude-sonnet-5` target made the contradiction visible inside one object — `local_model.workers[0].model = "claude-sonnet-5"` beside `local_model.coder_model = "qwen2.5-coder:7b"`. `inference.local_coder_enabled` → **`inference.coder_enabled`** (named for the tool, not for where the model runs), carried by R9.6's migration table so no existing settings file breaks; `workers` promoted **out of** `local_model` to top level with a `local` flag per row, because the containment was the falsehood even though every field in it was true; `golem local status` now names any worker that does **not** run on that backend, since "Local model: ACTIVE" above a worker routed to api.anthropic.com is a true sentence arranged to mislead; and the `local-coder` guidance rule's *argument* was rewritten — "drafting locally leaves the paid model's tokens for judgment" inverts the moment `coder` points at a vendor model — which reaches existing projects only because R9.5 shipped first. Dogfooding the rename on this repo's own `.golem/settings.json` found a bug no unit test had: `config set` on a retired key wrote the live one but `writeSetting` only ever *adds*, so following the deprecation notice left the file holding **both** names and warning forever; `setConfig` now deletes the retired key from the same file via a helper that deliberately bypasses migration resolution (using `writeSetting` would have deleted the replacement). Optional fields throughout, and the VS Code renderer falls back to the old key, so a stale extension degrades rather than breaks. +10 tests (2462→2472). §119. |
| **R9.7 — the served-WebFetch red dot, measured and accepted** | 2026-08-09 | Reported as *"a webfetch which is serviced by golem works, but shows a red dot in claude code"* — Golem's **best** path (cache hit, no network fetch, no summarizer LLM call) is the one that looks broken, because serving content without running the tool has exactly one expression at PreToolUse: `deny` + content in `permissionDecisionReason`, and Claude Code draws a denied call as an error. §115 left one untested escape — `updatedInput` + `allow` pointing at a loopback endpoint — and it was **tested rather than assumed**, which split the premise into two claims wearing one coat. The rewrite half **works**: a probe rewrote `example.com/?r97-probe` → RFC 2606 and the call rendered **green** (note `updatedInput` replaces the *entire* input object, so `prompt` must be carried). The serve half is closed: `http://127.0.0.1` returns **`WRONG_VERSION_NUMBER`** because WebFetch upgrades http→https and speaks TLS at a plain-HTTP server; a self-signed cert returns **`self signed certificate`** (it *connects* — there is no private-IP blocklist, the cert is the only objection); `file://` and `data:` are **`Invalid URL`**. So a loopback endpoint costs a Golem-generated CA in `NODE_EXTRA_CA_CERTS` — process-wide trust covering `api.anthropic.com`, spent on a UI colour by the one component whose job is to be trustworthy. **Declined on the merits, not blocked on the cert:** part (a) alone re-bills WebFetch's summarizer per fetch, and that call was measured transiting the proxy single-turn and **uncached** (`example.org`, ~1.2 KB → **551 input / 9 output**, zero cache read or creation), which scales to **~62k input tokens per fetch at 0% hit** for the 248,890-char hooks doc that started this, against a session whose real rate is 98.4% (§93); and part (b), the proxy short-circuit that would erase those tokens, does it by putting the **raw page in context whole** — the exact blow-up `MAX_SERVED_CHARS` (8,000) exists to prevent. Shipped instead is the half-measure the brief allowed: the intro was `✓ Golem served this URL…`, **a tick inside an `<error>` box**, which is why this was reported as broken rather than as ugly — both serve paths now open `NOT AN ERROR —` and state that Claude Code renders hook-served content as a *denied* call, pinned by a test and recorded in the module docstring so the next reader does not re-derive §115. Revisit if Claude Code ever gains a PreToolUse output-substitution field; until then the red dot is the documented price of skipping both a network fetch and an LLM call. §120. Debrief: `debriefs/2026-08-09-R9.7-webfetch-red-dot.md`. |
| **R9.13 — a project that upgrades Golem fixes its own settings files** | 2026-08-09 | Asked directly: *"how does a project using Golem know its config needs migrating, and how does it happen automatically?"* The honest answer was that it half did — R9.6's table makes a retired key keep **working** at read time, but nothing ever fixed the file, so adoption was manual and the warning was forever. A project now stamps the version it last ran (`.golem/state/version.json`, beside R9.5's `managed-files.json`) and, on the first run under a new one, rewrites retired names to live ones across **all three scopes**. The stamp is deliberately **not load-bearing** and the module says so: the trigger is the *presence of a retired key*, so a missing or wrong stamp degrades to "scanned more or less often than needed", never to a bad rewrite. Two details carry the safety of an unasked rewrite of a **committed** file: the key is renamed **in its original position** (JSON key order is insertion order, so the obvious `delete` + assign moves it to the end and turns a one-line diff into a whole-section one), and the original is copied to `.golem/state/config-backups/<scope>-<version>.json` **before** the write — under `state/`, already gitignored, rather than a `settings.json.bak` that would surface as an untracked file in the user's repo. A file with both the retired and live key drops the retired one, matching what the loader already did; a file Golem cannot parse is **reported and left untouched**, never clobbered. Runs from `golem proxy` (before `loadConfig`, so the R9.6 warnings that follow describe what is still wrong rather than what was just fixed) and `golem init`; **not** from `golem status`, since a read-only diagnostic that silently rewrites files is its own surprise. `golem config migrate` is the manual door, check-mode by default. +15 tests (2474→2489). Debrief: `debriefs/2026-08-09-r9.13-config-file-migration.md`. |
| **R9.14 — the MCP server could not authenticate to anything** | 2026-08-09 | Found while pointing `coder` at Anthropic Sonnet 5: it already WAS pointed there, and returned **401**. The 401 was not about Anthropic — `openrouter-qwen3`, which `golem target list` correctly reported as `key set — DPAPI-encrypted`, returned 401 too. Decision 47 hands a credential to a process the CLI **spawns**, which is why `credentialEnvForProxy` is called from the proxy start paths, `golem account` and `golem init`; Claude Code spawns the MCP server itself from `.mcp.json`, which carries no `env` block, so the MCP process inherited no `GOLEM_UPSTREAM_API_KEY__*`, `makeAuthMapper` returned `undefined`, and every remote dispatch went out with **no auth header at all**. `coder --target <anything credentialed>` had never worked. The checking path and the using path read the credential from different places, which is why `golem target list` and `golem target test` were both truthful and both irrelevant — a status surface can only vouch for what it itself does. `createTargetDispatcher` now takes an optional `resolveKey` and `golem mcp serve` installs one; **a resolver rather than more env**, because the obvious five-line mirror of the proxy would put N secrets in the environment of a process that spawns capability probes and Ollama calls, all of which inherit it — the widened blast radius `credentialEnvForProxy` accepts for the daemon, taken on again where it was not needed. Resolution logic stays `credentialEnvForProxy`'s so the store-id mapping has one implementation. A target whose scheme needs a credential and whose credential does not resolve now **refuses before the network, naming the target and the account**, instead of earning a 401 that names nothing (`auth_scheme: inherit` still dispatches headerless — a configured choice, not a missing secret). Five existing tests had encoded the unauthenticated dispatch as acceptable by passing `env: {}`; making the dispatcher refuse turned them red. +4 tests (2489→2491). Live-verified: `openrouter-qwen3` replies where it 401'd, no `GOLEM_UPSTREAM_API_KEY*` in the process env. Debrief: `debriefs/2026-08-09-r9.14-mcp-credential-resolver.md`. |
| **R9.15 — spending the subscription without borrowing its token** | 2026-08-09 | Asked the follow-up R9.14 invites: *what if I have no pay-as-you-go credit — can't we use the quota I already have?* Two routes existed and only one was honest. The cheap one was nearly free to build: Golem's proxy already sees Claude Code's `authorization` header on every forwarded request, and the OAuth credential sits in Claude Code's own store — either could have been replayed against `api.anthropic.com`. **Declined.** Forwarding the user's own requests is Golem's whole job; minting requests Claude Code never made, on the credential issued to it, is a different act, and the spec had already written the rule (§267: *account switching is user-initiated and transparent, not covert evasion of limits*). The distinction that matters is not whose quota but **who originated the request**. Shipped instead is a `claude-cli` target provider that **spawns the user's own Claude Code CLI**, which authenticates as itself — Decision 53's ladder applied to a credential rather than a binary: external tools are spawned or detected, never shipped, and now never impersonated. The user described the feature as firing *only when the coder model differs from the current model and the upstream isn't third-party*; that was not how dispatch worked (routing was purely `worker_targets.coder` naming a target, with no comparison of any kind), so **both halves became real guards** — a mistaken description of current behaviour is often an accurate description of desired behaviour. Both **refuse rather than reroute**, since falling back to local would be the silent-substitution class this dispatcher already rejects for unknown ids; the one documented exception is an unknown session model, which allows the dispatch because refusing on no evidence costs more than the waste. Three mechanics carry the safety: the **prompt travels on stdin, never argv** (so a drafting prompt full of source and model output has no command line to escape from, which is also what makes the Windows `cmd.exe /c` shim path free), the **environment is scrubbed** — stripping `ANTHROPIC_*` is routing, not hygiene, since an inherited `ANTHROPIC_BASE_URL` would loop the spawned client back into Golem and on to whatever upstream is configured, the exact case guard 2 exists to prevent — and **a spawn is an egress**, so redaction runs before it like any HTTP target. Adding the provider member was caught immediately by `defaultAuthScheme`'s exhaustive switch; the place the compiler could NOT see was `proxy.upstream_provider`, which would have accepted `claude-cli` and produced a setting that parses and cannot work, so `PROXY_PROVIDERS` now excludes it at config-load time. Costs stated rather than implied: **~6.5s per draft** cold against a local model's sub-second, and it spends the **same pool** as the interactive session, competing for the rate limit `snooze` parks on — it does not conjure quota. Opt-in. +10 tests (2500→2510). Live-verified through the adapter against the real binary. Debrief: `debriefs/2026-08-09-r9.15-claude-cli-target.md`. |
| **R9.16 — the status bar was right about a model nobody was using** | 2026-08-09 | Reported in one line: *'ive restarted but the vs code status line shows qwen2.5'*. The config was right and the CLI was right (`✎ claude-sonnet-5 · ◆ claude-opus-5`); the **deployed** extension was three releases behind. Its `render.js` read the worker list from `st.local_model.workers` while the CLI had moved `workers` to the top level of `status --json`, so it found none, fell back to the implicit coder row and filled it with `localCoderModel` — a surface stating confidently a model that was not in use, the R8.32/R9.4 class arriving through a door nobody had checked. Root cause: `installVscodeExtension` returned `skip: already installed` on **the directory existing**, and that directory is named for the extension *version* — ship a renderer fix without bumping the version and every later `golem init` is a silent no-op for as long as the version holds still. This is R9.5's seed-once bug (*an improved rule never reaches an existing project*) one level up, in the artifact R9.5 did not think of as a managed file, surviving it by living in a different directory. Identity is now **content, not name**: per-file sha256 (directories digested from sorted relative paths plus each file's hash, so an added, removed or edited asset all register), because a version string is a promise about content and promises drift. `init` refreshes a stale deployment and reports `modify` naming the files it replaced; an identical copy is still a skip, so the common case stays quiet. Deliberately **no R9.5 ownership check** — that care would be theatre for a build artifact in VS Code's own directory that nobody hand-edits, so *differs from what we ship* means stale, full stop; writing down why the two managed-file mechanisms disagree matters more than the code. `golem status` names a stale deployment (files that differ + the command that repairs them) but does not fix it, holding R9.13's line that a read-only diagnostic must not rewrite an install. **The suite was green throughout and could not have been otherwise**: `vscode-extension/render.test.js` tests the *repo* copy — the one that was correct — and no coverage of a source file can detect that a different copy is running; the gap was untested *artifact*, not untested behaviour, which is a category no coverage report shows. +10 tests (2510→2520). Verified live end to end on the reporting machine: staled a file → `stale [render.js]` → status warned → `golem init` reported `modify … refreshed render.js` → `current`, warning gone. Debrief: `debriefs/2026-08-09-r9.16-stale-vscode-extension.md`. |
| **R9.22 — `golem init` stops committing a path that only exists on one machine** | 2026-08-10 | R9.12's CA trust worked, and shipped a diff nobody could commit: `NODE_EXTRA_CA_CERTS=D:\…\.golem\loopback\ca.pem` written into **`.claude/settings.json`, which is checked in**. On any other clone the path resolves to nothing and Claude Code warns twice at every start — noise inflicted on someone who never opted in. Moved to **`.claude/settings.local.json`**: gitignored, and *higher* precedence than the committed file (notes §13), so nothing about how it is read changes — only who receives it. `ANTHROPIC_BASE_URL` deliberately stays committed; `http://localhost:<port>` is portable and its presence is what tells a teammate the project is wired. Two halves make it safe: `isStaleGolemCaPath` recognises a Golem CA from a *different* checkout root by its `.golem/loopback/ca.pem` tail, so a fresh clone **self-heals** the inherited path on first init instead of just shadowing it; and the §121-C ownership rule now spans both files, so a corporate TLS-inspection bundle in either scope stops the write entirely. The trap was in `init.ts`, not the design: `settings` is a live in-memory object rewritten by the permissions step, so healing on disk alone puts the path straight back. `status` follows the same precedence (local first) or it would report `wired: false` on a correctly wired project. Gate proven live, not just in tests — `golem init` on this repo now produces **no diff at all** to the committed file. +10 tests. Debrief: `docs/wiki/debriefs/2026-08-10-R9.22-ca-path-local-scope.md`. |
| **R10.11 — a test suite nothing ran, and the drifts it was hiding** | 2026-08-14 | `vscode-extension/render.test.js` was executed by NOTHING: `vitest.config.ts` includes only `tests/**/*.test.ts`, so `npm test` never saw it (`.js`, outside `tests/`) and CI runs the same command. It had been **4 tests red on `main` for four releases** while the extension drifted behind the CLI three separate times, every symptom user-visible. The 4 failures were a *third* drift the earlier fixes had not reached: the tests asserted `m.localCoderEnabled`/`localCoderModel` while `buildModel` returns `coderEnabled`/`coderModel`, so they would have failed against a correct implementation — which is how you can tell nobody had run them. Fixed against the shapes `status --json` actually emits (`local_model.model`, no `coder_enabled`), and **made unmissable**: a contract test spawns `node --test` over every `*.test.js` beside the extension, so `npm test` and CI both cover it and a new suite is picked up the day it lands. Discovery is a `readdir`, not `node --test vscode-extension/` — handed a directory Node resolves it through that directory's `package.json` `main` and tries to run `extension.js`, which fails on `require("vscode")`. Then the fix for the class: `render.js` DECLARES every `status --json` path it reads (`STATUS_FIELDS_READ`, classified required/stateful/legacy/unemitted) and a contract test resolves each against a REAL `collectStatus` report, so a CLI rename fails in the CLI's own suite instead of in a user's status bar — verified by mutation. That contract immediately found **two more drifts nobody had reported**: `extension.js`'s `pickAccount` cold path still read the removed `.accounts` key (so the FIRST open of the upstream picker on a fresh window said "no upstream accounts configured" on a project with several — now one exported `gatewayRows` helper knows the shape and both call sites use it), and `proxy.bypass` is read by the panel but emitted by nothing — which turned out to be a **regression, not a gap**: `stopProxy` just kills the pid, and Decision 56's redaction-only bypass shim, its three-state `ProxyDesired`, and `src/cli/proxy-state.ts` are all absent from the tree, so `golem proxy stop` again leaves `ANTHROPIC_BASE_URL` on a dead port. Filed as **R10.12** rather than fixed here, and the contract asserts the gap stays open so it trips the day it closes. +12 tests. Debrief: `docs/wiki/debriefs/2026-08-14-R9-R10-batch-close.md`. |
| **R9.23 — the usage-limit park could not be reached** | 2026-08-14 | Hit live twice (2026-08-10, 2026-08-13). Under snooze enforcement (Decision 45) the only permitted call is `mcp__golem__snooze` — a DEFERRED tool, so calling it first needs `ToolSearch` to load its schema, and every such response came back elided by the lossless dedup. `expand`, the way back from a CCR reference, is *also* deferred and *also* denied. A deadlock: the one tool the agent may call cannot be called, so the note meant to survive the session is never written. The second reproduction settled the brief's open question — the hash is identical across four query spellings within a session and differs between sessions, so the dedup keys on the **response body**, which is why no rephrasing escapes it. Two independent fixes, because either alone leaves the class open. `DEDUP_EXEMPT_TOOLS` exempts `ToolSearch` results **unconditionally**: a schema is not prose but the executable contract for a call about to be made, and unlike a file excerpt it cannot be re-derived, narrowed or grepped for — CCR's bargain assumes expansion is always available, and here it is not. Matching is by `tool_use_id` → tool name collected from the message PREFIX, not by sniffing the body, so the stage's byte-stability obligation is untouched (a `tool_result` always follows its `tool_use`); exempt results are deliberately not seeded into the seen-set either, or a schema could become the first occurrence that elides some later non-exempt duplicate. And `PARK_EXEMPT_TOOLS` lets `ToolSearch`/`expand` through the enforcement gate — the general shape, since any deferred tool needed for parking has it — while a neighbouring MCP tool stays denied. The deny reason now names `until`/`note` AND the escape hatch: the 2026-08-13 session only escaped because it happened to know the parameters from a project rule file, a workaround that works solely for projects shipping that rule. Note for cache watchers: a conversation with repeated identical `ToolSearch` results now emits different bytes than before. +9 tests. |
| **R9.11 — skills orchestrate, tools execute — recorded, checkable, and the instrument fixed** | 2026-08-14 | The user asked whether Golem's MCP tools would be better served as skills with packaged scripts. Read from the code the answer is **no** and the layering was already right; what was missing is that the rule was nowhere written and nothing enforced it, so the next contributor had no way to know which surface a new capability belongs on. Recorded in spec §2.1 beside the existing proxy/MCP split (the same argument one level down) with the four properties a skill-plus-script cannot reproduce: portability past Claude Code (§R6.1), hook enforcement keying on tool NAMES (a Bash-invoked script is a `Bash` call, so snooze/coder-first/CCR would all have to parse command lines), round-trip plus typing plus per-tool permissioning, and the now-dead context-cost argument (Claude Code defers schemas, §102). Made checkable: a contract test flags a `golem <verb>` invocation in any installed `SKILL.md` whose verb names a tool capability, resolving tool names from the LIVE registry rather than a hand-kept list, with a CLI-verb→tool alias map so the case that most needs to fire (`golem slider` ↔ the `level` tool) actually does. A deliberate exception is declared in the skill as `<!-- golem:layering-exception <verb> — <reason> -->` and must carry a reason; a stale one that no longer matches an invocation also fails. The audit found two real leaks and fixed them (`/golem/expand` suggested `golem stats` where it can call the `stats` tool; the coder-first footgun said `golem devices` where the `devices` tool answers), and the `slider`/`bypass` exceptions are marked — level 0 turns redaction off, so by design no tool call may reach it. **On the demotion question the honest answer was "cut nothing", and the reason is the deliverable**: live telemetry gave `coder` 45, `search` 44, `wiki_read` 1, `ingest` 1 and zero for everything else — but the zeros were an **instrument gap, not evidence**, because `expand`/`stats`/`level`/`devices` never called `instrumented()`, so three of the four demotion candidates could not be measured at all. `tel` is now built before those registrations and all four record, so the question is answerable next time on numbers. Honest observability is a pillar; a tool cut on an unmeasured zero would have been the opposite. +4 tests. |
| **R10.9 — "local" meant Ollama, whatever the target actually named** | 2026-08-14 | `dispatch()` short-circuited a trusted-local target straight into `options.inference` — the **Ollama-backed tiered service** — so the branch meant "local ⇒ Ollama", not "local ⇒ the endpoint this target names". Declare a loopback `llamacpp` target on `:8080` and `coder` silently drafted on Ollama at `:11434`, or failed outright with no Ollama installed. ~True while `ollama` was the only self-hosted provider; R10.8 added `llamacpp` and made it reachable by following the documented path. The fix splits the two questions the branch answered at once: **`permitsUnredactedDispatch` decides whether to REDACT; it does not decide where the bytes go.** Its condition is untouched (out of scope, independently re-checked); only a target that IS the tiered service's endpoint still goes to `InferenceService`, and every other trusted-local target is dispatched to the endpoint it names by its own provider's transport. The tiered service keeps its special case because it is the only path that maps a ROLE through the hardware-tier catalog, so it alone can serve a target declaring no model; `localServiceBaseUrl` (wired from `inference.ollama_base_url`) answers "is this that endpoint" by origin, and the documented fallback when it is absent is the provider name, whose residual imprecision — two Ollamas on different loopback ports — is stated rather than hidden. On R10.8's reserved question of a second unredacted egress: this is the **existing local class generalised from one hard-coded endpoint to any endpoint that passed the same loopback check**, not a new class — the `InferenceService` path has always POSTed an unredacted prompt to a loopback port. A spawn is excluded unconditionally whatever its trust claims, since the child talks to Anthropic. The audit line now names the endpoint, because "it went to the target it names" is the whole claim and `targetId` alone does not evidence it. **One pre-existing test was rewritten and it is called out per the R10.8 precedent**: `target-dispatcher.test.ts`'s "takes the unredacted direct path for a LOOPBACK llama.cpp server" asserted `sent.length === 0` and arrival at `inference.calls[0]` — it pinned the defect as the contract, and could only pass while the bug was present. The 3-block redaction gate and the rest of the egress suite are untouched. +4 tests (37→41). |
| **R9.20 — 98% of proxy start-up was finding the same secrets twice** | 2026-08-14 | Measured during R9.18: `credentialEnvForProxy` cost **6668ms of the proxy daemon's pre-`listen()` time — 98% of it**, against `migrateOnVersionChange` at 7ms and `loadConfig` at 11ms. Every account with a stored credential meant a PowerShell **process start** to DPAPI-decrypt it (~0.94s each), plus a one-time ~2.6s self-test probing `powershell.exe` then `pwsh` with an encrypt/decrypt round trip. R9.18 had already tried concurrency and got nothing, because concurrent PowerShell startups contend rather than overlap. Three changes, and the two that mattered removed the cost rather than hiding it. **One batched decrypt**: `getMany` sends every blob on stdin, one per line, and gets one base64 result per line back — base64 so a key containing an unexpected byte cannot desynchronise a positional mapping and hand one gateway another's key, and the exit code deliberately reports the HOST not the blobs (0 if any decrypted, 3 if none), because a PowerShell whose Security module fails to autoload throws on every line and an exit 0 there would report every credential missing. Secrets stay off the command line throughout, as the single-blob path always required. **The self-test stopped gating reads**: rather than caching a positive that can go stale — the risk the brief flagged — the read path now runs on the first host that succeeds at the REAL work, which is strictly stronger evidence than an encrypt/decrypt of a fixed string and was being gathered anyway; the self-test survives as `available()` and as the diagnostic that separates "no working PowerShell" from "this blob belongs to another user or machine" once something has actually failed. **And the proxy stopped paying twice**: the parent resolved to build the child's env and the child resolved again on startup, even though its own injection is `??=` so the second result was discarded every time — `CREDENTIALS_INJECTED_ENV` rides with the credentials it describes so the two cannot disagree, and the env allowlist is what stops a shell export forging it. Measured after, on this project: **6668ms → 1068ms**, and a unit test asserts ONE round trip for N accounts so a per-account loop cannot creep back. Precedence, silent-absence and one-bad-blob-doesn't-deny-the-others all preserved and pinned, plus a real Windows DPAPI round-trip over three keys. +16 tests. |
| **R9.21 — the large page that silently downloaded twice** | 2026-08-14 | Observed on `https://www.rfc-editor.org/rfc/rfc5280` (327,623 chars extracted): the web cache contained it `raw: true` — so Golem's own fetch completed — yet the tool result was WebFetch's genuine answer, meaning Claude Code fetched the same page again and paid the summarizer for it. Two downloads and a summarizer call; the user sees a normal answer, so nothing looks wrong and the cost is silent. Root cause was **two numbers that were the same 15 seconds**: `golem init` writes the pre hook's `timeout: 15`, and `DEFAULT_RAW_FETCH_TIMEOUT_MS` was also 15_000 — so the self-fetch was entitled to the entire budget and everything that must follow it ran past the platform's kill deadline. Not fixed by raising the timeout (the pre hook blocks the tool call, so a generous timeout turns a slow page into a long unexplained stall). Instead: **one exported constant** both `golem init` and the hook derive from, with a test asserting the value actually reaches `.claude/settings.json` — the hook cannot read its own timeout from the payload, so two hand-kept numbers was the bug. The fetcher gets only what is left after a reserve for extraction, redaction, the cache write and the serve, and a miss with no budget left **declines before fetching** so exactly one download happens. And the ordering was reversed: the KB **ingest**, which embeds the whole page and on a 327KB document is seconds of work, used to run BEFORE the serve on the critical path — so the fetch could succeed, the cache write land, and the hook still die before its stdout was read. Serving is what prevents the second fetch; indexing is a bonus, and doing the bonus first was strictly backwards. Out of budget now skips the ingest with a stderr note, having already served AND cached, so the next fetch is a hit either way. +6 tests. |
| **R9.19 — the reachability latch R9.12 designed and did not build** | 2026-08-14 | R9.12 shipped trust detection with two of its three signals: a published endpoint, and `NODE_EXTRA_CA_CERTS` naming our CA, confirmed by a TLS probe. §125 then measured that a hook's environment reflects the settings FILE rather than what Claude Code's TLS stack honours, and §121-A records they disagree in cloud and Desktop-app-managed sessions — so all three checks pass, the hook rewrites, and the fetch fails with an opaque TLS error. Nothing is lost (§122: `additionalContext` still delivers the page) but the transcript shows a failure where the floor would have shown Golem's own `NOT AN ERROR —` framing, which is strictly worse in appearance than R9.7 — the one thing R9.12 promised not to be. Built the third signal: **positive evidence that a real WebFetch reached the endpoint.** The endpoint records each `/w` request to a file it alone writes; the hook keeps its own verdict file. Optimistic-once, because a latch demanding evidence before its first rewrite could never obtain any — the only way to learn a rewrite works is to rewrite — so an undecided window goes green and records the attempt, and the next serve looks for a hit logged AFTER it. A trusted session is therefore green from its very first fetch, and an untrusting one renders **at most ONE** TLS error before every later serve takes the floor. The verdict expires after 10 minutes because it records a condition a restart fixes, and is keyed on the endpoint's `startedAt` so a restart with a regenerated CA re-probes rather than inheriting a stale yes — not on the session id, which R9.17 records rotates per tool call. Two files with one writer each, deliberately not the shared coordinates file. The R9.12 signals keep primacy: the latch can only ever take green away, never grant it. The brief said this cannot be reproduced from a terminal, so the gate is proven by **simulating the mismatch faithfully** — a real endpoint, real CA, real passing TLS probe, and `/w` left un-hit, which is exactly what differs in such a session; a real Desktop-app-managed session remains unverified and is called out. +15 tests. |
| **docs-slider-drift — the check that stops the sixth stale line** | 2026-08-21 | R11.1 retired the slider and R11.4 swept the CLI, the settings help and the installed skills; neither task listed `README.md` in its `touches`, so the **front page** went on teaching the retired control to every first-time reader. Fixing five lines was ten minutes — the deliverable is the check that stops the sixth, the same shape as R11.5's Index rule: `golem wiki check` now fails (exit 1) on a **retired identifier in prose that teaches the current system**, over every non-record wiki page plus `README.md`. **The exemption list is the whole design**, because `slider` appears in 90+ markdown files and the rule is only useful if it fires on the handful that teach and stays silent on the ones that *record*: dated-record zones (`debriefs/`, `syntheses/`, `sources/`), ADRs and `docs/plan/` are never scanned; a unit citing a record is exempt (what keeps `WIKI.md`'s Index legal — those lines describe what a debrief said); and a unit naming the retirement (`ADR-NNNN`, `R11.x`, `retire`, "no longer", "used to") is exempt, since prose is allowed to say a thing is gone. Granularity carried the correctness: a whole-section scope would have let one honest "retired" line inside `## Index` pardon the very drift the task was filed about, so **every list item and table row is its own unit, a paragraph is one unit, and a heading joins the paragraph beneath it — except when it is followed by a list**. `level N` is deliberately **not** in the pattern (`compression` is still a 0–3 dial; "level 3 (aggressive)" is live output of `describeDial`), and code fences are scanned rather than stripped — a copy-pasteable `golem slider 3` is the worst drift on a page, not the most excusable. The sweep it forced was 4× the brief: **22 hits across 8 files**, including `Redaction Stage.md` naming a *dial value* as the way to turn redaction off (the exact claim ADR-0004 made unrepresentable), `WIKI.md`'s "level 0 = passthrough (redaction OFF)", a retired `level` MCP tool in the Architecture diagram, and two doc references to symbols that no longer exist (`setSliderLevel`, `cli/slider-read.ts`). Two of the brief's five facts were wrong and checking beat trusting: the panel really does render `Level 1 lossless`, so that line stayed; the mock-up's hand-written `Golem 0.1.1` became `<version>`. Deliberately deferred with a task doc rather than a silent hole: the spec body, the VS Code README, and three stale user-visible strings in code (`docs-slider-drift-remainder`). +22 tests (2909 total, all green). Debrief: `debriefs/2026-08-21-docs-slider-drift-the-check-that-stops-the-sixth-line.md`. |
| **R12.6 — how a phone learns the agent is blocked: a doorbell, not a channel** | 2026-08-21 | Research spike, no code — verification-notes **§135**, all sources fetched live 2026-08-21. ADR-0006 Revision 2 had promoted **relay-originated push** to option 1; it is **refused**, by one bullet the ADR already contains. Sending to a VAPID-restricted subscription needs a JWT signed by the key bound at `subscribe` time (RFC 8292 §4; Apple: "The public key you include must match the public key you provided to `PushManager.subscribe`"), so the relay would hold a signing key — contradicting §3b's "It holds no key and terminates no TLS". Forwarding a *pre-signed* one fails differently: RFC 8030 §8.3 makes the endpoint a capability URL ("Knowledge of a push URI implies authorization to send push messages"), and RFC 8292 §5 concedes JWT replay, so the relay gains a durable wake address — **offline addressability, which §3b never conceded**, not a metadata increment. The argument that actually settled it was not cryptographic: **the blocked laptop is awake by definition**, so it can originate its own push, and relay-origination buys only IP-hiding from Apple/Google. Verified for the four brief questions: bodies are genuinely opaque (RFC 8291 exists "to be secured against inspection … by a push service", keys minted on the phone), but the W3C Push API's own §4 concedes timing, frequency and size with padding as "the only known mitigation", and its endpoint guarantee is scoped to actors "**other than the push service**"; iOS delivers Web Push to a **Home Screen** web app since 16.4 with **no Apple Developer Program**, gesture-gated, over APNs, and Safari 26.0 only made installing easier ("every website added to the Home Screen opens as a web app") with no push changes through 26.6; Apple's ban on invisible push turns out to **be** the feature (push count == notification count, so no covert channel is even constructible), and **Declarative Web Push** is the right primitive because ITP deletes service-worker registrations of rarely-visited sites and a companion app is rarely opened by construction. Every LAN-only alternative fails at the same point — **locked and off Wi-Fi**: Background Sync does not exist in Safari (MDN BCD `version_added: false`), ntfy's own docs require forwarding to an APNs/Firebase-connected upstream because "iOS heavily restricts background processing" and unaided delivery "can take hours", UnifiedPush has no iOS distributor, and Local Push Connectivity is SSID-bound behind an Apple-granted entitlement. **Outcome 1 of three, conditionally**: a self-hosted-enough path exists — laptop-originated, payload a padded constant ("something is waiting", never the project or command), no `Topic` header, `TTL` ≤ the gate deadline, no Golem-operated push service — with Apple/Google/Mozilla named as the third parties that remain. Two reproducible negatives recorded beside it. The transport answer then exposed a bigger question: a subscription is bound to an **origin**, and the app's origin is a LAN address that changes with the network, so R12.5's "the UI must not know which transport it is on" hardens into a requirement on the *origin* and lands on R12.8. Until a two-observation device test passes, R12.5 ships the degraded path and says so on screen: *"Alerts are off — this screen only updates while it is open."* Debrief: `docs/wiki/debriefs/2026-08-21-R12.6-a-doorbell-not-a-channel.md`. |
| **R11.8 — With brevity off, both status lines now render `🗜 lossless · ✂ off` — and the parity test finally visits that state** | 2026-08-21 | The last thing R11.6 left open, and the reason it was left: `render.test.js` asserted the old behaviour on purpose, so reversing it needed a written reason rather than a silent flip. The tell was two dials on one line under opposite rules — the bar printed `🗜 off` for compression at zero and nothing for brevity at zero — and an absent segment reads as an absent DIAL, not as a value: the reader cannot tell a setting from an older Golem that never had one. R10.24's suppression rule stands where no stage runs (off / unwired / bypass), which is a different question from a single dial sitting at zero, and both rules now have their own parity case. Root cause of the whole class: every R10.24 fixture used brevity "full", so a parity test that had held two renderers together for a release had simply never been handed that input. |
| **R11.7 — The proxy log is timestamped, every request reports what became of it, and a stream that ended without `message_stop` is named as TRUNCATED** | 2026-08-21 | Written after a live "Connection lost mid-response" took the client's transcript and the process table to diagnose, because Golem's own records could not: `proxy.log` had 11,655 lines with no timestamp on any of them, nothing was logged when a request ENDED (only the routing decision, taken before forwarding, so a request that died looked exactly like one that succeeded), and telemetry recorded savings but never outcomes. Now one sink stamps every daemon line ISO-8601 UTC — the format the client transcripts it must be correlated against use — and one line per request records target, status, duration, bytes and SSE event count across six outcome kinds, including the two that used to return in silence (a client hanging up, a translated stream breaking mid-flight). The signal only a proxy can see: an Anthropic stream ends with `message_stop`, so ending without it and without an `error` event (R10.23's case) is a truncation, named as one. Built on the EXISTING usage sniffer rather than a second stream hop — that scan already read the event names, so the answer was one boolean away. Metadata only, never bodies. Measured before deciding about rotation: 181 bytes/request against 65, ~2.0 MB per 11.6k requests. |
| **R11.6 — Every model on the status line wears one format — `<gateway> (<model>)` — with `+` joining models and `·` reserved for separating kinds** | 2026-08-21 | A user instruction, an hour after R10.24 landed: the chat model read `<gateway> (<model>)` while a worker read a bare id, and one glyph joined both the model list and the dial list so the line read as a flat run of fields. The ask could not be met without fixing the DATA first: `collectGolemState` passed a worker's raw `accountId` as its "gateway" (so a target with no account rendered gateway-less while the chat side said "ollama" for the identical provider), and `golem status --json` had no `gateway` on a worker row at all, which is where the VS Code extension gets everything it draws. Both now call `providerUpstreamLabel`, the function the chat segment already used. R10.24's parity test earned its keep on the first change after it shipped — failing on exactly the stale literal, passing everything else. Found and deliberately NOT fixed: the CLI prints `✂ off` where the status bar omits the segment, which `render.test.js` pins on purpose — every parity fixture used brevity "full", so a parity test only pins the states its fixtures visit. |
| **R11.3 — `golem on` / `golem off` persist `proxy.bypass_all` and apply it live, so the master switch survives a restart and every surface can see it** | 2026-08-21 | The switch was one POST to the running proxy's admin endpoint, which made it in-process only — and the non-persistence was the lesser half. `golem off` forwards raw with REDACTION OFF, while every status surface reads `proxy.bypass_all` from settings, so a bypassed machine rendered as a healthy pipeline with dials beside it and `REDACTION_OFF_WARNING` never fired. R11.1 had built the right home for that state a day earlier; the switch simply was not using it. Persisting fixed the reporting for FREE — no new status code, no fifth proxy state for R10.24's parity test to pin. Writing alone would have cost the instant apply (the setting is read at proxy construction), so it writes and then POSTs the same state live: durable and immediate. Nothing in the suite had ever covered these two commands, which is why R11.1's sweep missed them; the logic moved out of the commander callback so it could be tested at all. |
| **R11.5 — Every debrief is reachable from `WIKI.md` again, and `golem wiki check` fails on one that is not** | 2026-08-21 | Of 121 debriefs, 39 had no Index line — every one from 2026-07-16 on, all of R9 and R10. Not lost: the vector index still found them. What they had fallen out of was the GRAPH, so an agent starting at the Index and following wikilinks could not reach seven weeks of debriefs — the half the wiki exists for. `wiki check` passed throughout because it lints what a page CONTAINS (frontmatter, dates, resolvable links) and never asked whether anything pointed AT it. Backfilled with hooks read from each debrief, and the check now enforces it, scoped to `debriefs/` because those are append-only records whose whole value is being findable later. It caught its own debrief on the first run. |
| **R11.4 — R11.1's leftovers: four strings that still named the slider, a classifier branch for a tool that cannot be called, and the retired `/golem/slider` skill that `golem init` only CLAIMED to prune** | 2026-08-21 | All three found by deploying the previous release and reading its output — `golem init` printed “set compression.headroom_sidecar=true and slider ≥3”, a hint naming the control that release had removed. Pulling the thread: `golem config get`'s example key pointed at a setting that no longer exists; `classifyAction` still routed `mcp__golem__level` to a classifier whose five tests passed vacuously (removal is stricter, since an unrecognized tool falls through to `unknown`, which is gated); and `BASICS_SKILLS` carried a comment asserting that init prunes a skill dropped from the table, which `installSkills` — iterating that table — cannot do. `pruneRetiredSkills` makes the claim true under R9.5's provenance rule: byte-identical to what Golem wrote is Golem's to delete, edited or unrecorded is a reported conflict, so the install and prune sides now share ONE ownership rule. A retirement is done when the installed project stops mentioning the thing, not when the source does. |
| **R11.2 — The session-start index sync is bounded: a cap on how much it may embed unasked, a checkpoint so an interrupted run is not repeated, and one collection per project however its path is spelled** | 2026-08-21 | Keeping the KB fresh costs GPU time on the user's machine, and three faults made it pay far more often than the work required. Measured here: ~42 s for 8 changed files, ~10 minutes for 114 — and a branch switch rewrites mtimes wholesale, so an ordinary `git checkout` queued hundreds. `knowledge.auto_index_max_files` (default 50) now defers past the cap and names `golem index`, which stays uncapped because it IS the explicit ask. Worse than the size was the repetition: the manifest was written only after the last file, so any session shorter than the sync recorded nothing and the next session redid the identical work — now checkpointed every 20 files, deletions first because they cost no embedding. Third, `canonicalProjectId` folded drive-letter case only, so `d:/repo`, `D:\repo` and `D:\repo\` hashed three different ways: this repo had three collections, 231 MB, two of them unreachable. Windows spellings now collapse to one; POSIX ids are left alone, where `\` is a legal filename character. |
| **R11.1 — The slider is retired: `compression.level` and `brevity.level` are the only dials, and the full bypass becomes its own explicit `proxy.bypass_all`** | 2026-08-20 | The user's own status output made the case — `Slider: level 3 (aggressive) → effectively 1 (lossless)` plus a warning to explain why the number did nothing. A preset over two dials meant two controls for one thing; on a caching upstream (the default) its top half was inert. Best outcome: the stage table no longer has a redaction-free row, so "a dial turned redaction off" is unrepresentable rather than defended by a clamp — `MIN_ACTIVE_COMPRESSION_LEVEL` is gone with the scale it guarded. The `level` MCP tool, `golem slider`, `golem.setSlider` and the `slider` status block went too. Migration transforms rather than renames (one key → three, values computed), reproduces the retired resolvers exactly so a pin still wins, and was verified on seven fixtures plus this repo's real settings file. Found on the way: `golem off` is in-process only and silently reverts at every proxy restart. |
| **R10.24 — The CLI status line and the VS Code status bar render one canonical shape, the four proxy states are named in one vocabulary, and the VS Code picker can reach every configured model** | 2026-08-20 | The two lines had drifted apart in six ways (dial position, role order, gateway-vs-vendor label, model precedence, how many proxy states exist, and which glyph bypass gets) with nothing pinning them but a test on the GLYPHS — which is why they felt covered. A new parity test renders equivalent state through both implementations and demands one identical string; it immediately found the CLI drawing the redaction-only bypass shim as a filled green hexagon. Dials are no longer displayed in states where no stage runs (off/unwired/bypass), extending R8.32's rule from the glyph to the numbers beside it. The picker now offers TARGETS: `inference.default_target` accepts a target id and `golem target use` exists, so a gateway fronting two models no longer strands one of them. |
| **R10.23 — A mid-stream upstream failure reaches the client as an error instead of wearing the "empty completion" notice, OpenRouter reasoning traces stop being dropped, and Golem's own pipeline can no longer hold a live request silently** | 2026-08-19 | A user's deepseek-via-OpenRouter sessions dead-ended on Golem's empty-completion notice: the gateway's real cause ("Provider disconnected unexpectedly") arrives as a `data:` frame with a top-level `error`, and the translator dropped every frame without a `delta` — then closed the stream `end_turn`, so in-band prose read as a finished turn. Now relayed as Anthropic's own streaming `error` event, naming model, provider and cause, and retryable. Second dropped signal in the same file: OpenRouter returns the trace as `reasoning`, not `reasoning_content`, so every trace from every OpenRouter model was discarded (answers and corrects §84's open question). Third fault, from the same report's aside: the local-answer stage awaited a cold embedder inline on session-opening requests — now bounded at 2s, fail-open, and any hold past 750ms says so with a per-stage breakdown. |
| **R10.21, R10.22 — Claude Code's wiring moves to `.claude/settings.local.json`, `golem config set` defaults to the local scope, and the seeded guidance loses a third of its bulk** | 2026-08-14 | `golem init` no longer commits machine-local wiring (per-project port, hooks needing `golem` on PATH) into a file every clone receives — `claude.settings_scope` picks the file, default `local`, and init MOVES the wiring rather than duplicating it. Surfaced and fixed a second override path the two-file split creates: a user's `statusLine`/`defaultMode` in the committed file was left intact but inert. The seeded guidance rules, which load into every session, went 8,322B → 5,684B with four load-bearing facts restored. |
| **R10.12, R10.13 — The bypass shim is back, and daemon staleness sees code changes, not just versions** | 2026-08-14 | `golem proxy stop` no longer exists-and-kills: the port stays served by a redaction-only shim, so a stopped proxy answers 401 from upstream instead of connection-refused. `golem status` now reports a daemon that predates the last `npm run build`, which version-only comparison could never see. |
| **R10.16, R10.19, R10.20 — Stream keepalive, headroom_config reported where it is read, and synthesized thinking blocks labelled** | 2026-08-14 | A translated stream now pings during a long prefill instead of sending nothing for ~2 minutes (3 pings observed live). `golem status` names a `headroom_config` key that cannot reach Headroom even when the stage never runs. A thinking block Golem builds from `reasoning_content` now says so, after an unlabelled one was mistaken for cross-project contamination. |
| **R10.17, R10.18 — The CCR offload swap reaches `Read`, and an empty upstream completion surfaces as an error** | 2026-08-14 | `Read` output is offloadable for the first time (its text is nested at `file.content`; 62 unswapped in one project). An upstream that produces nothing now says so on both paths instead of returning a well-formed empty answer. |
| **R10.14, R10.15 — Vision-aware image translation for OpenAI-schema upstreams, and a local answer for `/v1/messages/count_tokens`** | 2026-08-14 | A duoop session that had answered nothing since 2026-08-13 answers again: 468k → 31k tokens (93.4%), 130s of silence → 4.4s. `count_tokens` returns `{"input_tokens":N}` instead of a billed completion. |
| **R8.14 — `golem pkg install/remove/upgrade` — the write half of the managed-package registry, and the detector it caught lying** | 2026-08-21 | Installs invoke the UPSTREAM's own installer as an argument array at a recorded pin, with consent the autonomy gate demands (`ask` for `outward` at every level); `upgrade` on a pinned row re-runs install byte-identically, so it can converge on the pin and never move past it, and both Headroom rows refuse `upgrade` outright and name T-C4. Two rows get an automated path, the rest refuse with a reason. `pluginOnDisk` was reading the content cache `claude plugin uninstall` leaves behind, so `golem pkg list` had reported Caveman `[found]` for ten days while `claude plugin list` said nothing was installed. |
| **R8.11 — Plugin seams — third-party redaction rules, pipeline stages and MCP tools without forking, and an ADR that admits there is no sandbox** | 2026-08-21 | Extending redaction no longer means forking Golem. ADR-0005 states plainly that a plugin has the full authority of the process and Node cannot contain it, so every mitigation is structural instead: built-ins always run first and the rule table has no remove/replace/reorder function; a plugin stage sees only redacted content and redaction RE-RUNS over whatever it returns; a tool name colliding with a built-in is rejected at load. `plugins.load` is empty by default and nothing is discovered — no scan, no registry, no download. |
| **R12.7 — continue is not approve: the reverse channel exists now, and Golem still ships no button** | 2026-08-22 | Written answer, no code — verification-notes **§136**, every source fetched live 2026-08-22 against the installed client `2.1.235`. The brief expected a confirmation and got a reversal: Decision 37's *architectural* half still holds verbatim (the proxy sits below Claude Code, `/resume` is a client-side command it never sees, no request-path mechanism was added), but its *product* half — "no IPC into an already-running interactive session", "the only techniques that reach a live TUI are tmux `send-keys` / keystroke injection" — is now **false**. Anthropic shipped three supported mechanisms, none of them screen-scraping, and the material one is an MCP server: **channels** ("a channel is an MCP server that pushes events into your running Claude Code session" — declare `capabilities.experimental['claude/channel']`, emit `notifications/claude/channel` with `content`/`meta` over stdio, the event lands in context as `<channel source=…>`), joined by **Remote Control** (`claude --rc`, phone or claude.ai drives your live local session) and **cross-session messaging** (`SendMessage`; "when the receiving session is idle, Claude Code starts a new turn with the message" — v2.1.224+, macOS/Linux only). Anthropic also relays permission prompts to a channel (`notifications/claude/channel/permission_request`, local dialog held open in parallel, first answer wins), which is ADR-0006 capability 2 built first-party and a question R12.3 now has to answer. So ADR-0006's capability-3 row and spec Decision 59(g) are superseded **on their reason, not their outcome**, and both carry dated pointers rather than an agent's silent rewrite. **The outcome holds: no continue button, and the reasons are now quotes rather than preferences** — opt-in per session by launch flag ("being in `.mcp.json` isn't enough"), off the Anthropic-curated allowlist (`--dangerously-load-development-channels` today), **no delivery acknowledgement** ("Claude Code drops the events silently and returns no error to your server", so a phone button can only lie), an explicitly unstable contract needing claude.ai/Console auth that does not compose with Golem's account routing, and decisively: **authoring a turn is a larger authority than answering one**, so injected free text routes around ADR-0002's class line instead of sitting behind it. The honest substitute is unchanged — **enqueue, do not inject** (R5.1) — and is deliberately kept out of R12.5: if ever surfaced it is labelled "Add a task for later" with "Nothing runs until your laptop picks it up.", never beside Approve/Deny, never with a spinner. Consistent with §135, which is not reopened. R12.5's sentence for the screen: *"There is no prompt box — this screen can answer what the agent is already asking, but nothing here can start a turn."* Follow-up filed as **R12.10**. Debrief: `docs/wiki/debriefs/2026-08-22-R12.7-the-channel-that-appeared.md`. |
| **R12.2 — The blocked-state file became a documented read model: which project, which session, blocked on **what** (the tool and its verbatim argument), and since when — redacted before it is written, and rendered by all four surfaces** | 2026-08-21 | "Waiting on your input" is not an approvable question, and Claude Code's own `Notification` payload names no tool (verified against the live hooks reference: its example message is the generic "Claude needs your permission"), so the tool and argument are captured by the PreToolUse hook as a pending call and correlated when the notification arrives ~6s later — recorded for a deferral *or* a forced `ask`, which a test using `rm -rf` caught the first implementation skipping. `resolveBlock` replaces per-renderer staleness arithmetic with four statuses, so `abandoned` (blocked, stale, nobody ever wrote again) stops reading as "nothing is happening", and `unknown` stops reading as `clear`. Redaction is an unconditional, fail-closed choke point inside the write path — a recursive walk over every string, so a field added in R12.5 is covered by construction. R11.8's lesson went unheeded exactly one release: `golem statusline` had printed `⏸ waiting` since Decision 21b while the VS Code status bar an inch away printed nothing, and nine parity assertions missed it because no fixture was ever blocked. No listener binds anywhere new; nothing gained write access. |
| **R12.1 — ADR-0006: a phone may unblock the agent, never authorize the irreversible** | 2026-08-21 | The threat-model ADR R6.3 had been blocked on for 52 days, ACCEPTED as spec Decision 59. Confining remote approval to `read`/`write`/`unknown` — never `destructive` or `outward`, with no setting to change it — meant ADR-0002 needed **no amendment**: a stolen phone cannot push, publish, deploy, `ssh` or delete. Revision 2 reversed R6.3's "self-hosted relay only" on the user's instruction and recorded it as a reversal, made safe by three constraints added in review: a blind relay, mandatory 2FA, and **local-only enrolment**, so a fully compromised account cannot pair a device. Decision 59(g) corrected 2026-08-22 after R12.7: capability 3 is declined, not impossible. |
| **subagent-park — a subagent cannot park, so the spawn is what gets gated** | 2026-08-22 | The park is a *tool-call* gate and a subagent never reaches it: the limit is hit on a **model request**, so a child's turn fails upstream (`Agent terminated early due to an API error: You've hit your session limit`) before it can propose a call to deny — no turn, no note, and anything uncommitted is gone. Observed 2026-08-22, when two of three dispatched R12 agents died one word after "All green. Committing." and survived only because they had already committed, while the parent at ~96% was protected correctly. Making the child's gate stricter is impossible by construction, so the refusal moved to the one part the parent issues as an ordinary tool call: the **spawn**. `src/hooks/spawn-gate.ts` refuses when `utilization + spawn_cost_fraction × (in-flight + 1) > 1`, at a default 0.18 measured from the incident itself (~171k–186k subagent tokens over 85–94 tool calls each), and puts every number in the message because a refusal that does not say what it measured gets worked around. The **in-flight** term is what catches the actual failure: utilization already contains what running children spent, but not a sibling dispatched *since* the reading — the three-at-once fan-out where each spawn reads the same pre-batch number and each looks affordable alone. Blind (no reading, or a cold header feed) warns **once per reading** and then lets a re-issue through — fail-closed without recreating the R9.23 deadlock. Ordered *after* the park deliberately, so above the threshold the agent is still told to park; the gate therefore bites in the band below it, which is exactly where the lost agents were dispatched. `snooze.spawn_gate` / `snooze.spawn_cost_fraction`; `golem status` now ends `park enforced · spawns allowed\|REFUSED\|ungated\|warn-once ~18%/agent`, computed against the live reading so it claims the gate is *biting*, not merely enabled. What no tool-call gate can reach is seeded as the `subagent-headroom` guidance rule: commit working increments early on your own branch, and convert a dead child's task notification into a durable task — both inside Decision 37's boundary. Demonstrated against an injected utilization through the real hook, not argued from the code. Notes **§137**; debrief `docs/wiki/debriefs/2026-08-22-subagent-park-gate-the-spawn.md`. |
| **redaction-path-uuid — a path with an embedded UUID is redacted no longer** | 2026-08-22 | `isPathLikeToken` (redaction-rules.ts) required every `/`-`-`-`_`-delimited chunk of a candidate to be pure-alpha or pure-numeric, so a UUID/hash chunk (mixed letters+digits) disqualified the whole path — the session scratchpad path and `.claude/worktrees/agent-<id>/` redacted whole, seen twice independently plus a third sighting from the heredoc-readback angle. Fixed by accepting a pure-hex chunk as clean too, gated by a 3-chunk floor; safe by call order, not just the guard — `isHighEntropyToken` already excludes whole-token pure hex before `isPathLikeToken` runs, so any admitted hex chunk implies another chunk is a genuine word. Verified end-to-end after rebuild+restart, not just in unit tests: fresh UUIDs built at runtime, ground truth measured on disk before viewing the tool-output round-trip, both path families and both `/`/`\` spellings agreeing, a random-secret negative control still redacting. `npx vitest run` 230 files/2987 tests passed (1/2 skipped), `tsc`/lint/format/`wiki check` all exit 0. Wiki: [[Redaction Path Placeholders]]; verification-notes **§140**. |
| **ccr-ref-scope — `expand` misses a ref issued minutes earlier across a git worktree; "expired" was never real** | 2026-08-22 | Reproduced BEFORE fixing (`tests/integration/ccr-worktree-scope.test.ts`, committed failing first): a `PostToolUse` hook rooted its CCR write on `cwd`, `expand` is served by whichever `NativeLosslessCompression` the MCP server was built with (the main checkout) — same refId, two `.golem/ccr` directories. Grepping `CcrStore`/`LocalDirBlobStore` for `prune\|evict\|ttl\|maxEntries` found nothing, so "unknown or expired" was always a guess. Decided a git worktree is the SAME project as its main checkout for CCR purposes, agreeing with `canonicalProjectId`'s identical call for the vector index (R11.2 precedent) — both now resolve through one new shared, synchronous, subprocess-free function, `src/shared/git-worktree.ts#resolveWorktreeRoot`, reading git's own `.git`-file/`commondir` bookkeeping directly so the two identities cannot drift apart. `UnknownRefError` gained `location` and `reason: "not-found" \| "corrupt"` so a miss names what was checked and where, replacing the fixed "Unknown or expired" text in `expand` and the in-memory MCP stub. Verified end-to-end against the rebuilt `dist/` artifacts, not just source: a worktree-issued ref retrieved byte-identical from the main checkout, and an unsatisfiable ref's message names the exact root searched. +18 tests across two commits: the 1-test reproduction (committed first, failing), then +17 in the fix commit (`tests/unit/shared/git-worktree.test.ts`, `tests/unit/compression/ccr-store.test.ts`, a worktree case added to `file-driver.test.ts`). Debrief: `docs/wiki/debriefs/2026-08-22-ccr-ref-scope-worktree-identity.md`. |
| **docs-slider-drift-remainder — the spec body, the VS Code README, and the last two stale strings** | 2026-08-22 | The deliberate deferral docs-slider-drift left behind: the check's own allowlist (`PROSE_FILES_OUTSIDE_WIKI`) still read `["README.md"]`, so `docs/golem-spec.md` and `vscode-extension/README.md` kept teaching the retired slider undetected. Widening it required new machinery, not a weaker rule — verified concretely rather than assumed: most of §9's 44 slider mentions cite a decision or `verification-notes` by name rather than the literal path/extension `RECORD_CITATION` requires, so a heading-keyed `decisionsLogStartLine` exemption (skip any unit at or after a `## … Decisions Log` heading) was added instead of loosening the per-unit regexes project-wide. The spec rewrite across §1-8 caught two bugs beyond renaming: two references to a `slider ≥4`/"levels 3-4" that never existed even pre-ADR-0004, and one line describing a local-draft feature Decision 31 removed outright rather than renamed. The VS Code README rewrite was grounded in `render.js`/`extension.js`'s actual current model before touching prose — the panel already renders a `compression.level` picker and a `proxy.bypass_all` danger prompt, so the fix was catching prose up to code, and in the process found the pre-existing text conflating **Passthrough** (proxy stopped) with **Bypass** (proxy running, `bypass_all` on — Decision 56), corrected in the same pass. `src/config/ui-model.ts`'s brevity summary dropped the invalid `"auto (follow the slider)"` option and fixed a garbled leftover detail string; `src/tui/header.ts:42`'s `slider` variable was renamed to `level` (render-identical). Left deliberately untouched: `src/dashboard/server.ts`'s `slider-level`/`slider-name` DOM ids (owned by R12.6) and stale prose in `render.js`'s code *comments* (not one of the three named surfaces). +3 tests (2985 total incl. 2 skipped, all green). `golem wiki check`: 171 pages + 3 docs, 0 issues. Debrief: `docs/wiki/debriefs/2026-08-22-docs-slider-drift-remainder-spec-and-vscode-readme.md`. |
| **R12.11 — Spike: Golem as a channel, measured against building the transport ourselves** | 2026-08-22 | Written answer, no code — verification-notes **§141**. Load-bearing discovery: `src/autonomy/gate.ts` never emits `deny`, only `allow`/`ask`/`null` — `destructive`/`outward` gets a forced `ask` at every level, and `ask` is a question, not a resolution, so it does not by itself hold ADR-0002's class line against Anthropic's channel-relay-notified dialog (R12.7). `code.claude.com/docs/en/hooks` documents an unused **`PermissionRequest`** event, resolving before any dialog exists, with its own `behavior:"allow"\|"deny"` shape — a real `deny` there closes the gap with a hook fix, not a new transport. **Recommendation: channel wins — conditionally**, pending one live interactive confirmation no in-repo PTY harness can provide. Disposition: `R12.3`/`R12.4`/`R12.8`/`R12.9` cancelled, `R12.5` reduced to observe-only, `R12.12` (hook fix, agent) and `R12.13` (live confirmation, `owner: user`) filed. Debrief: `docs/wiki/debriefs/2026-08-22-R12.11-channel-wins-conditionally.md`. |
| **R13.2 — the conversation store: redacted transcripts, local-only, bounded, forgettable** | 2026-08-22 | ADR-0007 §6's deliberate, narrow exception to `session-tree.ts`'s content-hashes-only rule. New frozen contract `src/interfaces/conversation-store.ts` (append/read/list/forget — R13.3/R13.5/R13.8 depend on it); `LocalConversationStore` runs every turn through `redactRequestBody` **unconditionally** inside `appendTurn`, proven with a runtime-generated github-token secret asserted absent from the stored bytes. Bounded by count (32) and age (30 days), oldest evicted first, precedent-shaped after `web-cache.ts`. Identity reuses `cachePrefixFingerprint` (R8.13) so one conversation has one id shared with `session-tree.ts`; `resolveWorktreeRoot` (`ccr-ref-scope`) collapses a linked worktree to its main checkout first. `.golem/conversations/` added to `.gitignore` explicitly and proven covered with `git check-ignore`, not assumed. `golem session forget <id>`/`--all` wired to the real store. Found and fixed along the way, verification-notes **§143**: a Commander.js quirk where a parent command and child subcommand declaring the same `--dir` flag silently drops the CLI-typed value — the `forget` action now reads it via `command.parent.opts()`. +18 tests (3059 total incl. 2 skipped, all green). Debrief: `docs/wiki/debriefs/2026-08-22-R13.2-conversation-store.md`. |

| **R13.1 -- Spike: hosted multi-turn claude CLI viability** | 2026-08-23 | Client `2.1.235`; verification-notes **§142**. Confirmed the unverified assumption behind ADR-0007 §3a runner: `claude -p --input-format stream-json --output-format stream-json` accepts a second message on stdin without exiting, holds one stable `session_id`, emits structured tool-call events, honours `ANTHROPIC_BASE_URL`, and fires `.claude/settings.json` hooks (invariant 2 precondition). Two real gaps: Windows SIGINT cannot interrupt a running turn (process-kill only), usage-limit park architecturally orthogonal (API error not Golem snooze). Fallbacks priced but neither built. Recommendation: keep the `claude` CLI as §3a runner. Debrief: `docs/wiki/debriefs/2026-08-23-R13.1-hosted-multi-turn-viability.md`. |

| **R13.11 — `coder` 401d on the harness default and could not iterate: `inherit` auth is a proxy concept, and a dispatch carried one prompt** | 2026-08-27 | Two user-reported symptoms, both reproduced live before any code changed. **(A)** With `worker_targets={}` and no `default_target`, R10.8’s step 4 lands on the synthetic `anthropic` target, whose scheme resolves to `inherit` — right for the PROXY (forward the client’s headers) and impossible for a dispatch, which ORIGINATES its request and holds no Claude Code credential. The pre-flight guard read `mapper === undefined && authScheme !== "inherit"`, i.e. it exempted the one case that could not work, so every unrouted `coder` call POSTed unauthenticated and returned a bare `401`. **Step 4 had never once worked**; R10.8’s gate was phrased over the audit record and `golem status`, both of which were correct throughout, and a test named *"still dispatches with no credential when the scheme is inherit"* asserted the defect as intent. Latent defect found beside it: `makeAuthMapper("inherit", key)` discards any key, so `golem gateway login anthropic` could never help `coder`. **(B)** `DispatchRequest` carried one `prompt` and dispatch sent one user turn, so every call was turn one — confirmed live, qwen3.7-flash replying *"I don’t have visibility into your previous attempt"*. Not a looping model, a stateless transport. Shipped: `originationAuthScheme()` (a different question from `defaultAuthScheme`, not derived from it) so `inherit` resolves to the header Golem must set itself and a stored key is actually presented; `NoDrafterConfiguredError` on the harness step only, surfaced by `coder` as a NON-error decline that hands the work back inline (USER decision 2026-08-26, chosen over a local-Ollama fallback — R10.8’s "local is a destination, never a fallback" rule preserved); `DispatchRequest.attempts`/`.system` rendered by `buildDispatchMessages` into real `system→user→(assistant,user)*` turns, exposed as the `coder` tool’s `previous_attempts`; `redactReversibleTexts` sharing ONE `PlaceholderTable` across every turn (per-message tables would silently CORRUPT restoration, not leak) with the single-string form reimplemented in terms of it; `REMOTE_MAX_TOKENS=4096` shared by both transports (the OpenAI path pinned none); `reasoning_content`/`reasoning` read as a fallback; Anthropic system turns hoisted to the top-level field; `RefineOutcome.revisedBy` so a locally-revised remote draft is attributed honestly. The `coder` tool description stopped claiming "local… nothing leaves the machine… omit for the local model" — false since R10.8 wired the dispatcher unconditionally. 10 defect-encoding tests updated, +16 added; **3074 passed / 2 skipped / 0 failed**, `tsc` and `format:check` exit 0. `npm run lint` exits 1 on **4 errors pre-existing on `main`** (merged R13.4-me mTLS files), proved identical by stashing and re-running — this branch adds none. Debrief: `docs/wiki/debriefs/2026-08-27-R13.11-coder-inherit-auth-and-stateless-dispatch.md`. |

| **R13.13 — two target-surface lines that stated the opposite of the truth** | 2026-08-28 | Both display-layer only, and both found while answering a user question about routing `coder` to a different model — the underlying logic was correct in each case, which is why they mattered: the code did the right thing and told the user it had done the wrong one. **(1)** `golem target list` rendered `key MISSING` for a provider reached without a key. The *warning* logic had asked the right question since R10.8 (it exempts a model server the user runs themselves, with a comment saying keyless is "the CORRECT state for these, not a degraded one"), but the renderer asked only whether an account id was present — so a correctly configured `ollama`/`llamacpp` target read as broken while the warning list stayed silent. Two surfaces, two answers about the same target. Fixed by carrying `keyless` on `TargetRow`, computed once in the collector where the provider is still strongly typed, rather than re-deriving it in the renderer; the new branch is asked BEFORE the account id, because "this provider needs no credential" outranks "this target names an account". **(2)** `golem gateway add` warned that a model id "will be IGNORED on the wire — recorded for display only" on a SPAWN provider. The condition was `!isTranslatingProvider`, standing in for "forwards the client’s model id" — different sets. A spawn provider forwards nothing (no wire) and pins the model explicitly (`claudeCliArgs` builds `--model <id>`); it is also what the dispatcher’s same-model guard compares against, so a user who believed the warning and omitted the model got a target that could not dispatch at all. **This one actively misled a real user, who asked whether the spawn route could target a different model because the warning said it could not.** Condition now asks both questions. +3 tests, deliberately written against `llamacpp`/`openai` so bug 1’s coverage survives R13.12 removing the `claude-cli` provider. Debrief: `docs/wiki/debriefs/2026-08-28-R13.13-target-surface-honesty.md`. |

| **R13.12 — `default_coder` picks a MECHANISM, and Golem generates the subagent it cannot spawn** | 2026-08-28 | USER decision 2026-08-28, confirming the rule `proposals/multi-target-routing.md` already recorded: the `coder` MCP tool serves weak local models, a subagent serves capable ones, and for the capable case there is "zero new Golem machinery". What was missing was the WIRING. The constraint that shaped the design: **an MCP server cannot spawn a harness subagent** — MCP exposes tools TO a client, so there is no call `coder` could make that starts one. So Golem owns the wiring instead: `inference.default_coder` naming a MODEL makes `golem init` write `.claude/agents/golem-coder.md`, and `coder` DECLINES naming that subagent rather than dispatching somewhere the user did not pick for this purpose (silence was the alternative and is worse — the definition would sit unused). Naming a TARGET keeps the existing dispatch path. Unset keeps R13.11’s decline. The ambiguity (`claude-sonnet-5` and `anthropic` are both bare words) is decided: a declared target wins, a bare gateway id resolves to its first target exactly as `default_target` does — written explicitly, because `resolveTarget` does NOT do gateway resolution and a test caught the convenient assumption — and a colon-shaped id resolving to nothing RAISES rather than becoming an agent naming a nonexistent model. `inference.coder_prompt` feeds the dispatch `system` field AND the generated agent body from one source; the module header records why it is NOT a skill, against R9.11’s layering rule (direction: a skill loads into the invoking session, not the drafter; portability: SKILL.md is one client’s format and R6.1 extends past Claude Code; semantics: `description` drives when-to-invoke, meaningless for a system prompt). The generated file is provenance-guarded via `managed-files.ts` so a user edit is reported as a conflict and KEPT, and the install step also REMOVES a stale definition when `default_coder` stops naming a model — generation implies de-generation, or the file becomes a confident lie the moment the setting changes. `.claude/agents/` treated as a SHARED namespace: only Golem’s basename is touched, directory pruned only if empty. +25 tests; verified live against a rebuilt `dist/` (creates, refreshes, removes, prunes, uninit takes it). **`claude-cli` removal deliberately DEFERRED to R13.14**: nothing in this repo can prove Claude Code honours the generated `model:` and routes the delegation through `ANTHROPIC_BASE_URL`, and deleting a working mechanism on an unverified replacement inverts the safe order. Debrief: `docs/wiki/debriefs/2026-08-28-R13.12-default-coder-subagent.md`. |

| **R12.12 — the gate moved one event earlier, where a decision can actually resolve the request** | 2026-08-28 | R12.11 found the deciding fact: `src/autonomy/gate.ts` never emitted `deny` for `destructive`/`outward` — it emitted `ask`, at every autonomy level, on `PreToolUse`. **`ask` forces a question; it does not answer one**, so a permission dialog still opened — and per the channels reference a connected permission-relay channel is notified *"when a permission dialog opens"*, with nothing anywhere exempting a particular call once one exists. Shipped: a `PermissionRequest` hook (`src/hooks/permission-request.ts`) returning `hookSpecificOutput.decision.behavior: "deny"` for those two classes at every level, carrying the same reason text the `ask` carried, so no dialog is shown and by construction there is nothing to relay. `decidePermissionRequest` is a pure, level-independent decision in `gate.ts`, deliberately NARROWER than `decideGate`: `unknown` still only earns an `ask`, because fail-closed means make the human decide, not decide for them one event earlier. `PreToolUse`'s `ask` deliberately KEPT as a redundant second layer — it is what carries `appendActionLog` and `recordPending`, and what still stands if the earlier hook is unwired or beaten by a foreign one; the new handler writes **nothing**, so it cannot double-count. The shapes are not interchangeable and the wrong one fails silently (`PreToolUse` flat `permissionDecision` vs `PermissionRequest` nested `decision.behavior` + `message`), so a test asserts the ABSENCE of `permissionDecision`. Wired by `golem init` and by `golem autonomy wire`, which now installs and removes BOTH gate hooks together; fast-pathed in `cli/fast-path.ts` because it fires with a human already waiting. Doc shape re-verified against the cached hooks reference. **What it cannot prove** — that the deny pre-empts a connected channel's relay in a live interactive session — is `R12.13` (`owner: user`); headless `-p` is structurally unable to open a dialog at all (§141). ADR-0006's R12.11 block updated to say exactly that. +15 handler tests, +4 matrix tests, +1 init assertion; **3121 passed / 2 skipped / 0 failed**. Debrief: `docs/wiki/debriefs/2026-08-28-R12.12-permission-request-gate.md`. |

| **R12.5 — the companion app: the dashboard that already existed, bound one interface wider** | 2026-08-29 | R12.11 had already reduced this to capability 1 (observe): Anthropic's channel relay carries remote *authorize* first-party, so the one genuinely novel thing this task might have built is subsumed rather than needed. What was left was four things the dashboard was not. **Reachable** — `startDashboard` bound `127.0.0.1` and nothing else, the one address on the machine a phone definitely cannot reach; `host` now defaults to loopback with `LAN_HOST` as an explicit opt-in (`golem dashboard --lan`, or `telemetry.dashboard_lan`, neither settable from the phone). **Phone-shaped** — mobile-first, widening at a 40rem breakpoint, safe-area insets for a notch, and the wide stage table scrolling in its own box instead of making the body scroll. **Installable** — a real web manifest plus real PNG icons generated from `node:zlib` in ~130 lines (a PNG is CRC'd chunks around a zlib stream; adding an image library for three icons would be a dependency for a decoration), supersampled 3x3, and asserted by DECODING them — signature, `IHDR` dimensions, `IEND` — not by checking that bytes were served. iOS ignores the manifest entirely, so `apple-touch-icon` + the `apple-mobile-web-app-*` tags ship alongside it. **Honest when disconnected** — the old poller caught fetch failures and silently retried, leaving the last good numbers under a live-looking header, which IS stale data; three missed polls now greys the readout and shows a "not connected" banner, with `visibilitychange` re-ticking because an installed app resumed from the background has been asleep, not connected. **No service worker, deliberately**: it cannot register over plain-HTTP LAN, but the deciding reason is that a caching worker is a machine for showing stale data and the gate forbids exactly that. The security argument for the LAN bind is **structural, not policy** — no write route exists and every method but GET/HEAD is refused, asserted across four methods x five paths — and the page says so out loud where a control is not: *"This screen is read-only… nothing here can answer the agent or start a turn."* +16 tests; **3137 passed / 2 skipped / 0 failed**. Live run bound `0.0.0.0` and served over a real LAN address, manifest as `application/manifest+json`, `/icon-180.png` 936 bytes and decodable, `POST /api/stats` → 405. **What no test here can prove** — `display: standalone` actually suppressing browser chrome, iOS using the touch icon rather than a screenshot, and whether a 6s stale threshold reads well walking out of Wi-Fi range — is filed as `R12.14` (`owner: user`), because no test in this repo holds a phone. Debrief: `docs/wiki/debriefs/2026-08-29-R12.5-companion-app.md`. |

| **R13.4 — device and user authentication: two claims, one gate, and a dead subsystem replaced rather than repaired** | 2026-08-29 | The write surface now requires BOTH a client certificate this project's device CA issued (mutual TLS) AND a live unlock window; either missing is a 401 naming which claim failed, never a degraded mode. **What was found first**: ~1,180 lines of this task were already merged on `main` and none of it ran — `devices-auth.ts` was never registered so `golem device` did not exist, `mount-mtls.ts` was imported by nothing, `requestCert` appeared nowhere in the repository so no server ever asked for a client certificate, the only `devices` test was about Ollama model slots, and `user-factor.ts` cited *verification-notes §143* — R13.2's conversation store — for the passkey finding the gate says must be MEASURED. **The measurement did not exist.** A latent trap sat beside it: `isWriteSurface()` returned true for any POST, i.e. every Anthropic API request, so had it ever been mounted the developer's own Claude Code traffic would have demanded a device certificate and a passcode. USER decision 2026-08-29: replace, don't repair. **What shipped**: a device CA separate from the loopback CA (one is installed in a trust store so clients trust our server, the other is installed nowhere and identifies a client — one key for both would put a client-identity signer into the file the user trusts for server identities); `clientAuth`-only device certificates verified explicitly per request, EKU included, because a chain proves who signed a key and not what the key may do; local-only enrolment with a single-use, 10-minute code burned BEFORE the certificate is returned; a passcode factor with an absolute window, an idle relock, and a separate step-up freshness check measured against when the passcode was TYPED (continuing to read a stream and originating a session are not the same authority); a write server on its own port with `requestCert: true, rejectUnauthorized: false` — a handshake-level rejection renders on a phone as "cannot connect", indistinguishable from Golem being off, so the decision moves into the guard which answers `device:revoked` or `user:locked`; `golem device enrol|passcode|unlock|lock|list|revoke|serve|status`; and a `golem status` line reporting where the write surface is bound and how many devices are paired (invariant 9). **Revocation is effective on the next request because the catalog is read every time and never cached — the absence of a cache IS the mechanism.** **The user factor is MEASURED, not assumed** (§146): passkeys are impossible here, and not for the usual reason — the secure-context rule is solvable since an `https://` origin the device trusts IS a secure context, but WebAuthn's `rp.id` must equal the origin's effective domain or a domain suffix thereof, and a LAN origin's effective domain is an IP literal, which is not a domain. No amount of CA trust changes a naming rule. The passcode is the MECHANISM until R13.10 brings a real domain; ADR-0007 §7b updated to say so. +31 unit tests, +13 integration tests over REAL TLS handshakes; **3181 passed / 2 skipped / 0 failed**. Verified live from a built `dist/`. The write application itself is deliberately absent — R13.4 builds the gate plus one demonstrating route, and `startWriteServer` takes an injected handler for R13.3/R13.5. Debrief: `docs/wiki/debriefs/2026-08-29-R13.4-device-and-user-auth.md`. |

| **R13.3 — the session host: Golem spawns, supervises, and can refuse outright** | 2026-08-29 | The load-bearing task of the R13 series. `golem session host start "…"` spawns the `claude` CLI as ONE long-lived process over `-p --input-format stream-json --output-format stream-json --verbose` (R13.1's ruled verdict, re-confirmed on client `2.1.246`), with `ANTHROPIC_BASE_URL` pointed at Golem's proxy so a hosted session gets the same redaction, telemetry and limits as anything else (invariant 8, no side door). **The finding that changed the design**: the brief assumed the host would reuse R12.12's `PermissionRequest` deny, and measurement said no — that event fires only when Claude Code is about to ASK for permission, and in a headless session under `--permission-mode default` most calls never ask. A `PermissionRequest` deny hook was installed and a plain `echo` in the session's own cwd ran to completion **with the hook never firing**, proven by having it append to a file before deciding. `PreToolUse` fires before every call and its FLAT `permissionDecision: "deny"` stops it with the reason delivered to the model as the tool result. R12.12 is not wrong — its problem was a DIALOG opening and a channel answering it, and `PermissionRequest` is what precedes a dialog. Two problems, two events. **The gate is the host's own**: spawned with `--settings <inline JSON>`, measured to wire hooks for a project that has none — without it, enforcement would be one `golem autonomy unwire` away from a session Golem spawned running ungated. **Two enums, one matrix**: `GateEmission` stays `allow|ask|null` (an honest account of what a GUEST can do — widening it would tell every hook call site that refusal is on the table for the guest path), while `HostDecision` adds `deny`; but `decideHostGate` DERIVES from `decideGate` rather than restating it, after a hand-written copy mapped `manual` to `ask` and — with nobody attached — denied every **read**, making a hosted session useless at the default autonomy level. `null` (defer) means "Golem adds no restriction", not "ask a human"; a human to defer to is exactly what a hosted session lacks. An unanswered `ask` is a **refusal, not a wait** (invariant 3). Attribution is written **before** the relay (invariant 4) to a host log separate from the tool-shaped autonomy log, and every decision is recorded, `allow` included. Fail-CLOSED, the opposite of the guest hooks: a crash denies, because there is no human permission flow to fall back to. Also fixed: a commander `--dir` collision — the same quirk R13.2 hit at `session forget`, one level deeper — that silently hosted a session in the WRONG project. **Demonstrated live against the real runner** (§147 item 6): the session tried `rm -rf ./victim`, was refused with Golem's reason, reported it accurately, explicitly declined to route around it, and the file survived; the audit trail shows STARTED → TURN(local) → ALLOW(read) → DENY(destructive) → STOPPED. +18 unit, +18 integration tests spawning a real process that speaks the real protocol; **3217 passed / 2 skipped / 0 failed**. Deliberately NOT here: detaching a session from its CLI invocation, an answerer for `ask` (R13.5/R13.6 — the `HostAttachment` seam is tested on both branches), and a non-Claude runner. Debrief: `docs/wiki/debriefs/2026-08-29-R13.3-session-host.md`. |

| **R13.5 — the transport: SSE down, POST up, and two limits that disagreed** | 2026-08-29 | The wire between a hosted session and a device (ADR-0007 §7c), mounted BEHIND R13.4's write server so it carries no authentication of its own — two places deciding who may write is one too many. SSE plus POST rather than a WebSocket, which is what holds the five-dependency pin: `text/event-stream` is a content type and a newline convention and `node:http` already has both. Event shapes are a FROZEN CONTRACT in `src/interfaces/session-events.ts`, because R13.6 and everything after code against them and leaving them implicit in a server file would make each future client an archaeology exercise. **Every event carries a monotonic `seq`**, which is what makes reconnect-without-loss-or-duplication possible at all; a client resumes via `Last-Event-ID` (what `EventSource` sends itself) OR `?after=` (what an explicit client controls), and both are read because ignoring either silently restarts one kind of client from nothing. **Connection state is an EVENT, never an inference** — ADR-0006's "a dropped link shows not connected" inherited for conversation, where the same failure looks like a message the user believes they sent; heartbeats every 15s are what let silence mean "still here" rather than "gone". A replay **gap is REPORTED** (`gap: true`) when the bounded 500-event ring no longer holds the cursor: a gap the user can see is recoverable, one they cannot is a conversation they will misread. **Backpressure drops the CLIENT, never the session** — one phone's slow radio must not stall the agent, and the ring still holds what a dropped subscriber missed, so the cost is a reconnect rather than data. **Acknowledgement means DELIVERED**, not accepted (§3b would have been undone by one optimistic 200): the POST resolves only once the turn reaches the session, a refusal is a 502, and attribution is written and awaited BEFORE delivery (invariant 4) so a failure to record is a failure to send. **Idempotent by `messageId`** — a retry after a dropped connection returns `duplicate` without delivering again, because a duplicated instruction to an agent is not a duplicated packet; the ledger is bounded, a retry window rather than a history. **The bug found on the way**: R13.4's 8 KiB body cap (sized for an enrolment claim) silently overrode R13.5's 32,000-character message limit AND enforced itself by destroying the socket, so at the client an oversized message was indistinguishable from a crash or a dropped network — the server cap now sits at 128 KiB so the handler's own limit is the one that speaks, and overflow answers 413 naming the limit. Also raised a LOCAL timeout ceiling on `runtime-assets-drift.test.ts`, which walks all of `src/` twice and had gone ~50/50 at 6.1s against the 5s default under parallel load — a pre-existing fragility this task's new files pushed over the line, failing on TIME rather than content. Wired reachable as `golem session host serve`, verified live: both routes answer an unpaired caller `device:no-certificate`. +14 unit, +14 integration tests over the real mTLS server with real client certificates; **3245 passed / 2 skipped / 0 failed**, four consecutive clean runs. Not here: the UI (R13.6), device-side start/resume (R13.8), live-harness injection (R13.7), anything beyond the LAN (R13.10). Debrief: `docs/wiki/debriefs/2026-08-29-R13.5-session-transport.md`. |

| **R13.6 — the chat surface, and an XSS its own test found** | 2026-08-29 | A phone-shaped conversation view served over mutual TLS to a paired device: assistant text as it streams, tool calls as collapsed blocks (a `Read` result is not a phone screen's worth of content), refusals rendered as refusals, scrollback rehydrated from R13.2's store, an honest Stop, and a header that says out loud what this is. Self-rendered HTML, no framework, no build step — R12.5's shell inherited rather than forked. **The bug worth the whole task**: the session id was HTML-escaped in the markup and NOT escaped where it is embedded into the inline script — `JSON.stringify` is not an HTML-context escape, and the HTML parser hunts for the end-of-script tag BEFORE any JavaScript is parsed, so a string containing one closes the `<script>` element early and everything after it is markup. A real XSS in a page served to an authenticated device. It then took **three attempts to fix**, each failing identically: a correct-looking escape that the compiler read back into a literal `<`, making the call a silent no-op. Fixed by removing backslashes from the source entirely (`String.fromCharCode(92)`). **The transferable lesson is about the test, not the escape**: the assertion was on OUTPUT (the raw sequence must not appear in the page) rather than on the mechanism (that an escaper is called), and that is the only reason all three wrong implementations were caught — a mechanism test would have passed every one. Honesty rules enforced by test: the header states outright that this is **not a mirror of the terminal on the laptop** (ADR-0007 §2's specific dishonesty); there is **no approve/deny affordance at all**, not a disabled one, because gate-map item 3 is locked and a control that would always fail implies an authority the design does not grant; the send box is **removed, not greyed**, when the passcode window lapses, with the unlock command named; "Not connected" **replaces** the content rather than decorating stale turns; a scrollback gap is disclosed; the user's turn is echoed only after the server confirms DELIVERED (optimistic echo is exactly the "a message the user believes they sent" failure); and Stop's confirm says the SESSION ends, because §142 measured that SIGINT does not interrupt a running turn on Windows. The header names the session KIND — hosted vs joined (R13.7) — since those are two different things with two different capabilities. +21 tests; suite green. Live: `golem session host serve` prints the chat URL, and the chat, history and interrupt routes all answer an unpaired caller `device:no-certificate`. **What no test here can prove** — whether a mobile browser will present an installed client certificate to a LAN IP, which is the load-bearing assumption of the whole R13.4–R13.6 chain — is filed as `R13.15` (`owner: user`), with an explicit instruction not to work around a failure by weakening the device claim. Debrief: `docs/wiki/debriefs/2026-08-29-R13.6-chat-surface.md`. |
| **R14.1 — the persona registry: a bench a project declares, and a retired key that raises rather than lying** | 2026-09-02 | `KNOWN_WORKERS` was a `const` of length 1, so a project could not add a persona without a Golem release. The roster is now data: `inference.personas.<id>` with `discipline`, `model`/`target`, `prompt_file`, `tools` and `owner`, validated by `personaLayerSchema` (`.strict()`). It sits as a **leaf under `inference`, not a top-level section**, because every section in `SETTINGS_LEAVES` is a flat map of known keys from which `GolemSettings`, the `GOLEM_<SECTION>_<KEY>` env mapping, `writeSetting` and the UI model are all derived — a section keyed by user-defined ids has no leaf table to derive from and would need a parallel mechanism in each; `worker_targets` set that precedent. It is the one leaf that **merges per key** (`MERGE_PER_KEY_LEAVES` in `loader.ts`) — wholesale replacement would let a project bench silently erase the user's, and would make "this repo's bench, but downgrade the reviewer on my machine" require restating every persona. Each layer parses with **no defaults applied**, so a higher layer's implicit value cannot overwrite a lower layer's explicit one; defaults belong to the read side (`src/inference/personas.ts`), after the merge. A persona holds no credential and no `base_url` — it names a model or a target, the target names a gateway, the gateway holds the keychain reference — which is what keeps ADR-0003's threat model intact without amendment, and why there is deliberately no `key` field. `golem personas list` reports discipline, model, owner, prompt source and originating layer (`--json` for machines); `golem personas eject <id>` writes the current prompt to `.golem/personas/<id>.md` and **never overwrites** an existing file. There is deliberately **no `add`/`set` verb** — `golem config set` already writes config, and a second write path is a second place for the schema's rules to be enforced or not. **`inference.default_coder` is retired, and retirement RAISES**: its replacement is a field inside a record, not a leaf, so `assertLeafRename` rightly refuses to call it a rename — hence a new `RETIRED_SETTINGS` table separate from `SETTING_MIGRATIONS`, whose error names the file, the key, the replacement (`inference.personas.coder.model`) and the task that retired it. A warning would have reproduced exactly the failure class this module exists for: "the setting silently stops taking effect while every surface reports success". The bench ships **unstaffed** (`personas` defaults to `{}`; coder/reviewer/scribe are unstaffed defaults on the read side), so an unconfigured repo behaves exactly as before and `golem init` writes no agent file — the same inert-on-landing strategy as R9.1's `proxy.targets`. Nothing routes on it (R14.2) and nothing is generated from it (R14.3). +3 test files (`config-personas`, `cli/personas`, `inference/personas`); **3308 passed / 2 skipped / 0 failed**, 250 test files. Wiki: `docs/wiki/concepts/Persona Registry.md`. Debrief: `docs/wiki/debriefs/2026-08-30-R14-persona-teams.md` (batch-level, lands with the R14 planning docs). |
| **R14.2 — which lane staffs a persona, decided in one place instead of two** | 2026-09-02 | R13.12 settled the question for one worker; this generalises it to the whole bench without changing the answer. `src/inference/persona-lane.ts` is the ONE implementation of the chain: a persona naming a **registry target** takes the `worker` lane (Golem dispatches it itself, through the R9.1 registry and the R9.3 redaction floor, as a bounded single-shot — what a weak or local model gets, because a 4B model cannot drive an agent loop); a persona naming a **model id** takes the `agent` lane (the HARNESS runs a subagent, because an MCP server exposes tools to its client and cannot invoke the client's own tools, so no call exists that spawns a subagent — Golem generates the definition in R14.3 and, when work arrives anyway, says so); **unstaffed** means the work stays in the calling session. `coder-route.ts` now delegates here rather than keeping a second copy — a second answer to "where does this work go" is exactly what retiring `default_coder` was about. **A declared target wins the ambiguity**: `claude-sonnet-5` and `anthropic` are both bare words and nothing in the string says which set it came from, so resolution is tried against the registry FIRST and only a value resolving to no target is read as a model — silently reading a declared target's id as a model name would send work somewhere the user did not choose while reporting success, the failure R10.8's fail-closed lookup exists to prevent. **A colon-shaped id that resolves to nothing RAISES, naming both sets**: registry ids are `gateway:model` shaped, so a colon-bearing value resolving to nothing is far likelier a typo'd target than a real model, and treating it as a model would generate an agent definition pointing at a model that does not exist — surfacing later, elsewhere, as "There's an issue with the selected model". `KNOWN_WORKERS` is gone as a compile-time literal; unknown-key warnings now come from the registry. Status, statusline and `golem personas` all report the resolved lane. +2 test files (`inference/persona-lane`, rewritten `inference/workers`); **3329 passed / 2 skipped / 0 failed**, 251 test files. **Two pre-existing timing flakes surfaced and were ruled out as regressions** — `headroom-adapter` waits 400ms for a worker that dies at 300ms, and `web-fetch-budget` leaves a 70ms margin before its reserve check; both passed 19/19 in isolation and on a full re-run, and belong to the class `R13.17-test-wall-time` addresses. Debrief: `[REDACTED:high-entropy:2].md` (batch-level). |
| **brevity-progress-signal — silence is not brevity** | 2026-09-02 | Reported as "Claude isn't giving me feedback that it is working on things", and it was a setting, not the model: `brevity.level = "full"` in `.golem/settings.local.json`, whose injected directive says *"no self-narration, no recap of what you just did"*. All three active profiles ban self-narration outright — right for prose, wrong for a long agentic turn, where it produces a window of pure tool calls with no sign that anything is happening. It also put Decision 52's directive in direct contradiction with this repo's own `golem-respond-every-turn` rule, which requires visible text every turn; the proxy-injected instruction wins, because it arrives in the `system` block. **The fix is one clause in `SAFETY_TAIL`**, the shared tail that already carries the verbatim-payload and prose-style-only guards — not in the three profiles, because a rule repeated three times drifts, and because this is a safety clause of the same kind: the existing tail stops a level talking the model out of doing the work, and this stops a level talking it into silence. It permits **ONE short line** before a tool call, or before ending a turn that would otherwise be tool calls only, and withholds exactly what the profiles removed — it may not restate the request, recap finished work, or offer further help. Registers are otherwise unchanged, and `off` is still the default. `brevityEffectNote` now says the progress line is kept at every level, so setting a dial reports it. `docs/wiki/concepts/Compression Levels.md` documented the two dials but never said what a brevity level does: it now tables all four, states the three clauses no level may violate, and separates **setting the dial** (live, next request, no restart) from **changing the directive text** (a code change needing a rebuild and proxy restart, invalidating the cached system prefix once — by design, since the prefix must stay byte-stable per level or the cache flaps and costs more than the stage saves). +1 test pinning the clause at every active level. Also filed: `test-timing-flakes`, for two load-dependent suite failures found while gating R14.2. |
| **R14.6 — close-out refuses while delegated output is unreviewed, and records the spawn rather than pretending to know the bytes** | 2026-09-02 | R14.1 staffed a real bench and used it three times. The coder (Sonnet) was correct; the **scribe (Haiku) produced good prose with two factual errors on its first run and five on its second** — an invented ADR filename, an invented config value, a stale model id, a dispatch mechanism that does not exist, and the config layer precedence inverted, all fluent and all confident. The role file was tightened between the runs and the second still produced five, so this is a property of the tier, not a prompt bug. USER's conclusion (2026-08-30): **review is a gate, not a phase** — workers are dispatched to PRODUCE artefacts, and a gate runs OVER what they produced at the quality of the planner, across every artefact rather than just code, held by the interactive session, which is the only thing that can spawn a subagent at all. Reviewing already happened; it happened by luck. `src/hooks/delegation-ledger.ts` makes it recorded, and `golem task done` now REFUSES while any dispatched run is unreviewed, naming which and what agent ran it. **The ledger records DELEGATIONS, not artefacts, because per-artefact provenance is not observable and saying so beats approximating it**: a subagent's file writes go through the harness's own tools and Golem never sees them as its operations; `PostToolUse` fires for a child's calls but nothing in the payload reliably separates child from parent (the one existing signal is a `cwd` heuristic for `isolation: "worktree"`, a special case rather than a general answer); and the proxy sees model requests — which is how verification-notes §148 proved the subagent route works — but cannot attribute a request to a file path. What IS reliably observable is the **spawn**, because the parent proposes it as a tool call and `pre-tool-use.ts` already intercepts exactly that to gate headroom, so the ledger records when, what agent, and what for, and the gate asks the manager to confirm the output was reviewed. A weaker claim than per-file attribution and an honest one; per-file needs a harness signal that does not exist today. New `golem task review [id]` (`--all` to clear every outstanding run) and `--waive <reason>`, which closes without reviewing while recording the reason — deliberate, never a default, never silent. A task with nothing delegated closes exactly as before. +151 test lines; **3345 passed / 2 skipped / 0 failed**, 252 test files. Debrief: `[REDACTED:high-entropy:2].md` (batch-level). |
| **R14.3 — one generated agent definition per staffed persona, and a delete path that cannot eat your files** | 2026-09-02 | Generalises R13.12's `installCoderAgent` from one file to N: staffing a persona writes `.claude/agents/golem-<id>.md`, and `src/cli/init-personas.ts` (replacing `init-agents.ts`) owns **removal as well as writing** — deliberately, because splitting them would let a stale definition survive a config change, the failure `/golem/slider` outliving R11.1 is the precedent for. Unstaffing a persona, deleting it from config, or restaffing it to a **worker-lane target** all take the file away, since a worker-lane persona is dispatched by Golem and has no subagent to define. **The delete path is the only genuinely new problem at N, and the rule is that the LEDGER decides, not the prefix.** Four conditions must all hold before anything is removed: the name matches `golem-<id>.md`, the id is no longer configured, `isUnmodifiedManaged` confirms Golem wrote the file (R9.5 managed-file provenance), and the content is unchanged since it did. A file Golem never wrote — a hand-authored `golem-foo.md`, or a same-named file from another tool — is **never deleted**; nor is one the user has edited. Both are reported as a `conflict` and left in place, because a silently reverted edit is worse than a missing file: the user's work is gone and nothing about the file says it is out of date. `pruneEmptyAgentsDir` removes `.claude/agents/` only when Golem has emptied it, never a directory holding anything else. Content is deterministic across runs so re-running `golem init` is a no-op, and no `golem/` namespace appears in generated frontmatter while verification-notes §114 caveat 5 remains open. Generated definitions for the staffed bench are committed alongside (`golem-reviewer.md`, `golem-scribe.md`, plus the ejected `.golem/personas/scribe.md`), matching R13.12's posture for `golem-coder.md`. +253 integration test lines; **3359 passed / 2 skipped / 0 failed**, 253 test files. Debrief: `[REDACTED:high-entropy:2].md` (batch-level). |
