---
task: R6.1-live
title: Live-verify the cloud provider adapters (Anthropic-native gateways and Gemini)
state: queued
owner: user
size: S
design: docs/plan/verification-notes.md §73, §77
gate: One real request each through an Anthropic-native gateway and Gemini, byte-faithfulness and streaming intact.
blocked: needs real provider credentials
depends_on: []
touches: [src/providers/]
created: 2026-07-30
updated: 2026-07-30
---

## Goal

Send one real request through each cloud provider path that shipped unverified.

## What is verified and what is not

**Live-verified:** Ollama-LAN (§74), SSE streaming vs Ollama (§75), OpenAI including
native `tool_calls` (which also live-verifies the b3 tool path), Kimi K3 via the
`openai` provider (§79).

**NOT live-verified:**
- **Case (a) — Anthropic-native gateways** (`upstream_provider` / `upstream_auth_scheme`
  header mapping, plus the `assumeCachingUpstream` fix for Claude-via-Azure). §73:
  unit-tested, no real gateway creds in-session.
- **b4 — Gemini** (`generateContent` translator, request/response/streaming/tools, the
  per-request path override for query-param `?key=` auth and `alt=sse`). §77:
  unit- and proxy-integration-verified, never against a real endpoint.

## Why this is the user's

It needs credentials.

## What to check

Byte-faithfulness at slider ≤1, streaming end-to-end, and a tool-use round trip.
`golem account login <id>` is the only way to set a key now (Decision 47 removed the
env-var backend).

## Out of scope

Adding providers. Quota routing (21e). Anything that stores a key in a setting.
