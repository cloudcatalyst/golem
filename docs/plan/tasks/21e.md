---
task: 21e
title: Per-request capability/availability routing — multi-target proxy
state: queued
owner: user
size: L
design: docs/plan/proposals/multi-target-routing.md; docs/plan/proposals/r6-multi-provider-remote-memos.md (R6.2); ADR-0003
gate: Umbrella only — closed when R9.1, R9.2 and R9.3 land. The live-docs gate is CLEARED (verification-notes §114).
depends_on: [R9.1, R9.2, R9.3]
blocked: umbrella only — nothing to implement here; it closes when R9.1, R9.2 and R9.3 land. Agents should pick up those three, not this.
touches: [src/providers/, src/proxy/, src/config/, src/cli/]
created: 2026-07-30
updated: 2026-08-08
---

> **Superseded by `docs/plan/proposals/multi-target-routing.md` (2026-08-08).**
> The user took the three scope decisions this task was blocked on:
> **no failover** (route-on-exhaustion stays OUT, so ADR-0003's line is
> untouched), **sticky-per-conversation binding with explicit override**, and
> **agents suggest, config decides**. Phase 1 of the proposal is therefore
> unblocked without a new ToS review. See the proposal for the design, the
> layer-by-layer code changes, and the phasing.
>
> **This is now an umbrella task.** The work is split across **[R9.1]** (target
> registry), **[R9.2]** (proxy serves many targets) and **[R9.3]** (`coder` on
> any target). R9.2 and R9.3 are independent siblings of each other. The
> arbitrary-model-id verification that gated the design is **cleared** —
> `verification-notes.md §114`.

## Goal

Route an individual request to a provider based on capability or availability, rather
than switching the whole account.

## Why this is a decision, not a task

R6.2 shipped **account switching** (Decision 21d, ADR-0003 ACCEPTED) with the user's ToS
scope explicitly limited to *legitimate switching only*. **Quota evasion is OUT.**

Per-request routing sits close to that line, and route-on-exhaustion sits on it: "this
account is out, use the next one" is the exact behaviour ADR-0003 rules out. So the
blocking artifact is a Decisions Log entry that says which of these is legitimate, not
code.

What is already built and can be reused once the line is drawn: the `proxy.accounts`
registry, `resolveActiveUpstream` (fail-closed, no silent cross-account fallback), the
OS-store credential chain (Decision 46/47), and the provider translators (R6.1).

## Out of scope

Implementing route-on-exhaustion before the decision. Anything that makes exhaustion of
one account automatically consume another.
