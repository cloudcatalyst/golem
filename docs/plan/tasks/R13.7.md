---
task: R13.7
title: "Join a live session — deliver a device's message as an injected block on the conversation's next request, visibly"
state: done
owner: agent
size: L
design: ADR-0007 §3b (the mechanism and why it is second-class), §3c (authorship is unrestricted — Revision 1 removed the draft's enforcement precondition), invariants 3, 4 and 6. The injection precedent already ships — `src/pipeline/pipeline.ts` appends a brevity directive to `system` and reports `brevity.injected`. Conversation identity is `cachePrefixFingerprint` as fixed by R8.13; `src/session/session-tree.ts` already knows which conversations exist. §136 carries the five objections to the channels route and the quotes behind them.
gate: A message sent from an authenticated device lands on the target conversation's NEXT request, exactly once, is visible to the developer locally before it lands, and is attributed in the action log. With nothing queued and compression ≤ 1 the request is byte-identical to today, proven by recorded-shape tests. Demonstrated against a second harness, not only Claude Code.
depends_on: [R13.2, R13.4]
touches: [src/pipeline/, src/session/, src/proxy/, src/dashboard/server.ts, tests/integration, docs/wiki]
created: 2026-08-22
updated: 2026-09-02T15:17:35.036Z
---

## Why this exists

This is the harness-agnostic half of ADR-0007, and the reason the proxy is a
better seam than a channel: it needs no launch flag, no allowlist, no client
cooperation, and it works for anything pointed at Golem. It is also strictly
second-class, and the brief must stay honest about why — the proxy speaks only
when the harness speaks to it, so a message reaches a *looping* session in
seconds and an *idle* one never.

**Revision 1 note.** The draft gated this path on R12.12 being active, because
the draft made authorship conditional on enforcement. It no longer is: ADR-0007
§3c permits sending anywhere the device can see, and the class line governs the
resulting tool calls exactly as it governs a locally-typed turn. R12.12 is still
the right fix and still strongly recommended — it is what makes a
`destructive`/`outward` call refusable rather than merely askable in a *guest*
session — but it is no longer a gate on this task, and this task must not
reintroduce one.

## What to build

1. **A queue per conversation.** A message from a device waits, keyed to a
   conversation identity that agrees with `session-tree.ts` and R8.13's fix. If
   the conversation cannot be identified unambiguously, refuse — invariant 3, and
   §99's collision case is exactly why this is not a guess.
2. **Delivery on the next request**, as an injected block, clearly framed as a
   message from the developer's other device rather than as system text. The
   model must be able to tell who is speaking.
3. **Exactly once.** A message carries an id; a retried or re-sent delivery must
   not double-inject. A duplicated instruction to an agent is not a duplicated
   packet.
4. **Visible locally before it lands** (invariant 4). The developer at the
   keyboard can see what their device said into their session — dashboard entry,
   action log, and, where the harness gives us a surface, a visible note. Golem
   does not deliver silent instructions even from the same person.
5. **Byte-faithfulness when idle** (invariant 6). Injection is opt-in, off by
   default, and a request with nothing queued must be byte-identical at
   compression ≤ 1. This is a CLAUDE.md hard rule and it needs recorded-shape
   tests, not an argument.
6. **Honest UI semantics.** Queued-and-not-yet-delivered is a state the device
   shows as such, with the delivery condition named ("this session is idle;
   nothing will be delivered until it runs again") and the alternative offered:
   start or continue a hosted session instead (R13.3/R13.8).
7. **Optional: the channels adapter.** Where a channel is available and
   configured, delivery MAY go that route instead — but as an adapter behind the
   same queue, never as the foundation, and never as the only path. §136's five
   objections all still stand; re-read them before building this part at all, and
   it is acceptable for this task to conclude "not yet".

## Out of scope

Hosted sessions (R13.3). Any attempt to mirror or drive the harness's TUI —
`send-keys` and keystroke injection stay rejected (ADR-0007 §8). Making injection
default-on. Widening the pipeline's injection surface beyond a single framed
block. Gating the send path on anything (§3c) — the controls live in R13.9 and
they govern what happens after a message, not whether it may be sent.

## Gate detail

Recorded-shape tests proving byte-identity with nothing queued, and the exact
injected shape with something queued. An integration test that a queued message
is delivered exactly once on the next request and not twice on a retry. A
demonstration against a real second harness (any client pointed at the proxy) to
prove the harness-agnostic claim rather than asserting it — that demonstration is
part of the gate. Plus a wiki page stating the idle-session limitation in the
words a user needs, because "sent" meaning "queued" is the misunderstanding this
feature will generate.

## Outcome

shipped — device messages land on a live conversation's next request, exactly once; queue claim is exclusive-create after §148 found concurrent fs.rename both succeed on Windows
