---
task: R12.5
title: "The companion app — a phone-shaped, read-only view of the locally-hosted dashboard, installable (observe tier only)"
state: queued
owner: agent
size: S
design: ADR-0006 (R12.1) is authoritative and must be accepted first. The server already exists — `src/dashboard/server.ts` (292 lines, `/`, `/api/stats`, `/api/state`, self-rendered HTML with no framework). Spec Decision 21b: "companion app + locally-hosted web", and the local panel deliberately does NOT surface permission prompts because Claude Code already does.
depends_on: [R12.1, R12.2]
touches: [src/dashboard/server.ts, src/cli/commands/note-dashboard-watch.ts, docs/wiki]
created: 2026-08-21
updated: 2026-08-22
---

> **REDUCED 2026-08-22 (R12.11).** This task was paused 2026-08-22 (spec
> Decision 59(i)) pending R12.11, which has now reported
> (`docs/plan/verification-notes.md` §141). R12.11 found that ADR-0006
> capability 2 (remote *authorize*) is already carried first-party by
> Anthropic's channel relay, so the part of this brief that would have been
> genuinely novel — a Golem-built approve/deny surface — is subsumed rather
> than needed. **This task is reduced to capability 1 (observe) only**: a
> responsive, installable, read-only view. The approve/deny bullet, R12.3's
> gate dependency, and R12.4's mTLS-pairing dependency below are struck for
> that reason — not because pairing/auth for a read-only view is solved. It
> is not: whether an observe-only phone view needs its own (lighter-weight)
> pairing mechanism, given that R12.4's mTLS work was built to carry
> capability 2's authority and is cancelled with it, is an open design
> question for whoever picks this task up, not something R12.11 answered.

## Start from web, and say why

The dashboard is already a self-rendered, dependency-free HTML page served from
`127.0.0.1`. Made responsive and installable to a home screen, that is a companion
app on every phone platform with no store, no signing, no second toolchain, and no
new dependency — which is the same discipline that kept ink and React out
(Decision 51) and keeps the runtime at five deps.

**A native shell earns its place only if it buys something the web cannot**, and
there is exactly one candidate: a notification that arrives when the app is
closed. That is R12.6's question, deliberately separated so this task is not held
hostage to it. Do not build a native app in this task; if R12.6 concludes a native
shell is required, that is its own task with its own ADR paragraph.

## What the screen has to get right

- **The block, legibly.** Project, what is being asked, the exact argument, how
  long it has been waiting — from R12.2's read model. A phone screen that says
  "waiting on your input" is a notification, not a control.
- ~~**Approve / Deny, and nothing that looks like them.**~~ **Struck 2026-08-22
  (R12.11): out of scope for the reduced brief.** Anthropic's channel relay
  already carries remote authorize; do not build an approve/deny surface here.
- ~~**A visible deadline** (R12.3)~~ **Struck with R12.3, which is cancelled.**
  A read-only view may still show that something is waiting, but it answers
  nothing and starts no timeout of its own.
- **Connection state, never optimism.** A dropped link shows "not connected" —
  never a stale "approved", never a queued decision that might land later. Default
  deny on link loss is the ADR's rule and it has to be legible on the screen.
- **Read-only until paired**, so an unpaired browser can still be a dashboard.

## Out of scope

A native app or an app store. Push (R12.6). "Continue" or any prompt entry
(R12.7). **Approve/deny of any kind, and the relay and account that would have
carried it (R12.3 / R12.4 / R12.8 / R12.9 — all cancelled per R12.11).**
Reworking the desktop dashboard's own layout beyond making it responsive.

**Pairing/auth for this reduced, read-only scope is undesigned.** R12.4's mTLS
pairing existed to carry capability 2's authority; it is cancelled with
capability 2, not repurposed here. Whoever picks this task up decides whether a
read-only phone view needs any pairing at all (LAN-only + no secret may be
enough for a read surface with no write path) or a lighter mechanism — that
decision belongs to this task, not to R12.11 or to R12.4's cancelled design.

## What R12.7 settled, so you do not re-derive it (2026-08-22)

**Ship no prompt box, no "continue", no send field.** Not because nothing could
reach the session — that changed, and R12.7 found it — but for five reasons that
each survive contact with the current client. Full evidence:
`docs/plan/verification-notes.md` §136.

The short version: Claude Code `2.1.235` does have a supported reverse channel now
(**channels** — an MCP server emitting `notifications/claude/channel`; plus Remote
Control and cross-session `SendMessage`). Golem still does not use it, because it
is opt-in per session by launch flag, off the Anthropic-curated allowlist, gives
**no delivery acknowledgement** (a button that cannot know whether its message
landed can only lie), rides an explicitly unstable contract, and — decisively —
*authoring a turn is a larger authority than answering one*, which is a new USER
decision (**R12.10**), not a UI affordance this task may add.

**Put this sentence where the button is not** (same register as R12.6's
"Alerts are off — this screen only updates while it is open."):

> **There is no prompt box — this screen can answer what the agent is already
> asking, but nothing here can start a turn.**

Keep it scoped to *this screen*. "No phone can steer a session" would be false
now, so do not write it.

**If someone later asks for the enqueue affordance** (R5.1's durable queue is real
and could back it), it is R12.10's call, not yours — and it arrives under three
rules: labelled **"Add a task for later"** with the sub-line **"Nothing runs until
your laptop picks it up."**, never grouped with Approve/Deny, and with no spinner
or progress state, because the only thing that happened is that a file was
written.

## Gate

From a phone on the same network: the current project state (blocked-or-not,
what is being asked if anything, limits, telemetry) is legible and read-only,
and pulling the network shows a disconnected state rather than stale data.
Installable to a home screen on at least one platform, verified on a real
device. No control affordance of any kind ships — that is the definition of
"reduced to observe" this task's gate holds itself to.
