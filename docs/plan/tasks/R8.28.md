---
task: R8.28
title: mcp/server.ts at 2,209 lines — split into domain modules
state: queued
owner: agent
size: L
design: Fresh-eyes audit 2026-08-03. src/mcp/server.ts contains tool registrations, all 8 prompt registrations, the LSP mode handler, hit assembly, grounding, wiki boost, graph-first lookup, the SSE helper, refine-outcome formatting, and the telemetry wrapper — all in one file.
gate: src/mcp/server.ts is under ~700 lines; search/fetch/ingest, wiki_read/wiki_upsert, coder (draft/edit), code+LSP, devices, snooze, and prompts each live in their own module. No behavioral change.
depends_on: []
touches: [src/mcp/server.ts, src/mcp/]
created: 2026-08-03
updated: 2026-08-03
---

## Problem

`src/mcp/server.ts` is 2,209 lines mixing 10 distinct concerns:
- Tool registrations: expand, stats, level, search, fetch, ingest, wiki_read, wiki_upsert, coder, code, devices, snooze
- Prompt registrations: all 8
- LSP mode handler + schema
- Hit assembly (`assembleHits`) + grounding (`gatherGrounding`) + wiki boost + graph-first lookup
- Refine-outcome note formatting
- Instrumented telemetry wrapper

## Goal

Split into:
- `src/mcp/server.ts` — `createGolemMcpServer`, deps types, tool/prompt registration wiring
- `src/mcp/search.ts` — search/fetch/ingest tools + hit assembly + wiki boost + graph-first lookup + grounding (already partially in server.ts)
- `src/mcp/wiki-tools.ts` — wiki_read/wiki_upsert
- `src/mcp/coder-tools.ts` — coder (draft/edit/refine)
- `src/mcp/code-tool.ts` — code tool + LSP modes
- `src/mcp/prompts.ts` — the 8 prompt registrations
- `src/mcp/devices-snooze.ts` — devices + snooze tools

Keep the exports from `src/mcp/index.ts` stable — `createGolemMcpServer`, `createStandaloneDeps`, `GolemMcpServerDeps`, `Grounding`, `HitAssemblyDeps`, `boostWikiHits`, `gatherGrounding` must remain importable from the barrel.

## Out of scope

- Any behavioral change to the MCP tool surface
- The program.ts split (separate task R8.27)

## Verification

- MCP server smoke test: `golem mcp serve` starts, tools enumerate
- All MCP integration tests pass
- `npm run typecheck`, `npm run lint`, `npm run test`