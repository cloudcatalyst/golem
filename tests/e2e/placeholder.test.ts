/**
 * E2E suite placeholder — owned by WS-E (agent-ux) + T-C2.
 *
 * Real content: `npx eol init` -> Claude Code smoke test on all 3 OSes,
 * asserting idempotent configuration and level-1 savings > 0 on a real session.
 */

import { describe, it } from "vitest";

describe("eol init smoke", () => {
  it.skip("pending — T-C2 provides the eol init -> Claude Code smoke test", () => {});
});
